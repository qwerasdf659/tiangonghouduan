/* pages/admin/customer-service/customer-service.wxss - 管理员实时客服聊天页面样式 */

/* 🎨 全局容器 */
.customer-service-container {
  padding: 20rpx;
  background-color: #f8f9fa;
  min-height: 100vh;
}

/* 🔴 实时聊天模式样式 */

/* 聊天模式容器 */
.chat-mode-container {
  display: flex;
  flex-direction: column;
  height: calc(100vh - 180rpx);
  background: #f5f6fa;
}

/* 管理员状态栏 */
.admin-status-bar {
  display: flex;
  justify-content: space-between;
  align-items: center;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  color: white;
  padding: 20rpx 30rpx;
  border-radius: 12rpx;
  margin-bottom: 20rpx;
  box-shadow: 0 4rpx 20rpx rgba(102, 126, 234, 0.3);
}

.status-info {
  display: flex;
  align-items: center;
  gap: 12rpx;
}

.status-text {
  font-size: 26rpx;
  font-weight: 500;
}

.status-badge {
  font-size: 22rpx;
  padding: 6rpx 12rpx;
  border-radius: 16rpx;
  background: rgba(255, 255, 255, 0.25);
  backdrop-filter: blur(10rpx);
}

.connection-info .ws-status {
  font-size: 22rpx;
  padding: 6rpx 12rpx;
  border-radius: 16rpx;
  backdrop-filter: blur(10rpx);
}

.ws-status.connected {
  background: rgba(46, 204, 113, 0.25);
  color: #2ecc71;
}

.ws-status.disconnected {
  background: rgba(231, 76, 60, 0.25);
  color: #e74c3c;
}

/* 聊天工作台 */
.chat-workspace {
  display: flex;
  flex: 1;
  gap: 20rpx;
  min-height: 0;
  transition: all 0.3s ease;
}

.chat-workspace.expanded {
  gap: 0;
}

/* 会话列表面板 */
.sessions-panel {
  width: 420rpx;
  background: white;
  border-radius: 16rpx;
  box-shadow: 0 2rpx 20rpx rgba(0, 0, 0, 0.06);
  display: flex;
  flex-direction: column;
  overflow: hidden;
}

.panel-header {
  padding: 24rpx 30rpx;
  border-bottom: 2rpx solid #f1f2f6;
  background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.panel-title {
  font-size: 30rpx;
  font-weight: 700;
  color: #2c3e50;
}

.session-count {
  font-size: 22rpx;
  color: #6c757d;
  background: #e9ecef;
  padding: 4rpx 12rpx;
  border-radius: 12rpx;
}

.loading-sessions,
.empty-sessions {
  padding: 60rpx 20rpx;
  text-align: center;
  color: #95a5a6;
}

.empty-icon {
  font-size: 64rpx;
  display: block;
  margin-bottom: 16rpx;
  opacity: 0.6;
}

.empty-text {
  font-size: 26rpx;
  color: #95a5a6;
}

.sessions-list {
  flex: 1;
  overflow-y: auto;
}

/* 会话项 */
.session-item {
  display: flex;
  align-items: center;
  padding: 16rpx 24rpx;
  border-bottom: 1rpx solid #f1f2f6;
  cursor: pointer;
  transition: all 0.3s ease;
  position: relative;
}

.session-item:hover {
  background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
}

.session-item.active {
  background: linear-gradient(135deg, #e3f2fd 0%, #f3e5f5 100%);
  border-left: 4rpx solid #667eea;
}

.session-item.active::before {
  content: '';
  position: absolute;
  left: 0;
  top: 0;
  bottom: 0;
  width: 4rpx;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
}

.session-avatar {
  width: 60rpx;
  height: 60rpx;
  border-radius: 50%;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  display: flex;
  align-items: center;
  justify-content: center;
  margin-right: 16rpx;
  flex-shrink: 0;
  box-shadow: 0 4rpx 12rpx rgba(102, 126, 234, 0.3);
}

.avatar-text {
  color: white;
  font-size: 22rpx;
  font-weight: 600;
}

.session-info {
  flex: 1;
  min-width: 0;
}

.session-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 8rpx;
}

.user-name {
  font-size: 24rpx;
  font-weight: 600;
  color: #2c3e50;
  max-width: 160rpx;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.session-time {
  font-size: 20rpx;
  color: #95a5a6;
}

.session-preview {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 8rpx;
}

.last-message {
  font-size: 20rpx;
  color: #7f8c8d;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
  flex: 1;
  max-width: 180rpx;
}

.unread-badge {
  background: linear-gradient(135deg, #e74c3c 0%, #c0392b 100%);
  color: white;
  font-size: 18rpx;
  padding: 4rpx 10rpx;
  border-radius: 12rpx;
  min-width: 28rpx;
  text-align: center;
  box-shadow: 0 2rpx 8rpx rgba(231, 76, 60, 0.3);
}

.session-status {
  margin-top: 4rpx;
}

.status-tag {
  font-size: 18rpx;
  padding: 4rpx 10rpx;
  border-radius: 10rpx;
  color: white;
  font-weight: 500;
}

.status-tag.waiting {
  background: linear-gradient(135deg, #f39c12 0%, #e67e22 100%);
}

.status-tag.active {
  background: linear-gradient(135deg, #27ae60 0%, #2ecc71 100%);
}

.status-tag.ended {
  background: linear-gradient(135deg, #95a5a6 0%, #7f8c8d 100%);
}

/* 聊天面板 */
.chat-panel {
  flex: 1;
  background: white;
  border-radius: 16rpx;
  box-shadow: 0 2rpx 20rpx rgba(0, 0, 0, 0.06);
  display: flex;
  flex-direction: column;
  overflow: hidden;
  transition: all 0.3s ease;
}

/* 展开模式的聊天面板 */
.chat-panel.expanded {
  position: fixed;
  top: 0;
  left: 0;
  width: 100vw;
  height: 100vh;
  border-radius: 0;
  margin: 0;
  padding: 0;
  box-shadow: none;
  background: #f5f6fa;
  z-index: 100;
  display: flex;
  flex-direction: column;
}

.chat-panel.expanded .chat-container {
  height: 100vh;
  display: flex;
  flex-direction: column;
}

.chat-panel.expanded .messages-container {
  flex: 1;
  overflow-y: auto;
}

.chat-panel.expanded .chat-input-container {
  position: relative;
  background: white;
  border-top: 2rpx solid #f1f2f6;
  padding: 20rpx;
}

.chat-panel.expanded .input-area {
  display: flex;
  padding: 20rpx;
  gap: 16rpx;
  align-items: flex-end;
  background: white;
}

.chat-panel.expanded .send-btn {
  min-width: 120rpx;
  height: 72rpx;
  border-radius: 36rpx;
  border: none;
  display: flex !important;
  align-items: center;
  justify-content: center;
  gap: 8rpx;
  padding: 0 20rpx;
  font-size: 28rpx;
  transition: all 0.3s ease;
  box-shadow: 0 4rpx 12rpx rgba(0, 0, 0, 0.1);
  flex-shrink: 0;
}

.chat-panel.expanded .chat-header {
  position: relative;
  background: white;
  border-bottom: 2rpx solid #f1f2f6;
  padding: 20rpx;
  flex-shrink: 0;
}

.no-session-selected {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  color: #95a5a6;
  background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
}

.no-session-icon {
  font-size: 96rpx;
  margin-bottom: 24rpx;
  opacity: 0.6;
}

.no-session-text {
  font-size: 28rpx;
  color: #95a5a6;
}

.chat-container {
  flex: 1;
  display: flex;
  flex-direction: column;
}

/* 聊天头部 */
.chat-header {
  padding: 24rpx 30rpx;
  border-bottom: 2rpx solid #f1f2f6;
  background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.back-btn {
  display: flex;
  align-items: center;
  gap: 8rpx;
  padding: 12rpx 20rpx;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  color: white;
  border-radius: 20rpx;
  cursor: pointer;
  transition: all 0.3s ease;
  box-shadow: 0 2rpx 8rpx rgba(102, 126, 234, 0.3);
}

.back-btn:hover {
  transform: translateX(-4rpx);
  box-shadow: 0 4rpx 12rpx rgba(102, 126, 234, 0.4);
}

.back-icon {
  font-size: 24rpx;
  font-weight: bold;
}

.back-text {
  font-size: 22rpx;
  font-weight: 500;
}

.chat-user-info {
  display: flex;
  flex-direction: column;
  gap: 6rpx;
}

.chat-user-name {
  font-size: 28rpx;
  font-weight: 700;
  color: #2c3e50;
}

.chat-session-id {
  font-size: 20rpx;
  color: #95a5a6;
}

.chat-actions .action-btn {
  background: linear-gradient(135deg, #e74c3c 0%, #c0392b 100%);
  color: white;
  padding: 12rpx 24rpx;
  border-radius: 20rpx;
  font-size: 22rpx;
  border: none;
  box-shadow: 0 4rpx 12rpx rgba(231, 76, 60, 0.3);
}

/* 消息容器 */
.messages-container {
  flex: 1;
  padding: 20rpx 30rpx;
  overflow-y: auto;
  background: linear-gradient(135deg, #f8f9fa 0%, #ffffff 100%);
}

.empty-messages {
  text-align: center;
  padding: 60rpx 20rpx;
  color: #95a5a6;
}

/* 消息项 */
.message-item {
  display: flex;
  margin-bottom: 24rpx;
  align-items: flex-end;
}

.message-item.admin {
  flex-direction: row-reverse;
}

.message-avatar {
  width: 56rpx;
  height: 56rpx;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  margin: 0 16rpx;
  flex-shrink: 0;
  box-shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.1);
}

.message-item.user .message-avatar {
  background: linear-gradient(135deg, #3498db 0%, #2980b9 100%);
}

.message-item.admin .message-avatar {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
}

.message-avatar .avatar-text {
  color: white;
  font-size: 20rpx;
  font-weight: 600;
}

.message-content {
  max-width: 70%;
  display: flex;
  flex-direction: column;
}

.message-item.admin .message-content {
  align-items: flex-end;
}

/* 消息气泡 */
.message-bubble {
  padding: 16rpx 20rpx;
  border-radius: 20rpx;
  word-wrap: break-word;
  position: relative;
  box-shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.1);
  backdrop-filter: blur(10rpx);
}

.message-item.user .message-bubble {
  background: linear-gradient(135deg, #ffffff 0%, #f8f9fa 100%);
  color: #2c3e50;
  border-bottom-left-radius: 6rpx;
  border: 1rpx solid #e9ecef;
}

.message-item.admin .message-bubble {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  color: white;
  border-bottom-right-radius: 6rpx;
}

.message-text {
  font-size: 26rpx;
  line-height: 1.5;
  margin: 0;
}

.message-meta {
  display: flex;
  gap: 12rpx;
  margin-top: 6rpx;
  font-size: 18rpx;
  color: #95a5a6;
}

.message-item.admin .message-meta {
  justify-content: flex-end;
}

.message-time {
  opacity: 0.8;
}

.message-status {
  font-weight: 500;
}

.message-status.sending {
  color: #f39c12;
}

.message-status.sent {
  color: #27ae60;
}

.message-status.failed {
  color: #e74c3c;
}

/* 聊天输入区域 */
.chat-input-container {
  border-top: 2rpx solid #f1f2f6;
  background: white;
}

.quick-replies-panel {
  padding: 20rpx 30rpx;
  border-bottom: 1rpx solid #f1f2f6;
  background: linear-gradient(135deg, #f8f9fa 0%, #ffffff 100%);
}

.quick-reply-item {
  padding: 12rpx 20rpx;
  margin: 6rpx 0;
  background: linear-gradient(135deg, #e9ecef 0%, #f8f9fa 100%);
  border-radius: 20rpx;
  cursor: pointer;
  transition: all 0.3s ease;
  border: 1rpx solid #dee2e6;
}

.quick-reply-item:hover {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  color: white;
  transform: translateY(-2rpx);
  box-shadow: 0 4rpx 12rpx rgba(102, 126, 234, 0.3);
}

.quick-reply-item text {
  font-size: 24rpx;
  color: inherit;
}

.input-toolbar {
  padding: 16rpx 30rpx;
  border-bottom: 1rpx solid #f1f2f6;
  background: linear-gradient(135deg, #f8f9fa 0%, #ffffff 100%);
}

.toolbar-btn {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  color: white;
  padding: 10rpx 20rpx;
  border-radius: 16rpx;
  font-size: 22rpx;
  border: none;
  box-shadow: 0 2rpx 8rpx rgba(102, 126, 234, 0.3);
}

/* 输入区域 */
.input-area {
  display: flex;
  padding: 20rpx 30rpx;
  gap: 16rpx;
  align-items: flex-end;
  background: white;
}

.message-input {
  flex: 1;
  min-height: 72rpx;
  max-height: 160rpx;
  padding: 16rpx 20rpx;
  border: 2rpx solid #e9ecef;
  border-radius: 24rpx;
  font-size: 26rpx;
  background: #f8f9fa;
  resize: none;
  transition: all 0.3s ease;
}

.message-input:focus {
  border-color: #667eea;
  background: white;
  box-shadow: 0 0 0 4rpx rgba(102, 126, 234, 0.1);
}

/* 发送按钮 */
.send-btn {
  min-width: 120rpx;
  height: 72rpx;
  border-radius: 36rpx;
  border: none;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 8rpx;
  padding: 0 20rpx;
  font-size: 28rpx;
  transition: all 0.3s ease;
  box-shadow: 0 4rpx 12rpx rgba(0, 0, 0, 0.1);
  background: #e9ecef;
  color: #95a5a6;
}

.send-btn.active {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  color: white;
  transform: scale(1.05);
  box-shadow: 0 6rpx 20rpx rgba(102, 126, 234, 0.4);
}

.send-btn.disabled {
  background: #e9ecef;
  color: #95a5a6;
  transform: scale(1);
  box-shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.05);
}

.send-icon {
  font-size: 24rpx;
}

.send-text {
  font-size: 26rpx;
  font-weight: 500;
}

.typing-indicator {
  padding: 12rpx 30rpx;
  text-align: center;
  font-size: 22rpx;
  color: #95a5a6;
  background: linear-gradient(135deg, #f8f9fa 0%, #ffffff 100%);
  border-top: 1rpx solid #f1f2f6;
}

/* 今日统计 */
.today-stats {
  margin-top: 20rpx;
  background: white;
  border-radius: 16rpx;
  box-shadow: 0 2rpx 20rpx rgba(0, 0, 0, 0.06);
  overflow: hidden;
}

.stats-header {
  padding: 24rpx 30rpx;
  border-bottom: 2rpx solid #f1f2f6;
  background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
}

.stats-title {
  font-size: 28rpx;
  font-weight: 700;
  color: #2c3e50;
}

.stats-grid {
  display: flex;
  padding: 24rpx 0;
}

.stat-card {
  flex: 1;
  text-align: center;
  padding: 0 20rpx;
  position: relative;
}

.stat-card:not(:last-child)::after {
  content: '';
  position: absolute;
  right: 0;
  top: 20%;
  bottom: 20%;
  width: 1rpx;
  background: linear-gradient(to bottom, transparent, #e9ecef, transparent);
}

.stat-card .stat-number {
  font-size: 32rpx;
  font-weight: 700;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  background-clip: text;
  -webkit-background-clip: text;
  color: transparent;
  display: block;
  margin-bottom: 8rpx;
}

.stat-card .stat-label {
  font-size: 22rpx;
  color: #95a5a6;
  display: block;
}

/* 🔧 聊天面板调试工具样式 */

/* 工具栏调试按钮 */
.debug-toggle-btn {
  background: linear-gradient(135deg, #ff6b6b 0%, #ee5a52 100%);
  color: white;
  border: 2rpx solid #ff4757;
}

.debug-toggle-btn:active {
  background: linear-gradient(135deg, #ee5a52 0%, #c44569 100%);
  transform: scale(0.95);
}

/* 聊天调试面板 */
.chat-debug-panel {
  margin-top: 16rpx;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  border-radius: 12rpx;
  overflow: hidden;
  border: 2rpx solid #5f6caf;
  animation: slideInUp 0.3s ease-out;
}

@keyframes slideInUp {
  from {
    opacity: 0;
    transform: translateY(30rpx);
  }

  to {
    opacity: 1;
    transform: translateY(0);
  }
}

/* 调试面板头部 */
.debug-panel-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 16rpx 20rpx;
  background: rgba(255, 255, 255, 0.1);
  border-bottom: 1rpx solid rgba(255, 255, 255, 0.2);
}

.debug-panel-title {
  font-size: 24rpx;
  font-weight: 600;
  color: white;
}

.debug-status-bar {
  display: flex;
  gap: 12rpx;
}

.status-item {
  font-size: 20rpx;
  color: rgba(255, 255, 255, 0.9);
  background: rgba(255, 255, 255, 0.2);
  padding: 4rpx 8rpx;
  border-radius: 8rpx;
}

/* 快速操作按钮区域 */
.debug-quick-actions {
  display: flex;
  justify-content: space-around;
  padding: 16rpx;
  background: rgba(255, 255, 255, 0.05);
  border-bottom: 1rpx solid rgba(255, 255, 255, 0.1);
}

.debug-quick-btn {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 12rpx 8rpx;
  background: rgba(255, 255, 255, 0.15);
  border-radius: 8rpx;
  border: none;
  color: white;
  min-width: 80rpx;
  transition: all 0.3s ease;
}

.debug-quick-btn:active {
  background: rgba(255, 255, 255, 0.25);
  transform: scale(0.95);
}

.debug-quick-btn .btn-icon {
  font-size: 24rpx;
  margin-bottom: 4rpx;
}

.debug-quick-btn .btn-text {
  font-size: 18rpx;
  color: rgba(255, 255, 255, 0.9);
}

/* 详细调试面板 */
.debug-detailed-panel {
  padding: 16rpx;
}

.debug-section-compact {
  margin-bottom: 16rpx;
}

.debug-section-compact:last-child {
  margin-bottom: 0;
}

.debug-section-title-compact {
  font-size: 20rpx;
  font-weight: 600;
  color: rgba(255, 255, 255, 0.9);
  margin-bottom: 8rpx;
  display: block;
}

.debug-buttons-compact {
  display: flex;
  flex-wrap: wrap;
  gap: 8rpx;
}

.debug-btn-compact {
  font-size: 18rpx;
  padding: 8rpx 12rpx;
  background: rgba(255, 255, 255, 0.2);
  color: white;
  border-radius: 6rpx;
  border: none;
  min-width: 80rpx;
  text-align: center;
  transition: all 0.3s ease;
}

.debug-btn-compact:active {
  background: rgba(255, 255, 255, 0.3);
  transform: scale(0.95);
}

/* 紧凑的调试信息显示 */
.debug-info-compact {
  padding: 16rpx;
  background: rgba(255, 255, 255, 0.1);
  border-top: 1rpx solid rgba(255, 255, 255, 0.2);
}

.debug-info-title-compact {
  font-size: 20rpx;
  font-weight: 600;
  color: white;
  margin-bottom: 8rpx;
  display: block;
}

.debug-info-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 8rpx;
}

.debug-info-item-compact {
  font-size: 18rpx;
  color: rgba(255, 255, 255, 0.8);
  padding: 6rpx 8rpx;
  background: rgba(255, 255, 255, 0.15);
  border-radius: 4rpx;
  display: block;
  text-align: center;
}

/* 响应式调整 */
@media (max-width: 500px) {
  .debug-quick-actions {
    flex-wrap: wrap;
    gap: 8rpx;
  }

  .debug-quick-btn {
    flex: 1;
    min-width: 60rpx;
  }

  .debug-buttons-compact {
    flex-direction: column;
  }

  .debug-btn-compact {
    width: 100%;
  }

  .debug-info-grid {
    grid-template-columns: 1fr;
  }
}
