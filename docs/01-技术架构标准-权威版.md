# 餐厅抽奖系统 - 技术架构审查标准

**文档性质**: 代码审查检查清单  
**适用范围**: Pull Request审查、代码质量检查  
**强制级别**: ⚠️ **P0 - 不可妥协**  
**最后更新**: 2025-01-20

---

## 🚫 零兼容三原则（不可妥协）

### 1️⃣ 零兼容原则

- ❌ 禁止：410 Gone引导、兼容路由、特性开关、版本参数控制
- ✅ 要求：旧API删除后自然返回404，无任何过渡代码

### 2️⃣ 零残留原则

- ❌ 禁止：注释代码、备份文件(.bak/.old)、TODO/FIXME标记
- ✅ 要求：删除必须彻底，无任何代码残留

### 3️⃣ 零技术债务原则

- ❌ 禁止：临时方案、占位实现、降级开关、"未来优化"注释
- ✅ 要求：新实现必须一步到位，完整实现所有验证逻辑

---

## 🏗️ API架构审查清单

### ✅ API域划分检查

```javascript
// 检查点1：API路径与目录名必须一致
/api/v4/market/*     ↔ routes/v4/market/
/api/v4/shop/*       ↔ routes/v4/shop/
/api/v4/lottery/*    ↔ routes/v4/lottery/
/api/v4/inventory/*  ↔ routes/v4/inventory/
/api/v4/user/*       ↔ routes/v4/user/
/api/v4/admin/*      ↔ routes/v4/admin/

// 检查点2：禁止混用词汇（如gacha/lottery同时存在）
```

### ✅ 路由参数检查

```javascript
// 检查点3：用户端禁止ID参数
❌ router.get('/user/:id/points', ...)  // 违规
✅ router.get('/user/points', authenticateToken, ...)  // 从JWT获取

// 检查点4：管理端必须使用UUID参数
✅ router.get('/admin/users/:id/points', authenticateToken, requireAdmin, ...)
```

### ✅ HTTP方法语义检查

```javascript
// 检查点5：GET不得有副作用
❌ router.get('/lottery/draw', ...)  // 违规：修改数据
✅ router.post('/lottery/draw', ...)  // 正确

// 检查点6：POST不得用于查询
❌ router.post('/user/query', ...)  // 违规：应用GET
```

### ✅ HTTP状态码检查

```javascript
// 检查点7：禁止410 Gone
❌ return res.status(410).json(...)  // 违反零兼容原则

// 检查点8：业务失败不得返回200
❌ return res.status(200).json({ success: false, message: '余额不足' })
✅ return res.status(400).json(ApiResponse.badRequest('余额不足', 'INSUFFICIENT_BALANCE'))

// 检查点9：参数错误不得返回500
❌ return res.status(500).json({ message: '参数错误' })
✅ return res.status(400).json(ApiResponse.badRequest('参数错误', 'INVALID_PARAMS'))
```

### ✅ 响应格式检查

```javascript
// 检查点10：必须使用统一响应格式
✅ 必需字段：success, code, message, data, timestamp, version, request_id

// 检查点11：必须使用ApiResponse工具类
❌ return res.json({ error: '错误信息' })  // 违规
❌ return res.json({ status: 'ok', result: data })  // 违规
✅ return res.status(200).json(ApiResponse.success('操作成功', data, 200))
```

---

## 🗄️ 目录结构审查清单

### ✅ 目录组织检查

```
// 检查点12：禁止技术语义目录
❌ routes/v4/unified-engine/  // 违规
✅ routes/v4/lottery/          // 业务域

// 检查点13：禁止存在（例外：backups/允许保留）
❌ docs/refactoring/
❌ reports/
❌ models/UserAssetAccount.js  // 旧模型文件

// 检查点14：Controller规模控制
✅ 150-250行：正常
⚠️ 250-300行：需要审查
❌ >300行：必须拆分
```

---

## 🧪 测试审查清单

### ✅ 测试策略检查

```javascript
// 检查点15：必须使用supertest + in-process
✅ const request = require('supertest')
✅ const app = require('../../app')
❌ const axios = require('axios')  // 违规：外部HTTP

// 检查点16：必须使用真实数据库
✅ await sequelize.sync({ force: false })
❌ const sequelize = new Sequelize('sqlite::memory:')  // 违规
❌ jest.mock('../models/User')  // 违规
```

### ✅ 测试数据检查

```javascript
// 检查点17：必须使用test_前缀
✅ const business_id = `test_${Date.now()}_${randomString()}`
❌ const business_id = `${Date.now()}`  // 违规：无前缀

// 检查点18：必须精准清理
✅ await User.destroy({ where: { mobile: { [Op.like]: 'test_%' } } })
❌ await User.destroy({ where: {} })  // 违规：清空整表
```

### ✅ 测试质量检查

```javascript
// 检查点19：禁止占位测试
❌ test('TODO: 实现登录测试', () => {})
❌ test.skip('登录测试', () => { ... })
❌ test.only('登录测试', () => { ... })  // 提交前必须移除

// 检查点20：覆盖率要求
✅ statements: ≥70%
✅ branches: ≥60%
✅ functions: ≥70%
✅ lines: ≥70%
```

---

## 💰 资产交易审查清单

### ✅ 幂等性检查

```javascript
// 检查点21：business_id必须包含业务语义 + 唯一标识
✅ const business_id = `lottery_draw_${userId}_${campaignId}_${Date.now()}_${randomString()}`
✅ const business_id = `market_purchase_${uuidv4()}`
❌ const business_id = `${Date.now()}`  // 违规：可能重复
❌ const business_id = uuidv4()  // 违规：无业务语义

// 检查点22：AssetService调用必须完整参数
✅ 必需：user_id, asset_code, delta_amount, business_id, business_type, meta
❌ 缺少business_id或business_type  // 违规
```

### ✅ 事务边界检查

```javascript
// 检查点23：业务操作和审计日志必须在同一事务内，或使用消息队列
✅ await AuditLog.create({ ... }, { transaction })  // 同一事务
✅ await auditQueue.publish('inventory.used', { ... })  // 消息队列
❌ await transaction.commit(); await AuditLog.create({ ... })  // 违规：事务外

// 检查点24：悲观锁必须有索引支持
✅ where: { item_instance_id: itemId, owner_user_id: userId }  // 有索引
❌ where: { name: itemName }  // 违规：无索引，退化为表锁
```

### ✅ 数据库约束检查

```sql
-- 检查点25：business_id必须 NOT NULL + UNIQUE
✅ ALTER TABLE lottery_draws MODIFY COLUMN business_id VARCHAR(100) NOT NULL;
✅ CREATE UNIQUE INDEX uk_lottery_draws_business_id ON lottery_draws(business_id);

-- 检查点26：必须有外键约束
✅ references: { model: 'users', key: 'user_id' }
✅ onDelete: 'RESTRICT', onUpdate: 'CASCADE'

-- 检查点27：必须有关键索引
✅ CREATE INDEX idx_item_instances_owner_status ON item_instances(owner_user_id, status);
```

---

## 🔐 身份权限审查清单

### ✅ JWT Payload检查

```javascript
// 检查点28：JWT仅包含必要信息
✅ 允许：user_uuid, mobile, nickname, status, iat
❌ 禁止：user_id, is_admin, role_level, roles  // 违规：权限必须实时查询
```

### ✅ 权限判定检查

```javascript
// 检查点29：必须从JWT获取UUID，实时查询权限
✅ const operator_uuid = req.user.user_uuid
✅ const operator = await User.findOne({ where: { user_uuid: operator_uuid }, include: roles })
❌ const isAdmin = req.user.is_admin  // 违规：信任token中的权限
❌ const { user_id, is_admin } = req.body  // 违规：信任客户端声明

// 检查点30：权限中间件必须实时查询
✅ const requireAdmin = async (req, res, next) => { 实时查询权限 }
❌ const requireAdmin = (req, res, next) => { next() }  // 违规：无验证
```

### ✅ API响应用户对象检查

```javascript
// 检查点31：必须使用标准字段
✅ 允许：id(user_uuid), mobile, nickname, roles[], role_level, role_based_admin, status
❌ 禁止：user_id, is_admin  // 违规
```

### ✅ WebSocket身份验证检查

```javascript
// 检查点32：必须在握手阶段验证JWT
✅ io.use((socket, next) => { jwt.verify(...) })
❌ io.on('connection', socket => { ... })  // 违规：无鉴权
❌ socket.on('authenticate', ...)  // 违规：连接后验证

// 检查点33：必须从JWT判定身份
✅ const user_uuid = socket.user.user_uuid; 实时查询权限
❌ socket.on('register_user', data => { const { user_type } = data })  // 违规：信任客户端
```

---

## ⏰ 时间处理审查清单

### ✅ 时区配置检查

```javascript
// 检查点34：必须统一使用北京时间（GMT+8）
✅ timezone: '+08:00', dialectOptions: { timezone: '+08:00' }
❌ timezone: 'UTC'  // 违规

// 检查点35：API时间格式必须ISO8601 + 北京时间
✅ "2025-01-20T20:19:57.000+08:00"
❌ "2025-01-20T12:19:57.000Z"  // 违规：UTC时间
❌ new Date().toLocaleString()  // 违规：时区不确定
```

### ✅ 时间戳生成检查

```javascript
// 检查点36：禁止仅使用时间戳
✅ const business_id = `lottery_${uuidv4()}`
✅ const business_id = `lottery_${Date.now()}_${randomString()}`
❌ const business_id = `lottery_${Date.now()}`  // 违规：可能冲突
```

---

## 📏 代码质量审查清单

### ✅ 日志规范检查

```javascript
// 检查点37：禁止在服务代码中使用console.*（启动阶段除外）
✅ logger.info('用户登录成功', { user_id, ip, request_id })
❌ console.log('用户登录成功:', userId)  // 违规

// 检查点38：日志必须包含request_id
✅ logger.info('处理请求', { request_id: req.id, path: req.path })
❌ logger.info('处理请求', { path: req.path })  // 违规：缺少request_id
```

### ✅ 错误处理检查

```javascript
// 检查点39：必须使用全局错误处理器
✅ app.use((err, req, res, next) => {
  return res.status(statusCode).json(ApiResponse.error(...))
})
❌ app.use((err, req, res, next) => {
  res.status(500).json({ error: err.message })  // 违规：非标准格式
})

// 检查点40：业务异常必须抛出，不得返回错误对象
✅ throw new BusinessError('余额不足', 'INSUFFICIENT_BALANCE', 400)
❌ return { success: false, message: '余额不足' }  // 违规
```

### ✅ 性能优化检查

```javascript
// 检查点41：禁止N+1查询
✅ 批量查询：findAll → 提取ID → findAll(where: { id: { [Op.in]: ids } })
❌ for (const item of items) { await Model.findOne({ where: { id: item.id } }) }  // 违规

// 检查点42：高频查询必须有索引支持
✅ CREATE INDEX idx_item_instances_owner_status ON item_instances(owner_user_id, status);
❌ SELECT * FROM item_instances WHERE owner_user_id = ? AND status = ?  // 违规：无索引
```

---

## 🔒 配置管理审查清单

### ✅ 环境变量检查

```javascript
// 检查点43：必须严格区分开发/生产环境
✅ if (process.env.NODE_ENV === 'development') { dotenv.config({ override: true }) }
✅ else { dotenv.config() }
❌ const env = process.env.NODE_ENV || 'development'  // 违规：缺省值
❌ require('dotenv').config({ override: true })  // 违规：全局override

// 检查点44：必须在启动时强制校验配置
✅ if (process.env.NODE_ENV !== 'development') { validateConfig() }
❌ function validateConfig() { ... }  // 违规：定义但不执行
```

### ✅ 健康检查检查

```javascript
// 检查点45：必须真实检查所有依赖
✅ await sequelize.authenticate()  // 数据库
✅ await redisClient.ping()  // Redis
✅ const overallStatus = allHealthy ? 'healthy' : 'degraded'
❌ res.json({ status: 'ok' })  // 违规：占位实现
```

---

## 📋 审查流程

### Pull Request审查步骤

1. **零兼容检查**：检查点1-2（禁止兼容代码、注释代码、TODO标记）
2. **API规范检查**：检查点3-11（路径、参数、方法、状态码、响应格式）
3. **目录结构检查**：检查点12-14（禁止技术目录、Controller规模）
4. **测试质量检查**：检查点15-20（in-process、真实DB、test\_前缀、覆盖率）
5. **资产交易检查**：检查点21-27（幂等性、事务、约束）
6. **身份权限检查**：检查点28-33（JWT、权限判定、WebSocket）
7. **时间处理检查**：检查点34-36（时区、格式、时间戳）
8. **代码质量检查**：检查点37-42（日志、错误处理、性能）
9. **配置管理检查**：检查点43-45（环境变量、健康检查）

### 审查结果判定

- ✅ **通过**：所有检查点符合规范
- ⚠️ **条件通过**：非P0级别违规，需要记录技术债务
- ❌ **拒绝**：存在P0级别违规，必须修复后重新提交

---

**文档版本**: v2.0  
**生效日期**: 2025-01-20  
**适用项目**: 餐厅抽奖系统  
**维护责任**: 架构组  
**权威性**: 本文档为最高优先级审查标准
