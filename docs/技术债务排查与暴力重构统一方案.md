# 技术债务排查与暴力重构统一方案

- 排查日期：2026-07-11（北京时间）
- 数据来源：当前工作区真实代码（commit 工作态）+ 真实数据库 `restaurant_points_dev`（直连 MySQL 核对）
- 原则：不引用历史报告；不兼容旧路由/旧接口；不留过渡方案；只保留新方案
- 排查范围：后端全部（app.js / routes / services / models / middleware / utils / migrations / scripts / tests / docs / jobs / config / constants），不含 admin 前端

## 一、总体结论

项目整体较干净：无 v1/v2/v3 路由；无孤儿路由文件；135 个模型与数据库 136 张表（含 sequelizemeta）一一对应；108 条迁移文件与 sequelizemeta 执行记录完全一致；旧 AssetService / PointsService / ExchangeRateService / C2C 市场服务与表均已物理删除。

剩余债务集中四类：
1. "拆分了一半"的服务：新服务已注册但路由仍走旧服务，真双轨并存（最高风险）
2. 数据迁移完成后未拆除的兼容分支（PII 明文兜底、DIY 旧格式、旧枚举值）
3. 死代码 / 死注册 / 一次性脚本 / 与项目无关文件
4. 超大文件（服务层、路由层、模型层各一批 1000+ 行）

## 二、边界划清：合法双轨（禁动）vs 兼容残留（必清）

### 2.1 合法业务双轨——保留，不是统一对象

| 双轨 | 内容 | 判定依据 |
|---|---|---|
| 背包双轨 | assets（可叠加：account_asset_balances + AssetTransaction 账本）↔ items（不可叠加：items + item_ledger 双录 + item_holds 锁） | 两种资产形态各有完整账本闭环，BackpackService 文件头明确声明 |
| 兑换频道双轨 | item_type：prop（道具商城/星石轨）↔ product/voucher（商品兑换/源晶轨） | 产品频道设计，exchange/QueryService 有白名单校验 |
| 积分货币双轨 | points 永续积分 ↔ event_points 活动积分（独立过期 job） | 双货币经济设计 |
| 商品编码双轨 | ProductSeries 可读系列号（SLNB-001）↔ ProductBatch 批次码 | 两个正交编码维度 |
| 用户等级双轨 | users.user_level（身份角色）↔ user_growth_levels（消费成长等级） | 语义不同 |
| 兑换订单双链路 | exchange_records（B2C 商城履约）↔ redemption_orders（核销码履约） | 不同履约链 |
| 基础设施降级 | Redis 限流退化内存限流、Sealos 内网转公网、分群规则回退 default | 容错降级，非新旧并存 |
| 统计分级查询 | 今日查 lottery_draws / 7-90 天查 hourly / 90 天以上查 daily | 性能分层 |

注意误判项：`old_status`（状态机审计字段）、`old_item_ids`（以物易物"投入的旧物"参数）、抽奖 `fallback`（保底档位）均为业务词，不是旧代码。

### 2.2 兼容残留——清理对象见第三～六节

## 三、代码层清理（最高优先级）

### 3.1 半拆分服务收口（真双轨，六组）

| 功能域 | 在跑的旧实现 | 已注册但零调用的新实现 | 收口方案 |
|---|---|---|---|
| 活动预算/生命周期 | ActivityService.js（1401 行，batch-operations 路由在用） | ActivityBudgetService.js + ActivityLifecycleService.js（逻辑逐行复制，零 getService 调用） | 删两个新文件（从未跑过生产），拆分留给 ActivityService 瘦身 |
| 系统配置 | AdminSystemService.js（console/config/settings.js 走 admin_system） | SystemConfigService.js（注册 system_config，零调用） | 二选一，只留一套 getSettingsByCategory/updateSettings |
| 资产组合查询 | asset/QueryService.getAssetPortfolio()（路由在用） | asset/PortfolioQueryService.js（注册 asset_portfolio_query，零调用） | 删 PortfolioQueryService.js 及注册键 |
| SPU 库存汇总 | ExchangeItemService.syncSpuSummary()（权威） | exchange/admin/ 下 ItemManagementService、BatchOperationService、SkuService 三处私有 _updateSpuSummary() 副本 | 三处统一调用 syncSpuSummary，删私有副本 |
| 全局错误处理 | app.js L891-968 内联 handler（生产真相） | middleware/errorHandler.js（从未挂载，仅测试引用） | 删 errorHandler.js 或搬入后挂载，只留一套；合并 validation.handleServiceError 重叠职责 |
| 抽奖编排器 | UnifiedLotteryEngine 内部 new DrawOrchestrator() | services/index.js 注册 draw_orchestrator（零调用）+ console/shared/middleware.js 又 new 一个 | 只留引擎内实例，删注册键与重复实例 |

### 3.2 真实 Bug（迁移漏改，会 500）

`routes/v4/backpack/index.js:608` 使用 `getService('item_lifecycle')`，注册键为 `asset_item_lifecycle`（其余三处路由均正确）。该接口调用即 SERVICE_NOT_FOUND。必修。

### 3.3 数据迁移已完成、可拆的兼容分支（已直连库核对）

| 兼容分支 | 数据库真实状态（2026-07-11 查询） | 结论 |
|---|---|---|
| PiiCrypto.js + User.mobile 明文兜底（非 v1: 格式原样返回） | users 14 行全部 v1: 加密格式，明文 0 条；user_addresses 明文 0 条 | 改 fail-fast，删明文兜底分支 |
| diy/WorkService.js L550/L699 兼容旧 total_cost 数组格式 | diy_works 0 行 | 直接删旧格式分支，只留 { price_snapshot, payments } |
| ConsumptionRecord.store_id allowNull:true（兼容历史） | store_id NULL 0 条 | 迁移改 NOT NULL，删注释 |
| ExchangeRecord.status 枚举含 completed（历史兼容） | 21 条 status='completed'（另 38 条 pending） | 先数据迁移映射到新状态机，再删枚举值 |
| LotteryTierMatrixConfig.budget_tier 保留旧 ENUM 供回滚 | 无回滚需求 | 下次 squash 收窄枚举 |
| lottery_draws.prize_type 枚举含 item/coupon | 数据仅 material(548)/points(238)；virtual 已统一为 material | 确认业务规划后决定是否收窄 |

### 3.4 死代码文件（全库零生产引用，可删）

- services/UnifiedLotteryEngine/utils/PerformanceAnalyzer.js
- services/lottery/LotteryAlertDetectorService.js（若告警检测是需求则接入调度，否则删）
- services/asset/PortfolioQueryService.js、services/ActivityBudgetService.js、services/ActivityLifecycleService.js（见 3.1）
- utils/materialConversionValidator.js（仅测试引用，AssetConversionRuleService 实际未 require）
- middleware/errorHandler.js（见 3.1）
- constants/AssetCode.js 中 ASSET_CODE_MIGRATION_MAP（迁移映射，零引用）
- constants/TradableAssetTypes.js（C2C 黑名单，C2C 已下线，仅测试引用）

### 3.5 服务注册表清理（services/index.js，约 120 键）

死注册键（无任何 getService 调用）：activity_budget、activity_lifecycle、asset_portfolio_query、lottery_alert_detector、system_config、draw_orchestrator、lottery_container、attribute_rule_engine、diy_template / diy_work / diy_material / diy_admin_query（实际只用 diy Facade）。随对应文件一并删除。同时清理约 15 处 "V4.7.0 从 AssetService 拆分" 类历史叙事注释——注释只描述现状。

## 四、路由层清理

路由层干净：仅 /api/v4 一个版本、无重定向、无孤儿文件；console vs user 双域路由为权限拆分（广告、装饰、商家积分、员工、纠纷、资产流水、通知、字典）而非别名，不动。

清理项：
1. `/api/v4/marketplace` 410 桩（app.js L704-715）：整段删除，同步删 tests/api-contracts/market.contract.test.js 的 410 断言
2. WebSocket 死事件：ChatWebSocketService.js L1416-1472 的 C2C auction_outbid/won/lost/new_bid 四个 push 方法零调用，删除；B2C bid_* 事件在用，保留
3. 接口契约债：服务端已无 register_admin listener、无 system_alert / badge_update emit——补实现或从客户端约定移除（admin 前端不在本次排查范围，但契约需一次切齐）
4. apidoc 旧路径：console/user/user-segments.js、console/risk/consumption-anomaly.js、console/lottery/lottery-health.js、shop/staff/index.js 注释仍写 /api/v4/admin/...，实际为 /api/v4/console/...，全部改正；app.js L445 已删除的 override 决策源、L832 standard_domains 含 marketplace，一并改
5. console 模块 catalog（routes/v4/console/index.js GET /）仍列 market-health、market-listings 等 C2C 条目，删除
6. 命名债：消除 /api/v4/console/lottery/lottery/... 双前缀（console/lottery/index.js 把 ./lottery 挂在 /lottery 下）

## 五、数据库层清理（已与真实库对齐验证）

核对结果：135 模型 ↔ 135 业务表 100% 对齐，无孤儿表无缺表；108 迁移文件 = sequelizemeta 108 条，无漂移。

1. 消灭 users.history_total_points 双真相（P0）：实测 user_id=32 存 3,153,691，从 asset_transactions 重算为 2,996,320，已漂移。先修数据，再定单一写入路径（纯派生查询或唯一入口单写 + 对账任务）
2. 重新 squash baseline：当前 baseline（20260322）后已积累 107 条散装迁移，且 baseline 仍 CREATE 已被 DROP 的表（lottery_prizes、market_listings、trade_orders、exchange_rates）。项目未上线，以当前真实 schema 导出新 baseline v4.2（scripts/maintenance/export_schema_ddl.js），108 条全部归档，migrations/archived/ 63 个旧文件随之移出 git 主干（留 README 说明）。一次性数据修复迁移随 squash 消失
3. exchange_records 21 条 completed 旧状态：数据迁移映射后从枚举删除
4. feature_flags 残留：c2c_marketplace_enabled（is_enabled=0，C2C 已物理删除）连同读它的代码删除；data_pre_launch_wipe 用完同样处理
5. S1-S5 脚手架表决策：suppliers、product_series、purchase_orders、purchase_order_items、consignment_orders、product_batches、product_bundles、product_bundle_items、external_channel_mappings 约 9 张表全部 0 行、业务未接。近期接入则保留，否则从新 baseline 移除，做 S1-S5 时再建
6. consumption_records 双状态字段：status 与 final_status 并存，合并为单状态机（存量 15 条，迁移成本极低）
7. 审核双轨收敛：approval_chain_instances（业务审批唯一体系）与 content_review_records（限定内容审核域），明确边界不再互相承接

## 六、脚本 / 文档 / 测试 / 杂物清理

### 6.1 立即处理（安全 + 功能缺口）

- 删 `.env.bak.20260628_135758`（明文 DB 密码/JWT）；`git log --all -- .env.bak*` 确认从未入库，入过则轮换凭据
- jobs/daily-decoration-expiry.js 从未注册到任何调度，限时装饰到期不会自动执行——接入 scheduled_tasks.js 或删该功能

### 6.2 可直接删除

| 类别 | 文件 |
|---|---|
| 自我声明"用后即删"临时脚本 | scripts/diagnostic/_verify_doc_status*_20260711.js（3 个）、scripts/_verify_e2e_topbanner.js、scripts/_verify_accounts.js |
| 已完成的一次性修复脚本 | scripts/maintenance/fix_negative_balances.js、scripts/sealos/batch_trim_diy_materials.js |
| 零引用测试辅助 | tests/helpers/ModelAssociationManager.js、tests/config/real-users-config.js |
| 零引用数据 | data/pcas-code.json（约 6 万行，真实数据在 administrative_regions 表 44,569 行） |
| 与项目无关 | excalidraw-editor.vsix（2.8MB）、.clauderc、.claude/ |
| 运行时产物（已 gitignore，删本地/移出仓库） | dump.rdb、appendonlydir/、backups/2026-06-22/（262 个快照文件） |

### 6.3 失效/过时测试

- tests/services/ExchangeRateService.test.js：服务已并入 AssetConversionRuleService，删或改写
- tests/business/admin/market-pages-api.test.js：SKU 断言用旧 P{id}_ 编码，现行为 ProductCodeGenerator 的 SK+12 位，修断言
- tests/business/item_instance/asset_service.test.js:65：fallback require 不存在的 services/PrizePoolService（已迁 services/prize-pool/），修路径
- tests/security/sensitive-operation.test.js：敏感端点清单仍为 C2C marketplace 路由，改为当前 B2C 敏感操作
- tests/stress/capacity/capacity-baseline.stress.test.js：仍带 market_listing_id 参数，清理
- marketplace 410 契约测试随桩删除

### 6.4 npm scripts 与脚本收敛

- package.json 104 个 scripts，三组纯重复别名各留一个：health:check=quality:check=system:check、db:check=test:database、lint:mappings=check:fields
- scripts/reconciliation/（4 个）与 scripts/reconcile-items.js + 定时任务 35/43 高度重叠，合并或删除
- scripts/check_route_layer_compliance.js 声称接 pre-commit 但从未接入——接入或删除
- scripts/maintenance/scheduled_tasks.js 头部注释仍写 C2C 挂牌过期任务，与实际不符，更新

### 6.5 文档与 .gitignore

- 《商品编码体系设计方案》§17 仍标 S1-S5 "尚未开始编码"，实际迁移与 S1S5ExchangeService 已落地，修订
- 《以物易物与会员成长等级功能启用方案》§十/§十二为执行记录（含 user 32 对账差异，与本次实测漂移一致），从设计文档剥离，设计文档只描述现状
- .gitignore 补：*.vsix、.claude/、.clauderc、backups/（整目录）、data/pcas-code.json（若保留文件）

## 七、超大文件拆分

### 7.1 服务层（按收益排序）

| 文件 | 行数 | 拆分方案 |
|---|---|---|
| scripts/maintenance/scheduled_tasks.js | 3169 | 调度注册表 + 按域任务模块（lottery/exchange/media/reconciliation） |
| services/exchange/QueryService.js | 2546 | 按频道拆：商城列表 / 订单查询 / 统计查询 |
| services/ApprovalChainService.js | 2451 | 模板管理、实例流转、动作执行三子服务 + Facade |
| services/exchange/CoreService.js | 2397 | 下单事务核心保留，校验/库存/通知拆出 |
| services/DataManagementService.js | 1802 | 按数据域拆（清理/导出/擦除） |
| services/ChatWebSocketService.js | 1785 | 混合连接管理+聊天+通知+竞价+库存五职责，抽独立事件 Publisher |
| services/UnifiedLotteryEngine/pipeline/stages/SettleStage.js | 1730 | 拆奖品发放 handler（material/points 各一） |
| app.js | 1429 | 域挂载、健康检查、错误处理、定时任务启动抽 bootstrap/ 模块，app.js 只装配 |

### 7.2 模型层（Fat Model 瘦身）

- models/LotteryCampaign.js（1120 行）：约 430 行预算扣减/参与校验/健康检查方法移入 lottery 域服务，模型只留 schema + 关联
- models/ConsumptionRecord.js（1122 行）：15+ scopes 与 toAPIResponse 移到查询服务/序列化器；文件头主键 record_id 与实际 consumption_record_id 不符，修正
- models/index.js（1065 行）：删除"已废弃模型"叙事注释块

### 7.3 路由层与测试层

- Top 3 拆分：routes/v4/exchange/index.js（1086 行，multer/锁/事务/多端点混杂）、console/analytics/business-records.js（984 行）、console/config/approval-chain.js（756 行）。原则：路由只做参数校验 + 调 service + 返回
- tests/helpers/test-mock-redis.js（2084 行）评估换 ioredis-mock；seeders/comprehensive-test-data.js（1995 行）与迁移内 seed 去重

## 八、功能合并收缩（降长期维护成本）

1. 对账三合一：scripts/reconciliation/*、scripts/reconcile-items.js、定时任务 35/40/43 收敛为一套对账框架
2. 审计/审批收敛：ApprovalChain 为唯一业务审批体系；AdminOperationLog / MerchantOperationLog / BatchOperationLog 三张操作日志表若查询场景一致可统一为带 operator_type 单表（影响面大，后期）
3. 数据库工具收敛：database_toolkit.js / validation_toolkit.js / check_db_connections.js 画清边界或合并为多子命令工具
4. 零数据功能面积决策：广告反作弊/归因/计费日志、竞价（bid_products/bid_records）、客服 issue 体系、user_behavior_tracks、user_hierarchy、user_ratio_overrides 等 0 行表逐个标注"启用中/排期中/砍掉"；砍掉的模型+服务+路由+表一次删净，不留空壳

## 九、实施顺序与验收标准

1. 第一批（安全+Bug）：删 .env.bak（必要时轮换凭据）；修 backpack/index.js:608 错误 service key；接入或删 daily-decoration-expiry job
2. 第二批（收口双实现）：3.1 六组逐一二选一，删死注册键与死文件——完成后代码层无新旧并存
3. 第三批（数据+兼容分支）：修 user 32 积分漂移并收口 history_total_points 单写；迁移 21 条 completed 状态；拆 PII 明文兜底、DIY 旧格式、store_id NULL；删 c2c_marketplace_enabled flag 及相关代码
4. 第四批（路由+契约）：删 marketplace 410 桩、WS 死事件、apidoc 旧路径、console catalog 过时条目，消 /lottery/lottery 双前缀，与前端一次切齐
5. 第五批（squash+外围）：新 baseline v4.2（剔除已 DROP 表、决策 S1-S5 空表去留），归档全部旧迁移；删一次性脚本/临时文件/零引用文件；修失效测试；合并重复 npm scripts；文档修订
6. 第六批（拆大文件）：从 scheduled_tasks.js 和 exchange 域开始，每拆一个跑对应测试

每批验收标准：
- `npm run lint` + `npm test` 通过
- `grep -rn "兼容\|旧方案\|legacy\|deprecated" services/ routes/ models/ middleware/ utils/` 命中仅剩合法双轨设计说明
- `npx sequelize-cli db:migrate:status` 无漂移
