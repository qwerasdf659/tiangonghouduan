# 餐厅积分抽奖系统 - 功能重复检查报告

**生成时间**：2026年01月08日  
**数据库**：`restaurant_points_dev`（真实库，非备份）  
**代码版本**：当前工作区最新代码  
**检查范围**：接口重复、审核模块重叠、API功能重复、同一功能需求多种实现

---

## 📊 执行摘要（Executive Summary）

### 检查方法

- ✅ 连接真实数据库（`restaurant_points_dev`）获取表行数和最新数据时间
- ✅ 扫描当前代码库所有路由、服务、模型定义
- ✅ 对比"代码存在"与"数据库实际使用"，区分"历史遗留"与"活跃功能"
- ✅ 分析业务逻辑和商业模式，识别功能语义重复

### 核心发现（3大类、8个具体问题）

#### 🔴 P0级（影响功能可用性）

1. **路由冲突**：`POST /api/v4/auth/refresh` 被注册两次，权限缓存刷新接口**不可达**

#### 🟠 P1级（影响架构一致性）

2. **审计入口重复**：`AuditLogService` 与 `middleware/auditLog.js` 同时写 `admin_operation_logs`，接口不统一
3. **审核系统重叠**：`ContentReviewRecord`（0行）+ `MerchantPointsReview`（0行）+ `AdminOperationLog`（1046行）三套并存
4. **角色审计重复**：`role_change_logs`（0行）+ `AdminOperationLog.operation_type=role_change`（85条）+ `PermissionAuditLogger`（文件）三套并行

#### 🟡 P2级（技术债务）

5. **交易流水双轨**：`AssetTransaction`（846行，活跃）+ `TradeRecord`（2行，残留）+ `TradeOrder`（0行，未启用）
6. **兑换系统语义混淆**：`ExchangeService`（B2C材料兑换）+ `RedemptionService`（核销码系统）功能边界不清
7. **遗留空表**：`audit_records`（0行，无模型）、`role_change_logs`（0行，有模型但未用）

#### 📋 P3级（代码质量）

8. **路由挂载路径不一致**：`/api/v4/shop/exchange/*` 代码注释写的是 `/api/v4/exchange_market`，实际挂载路径不匹配

---

## 🔍 详细分析（按业务域分类）

### 1. 认证授权域（Auth & Permissions）

#### 1.1 路由冲突（P0）

**问题描述**：`POST /api/v4/auth/refresh` 被注册两次

**证据链**：

1. **挂载方式**（`routes/v4/auth/index.js`）：

```javascript
router.use('/', tokenRoutes) // 包含 POST /refresh
router.use('/', permissionRoutes) // 也包含 POST /refresh
```

2. **Token刷新接口**（`routes/v4/auth/token.js:104`）：

```javascript
router.post('/refresh', async (req, res) => {
  const refresh_token = req.cookies.refresh_token
  // ... 验证并生成新 access_token ...
})
```

3. **权限缓存刷新接口**（`routes/v4/auth/permissions.js:181`）：

```javascript
router.post('/refresh', authenticateToken, async (req, res) => {
  const { user_id } = req.body
  await invalidateUserPermissions(user_id, 'manual_refresh', request_user_id)
  // ... 清除权限缓存 ...
})
```

**影响**：

- ❌ 权限缓存刷新接口**完全不可达**（被 token 刷新接口遮蔽）
- ❌ 前端无法手动刷新权限缓存（权限变更后需要等缓存自然过期）
- ⚠️ 可能导致权限变更不及时生效的问题

**解决方案**（二选一）：

**方案A（推荐）**：拆分 permissions 到独立域

```javascript
// app.js
app.use('/api/v4/auth', require('./routes/v4/auth'))           // 仅认证相关
app.use('/api/v4/permissions', require('./routes/v4/permissions')) // 权限管理独立

// routes/v4/permissions/index.js（新建）
router.post('/refresh', ...)        // 变成 POST /api/v4/permissions/refresh
router.get('/me', ...)              // 变成 GET /api/v4/permissions/me
router.post('/check', ...)          // 变成 POST /api/v4/permissions/check
```

**方案B（最小改动）**：在 auth 域内使用子路径

```javascript
// routes/v4/auth/index.js
router.use('/', tokenRoutes) // POST /refresh（token）
router.use('/permissions', permissionRoutes) // POST /permissions/refresh（权限缓存）
```

**迁移风险**：

- 需要更新前端调用路径
- 需要更新 API 文档
- 建议保留旧路径 90 天并返回 301 重定向

---

### 2. 审计审核域（Audit & Review）

#### 2.1 操作审计入口重复（P1）

**问题描述**：两套代码同时写 `admin_operation_logs` 表

**证据链**：

1. **服务层入口**（`services/AuditLogService.js`）：
   - 导出方法：`logOperation()`, `logAdminOperation()`, `logPointsAdd()`, `logInventoryTransfer()`, `logConsumptionAudit()` 等
   - 调用方：`AdminLotteryService`（5处）、`PrizePoolService`（4处）、`ConsumptionService`（2处）、`UserRoleService`（1处）、`middleware/auth.js`（1处）
   - 共计：**13+ 处调用**

2. **中间件入口**（`middleware/auditLog.js`）：
   - 导出方法：`logOperation()`, `logAssetAdjust()`, `logExchangeAudit()`, `logProductConfig()`, `logUserStatusChange()`, `logPrizeConfig()`, `logCampaignConfig()`, `logRoleAssign()`
   - 调用方：**0 处直接调用**（仅在 `routes/v4/console/asset-adjustment.js` 中通过 `AuditLogService.log()` 间接调用，但该方法不存在）

3. **operation_type 词表不一致**：
   - `AuditLogService` 支持：`points_adjust`, `exchange_audit`, `role_change`, `consumption_audit`, `inventory_transfer`, `lottery_force_win`, `lottery_probability_adjust` 等（15种）
   - `middleware/auditLog.js` 支持：`points_adjust`, `exchange_audit`, `product_update`, `user_status_change`, `prize_config`, `campaign_config`, `role_assign`, `session_assign`, `inventory_operation`, `consumption_audit` 等（10种）
   - ⚠️ 部分类型名不一致（如 `role_change` vs `role_assign`）

**真实库数据**（`admin_operation_logs`）：

```json
{
  "count": 1046,
  "min_time": "2025-11-09 04:43:40",
  "max_time": "2026-01-07 08:56:23",
  "top_operation_types": [
    { "operation_type": "points_adjust", "cnt": 528 },
    { "operation_type": "session_assign", "cnt": 349 },
    { "operation_type": "role_change", "cnt": 85 },
    { "operation_type": "consumption_audit", "cnt": 81 },
    { "operation_type": "exchange_audit", "cnt": 2 },
    { "operation_type": "inventory_transfer", "cnt": 1 }
  ]
}
```

**影响**：

- ❌ 调用方不知道该用哪个入口（服务层 vs 中间件）
- ❌ `operation_type` 词表不统一，导致统计和查询困难
- ❌ `routes/v4/console/asset-adjustment.js` 调用了不存在的 `AuditLogService.log()` 方法（应为 `logOperation()`）

**解决方案**：

**统一入口（推荐）**：

- ✅ 保留 `AuditLogService` 作为**唯一服务层入口**
- ✅ `middleware/auditLog.js` 改为薄封装（仅从 `req` 提取参数，转调 `AuditLogService.logOperation()`）
- ✅ 统一 `operation_type` 词表到单一枚举定义文件（如 `constants/AuditOperationTypes.js`）
- ✅ 修复 `routes/v4/console/asset-adjustment.js` 调用错误（`log()` → `logOperation()`）

**operation_type 标准化**：

```javascript
// constants/AuditOperationTypes.js（新建）
module.exports = {
  POINTS_ADJUST: 'points_adjust',
  ASSET_ADJUSTMENT: 'asset_adjustment', // 新增（覆盖所有资产调整）
  EXCHANGE_AUDIT: 'exchange_audit',
  CONSUMPTION_AUDIT: 'consumption_audit',
  ROLE_CHANGE: 'role_change', // 统一使用此名称
  INVENTORY_TRANSFER: 'inventory_transfer',
  LOTTERY_FORCE_WIN: 'lottery_force_win'
  // ... 其他类型
}
```

#### 2.2 审核系统重叠（P1）

**问题描述**：三套"审核/审批"机制并存，边界不清

**证据链**：

| 模块                                             | 表名                      | 真实库行数 | 主要用途                                    | 代码状态                      |
| ------------------------------------------------ | ------------------------- | ---------- | ------------------------------------------- | ----------------------------- |
| `ContentReviewRecord` + `ContentAuditEngine`     | `content_review_records`  | **0行**    | 通用业务审核流（兑换/图片/反馈）            | ✅ 代码完整，但**未实际使用** |
| `MerchantPointsReview` + `MerchantReviewService` | `merchant_points_reviews` | **0行**    | 商家扫码审核冻结积分                        | ✅ 代码完整，但**未实际使用** |
| `AdminOperationLog` + `AuditLogService`          | `admin_operation_logs`    | **1046行** | 操作审计日志（含 `consumption_audit` 81条） | ✅ **活跃使用中**             |

**代码层证据**：

1. **ConsumptionService 使用 ContentReviewRecord**：

```javascript
// services/ConsumptionService.js:310
await ContentReviewRecord.create({
  auditable_type: 'consumption',
  auditable_id: record.record_id,
  audit_status: 'pending',
  ...
}, { transaction })
```

2. **但审核通过/拒绝时又写 AdminOperationLog**：

```javascript
// services/ConsumptionService.js:480
await AuditLogService.logConsumptionAudit({
  operator_id: auditor_id,
  consumption_id: record_id,
  action: 'approve',
  ...
})
```

3. **MerchantReviewService 独立实现冻结-审核-结算链路**：

```javascript
// services/MerchantReviewService.js
submitReview() → freeze points → create MerchantPointsReview
approveReview() → settleFromFrozen → update status
```

**真实库数据表明**：

- ❌ `content_review_records`: **0 行**（代码写了但没跑起来）
- ❌ `merchant_points_reviews`: **0 行**（代码写了但没跑起来）
- ✅ `admin_operation_logs`: **1046 行**，其中 `consumption_audit` 有 **81 条**（说明消费审核实际走的是 AdminOperationLog）

**影响**：

- ❌ 三套审核机制代码并存，但只有一套在跑
- ❌ `ConsumptionService` 同时写两张表（`ContentReviewRecord` + `AdminOperationLog`），但前者是空表
- ❌ 未来新增审核场景时，开发者不知道该用哪套

**✅ 已确定方案（2026-01-09 决策）**

**架构定位**：

1. **操作审计层**：`AdminOperationLog` + `AuditLogService`
   - 职责：记录所有关键操作的审计日志（不可变、append-only）
   - 用途：合规追溯、操作回溯、安全审计
   - 保持现状：继续作为审计真相源

2. **业务审批流层**：`ContentReviewRecord` + `ContentAuditEngine`（统一审批流）
   - 职责：所有需要"待审核→审核中→已审核/已拒绝"状态机的业务
   - 适用场景：
     - ✅ 消费审核（`ConsumptionService`）
     - ✅ 商家积分审核（`MerchantReviewService`）
     - ✅ 未来其他审批流场景（图片审核、反馈审核等）
   - **强制要求**：启用并作为唯一审批流入口

3. **商家积分审核**：纳入统一审批流
   - 废弃 `MerchantPointsReview` 独立表
   - 改用 `ContentReviewRecord`（`auditable_type = 'merchant_points'`）
   - 保留冻结-结算业务逻辑，但状态管理统一走 `ContentReviewRecord`

**实施要点**：

1. **启用 ContentReviewRecord**：
   - 修复 `ConsumptionService` 中的审批流逻辑（确保 `ContentReviewRecord` 正常写入）
   - 将 `MerchantReviewService` 迁移到 `ContentReviewRecord`
   - 所有审批操作必须先更新 `ContentReviewRecord.audit_status`
   - 审批完成后再写 `AdminOperationLog`（审计日志）

2. **双写规范**：

   ```javascript
   // 标准审批流程
   async function approveReview(reviewId, auditorId, options = {}) {
     const { transaction } = options

     // 1. 更新审批流状态（业务层）
     const review = await ContentReviewRecord.update(
       {
         audit_status: 'approved',
         auditor_id: auditorId,
         audited_at: new Date()
       },
       {
         where: { review_id: reviewId },
         transaction
       }
     )

     // 2. 执行业务逻辑（如结算积分、发货等）
     await executeBusinessLogic(review, { transaction })

     // 3. 写入审计日志（追溯层）
     await AuditLogService.logOperation({
       operator_id: auditorId,
       operation_type: 'consumption_audit', // 或 'merchant_audit'
       target_type: 'ContentReviewRecord',
       target_id: reviewId,
       action: 'approve',
       transaction
     })
   }
   ```

3. **废弃 MerchantPointsReview**：
   - 删除模型：`models/MerchantPointsReview.js`
   - 删除服务：`services/MerchantReviewService.js`（业务逻辑迁移到 `ContentAuditEngine`）
   - 删除路由：`routes/v4/merchant/reviews.js`（路由保留但改用 `ContentAuditEngine`）
   - 删除表：`DROP TABLE merchant_points_reviews`

**数据迁移**：

- `content_review_records`：空表，无需迁移
- `merchant_points_reviews`：空表，无需迁移
- 历史审计日志（`admin_operation_logs`）：保留不动

#### 2.3 角色审计重复（P1）

**问题描述**：角色变更审计有三套并行机制

**证据链**：

| 模块                    | 存储位置                           | 真实库行数             | 主要用途         | 代码状态                      |
| ----------------------- | ---------------------------------- | ---------------------- | ---------------- | ----------------------------- |
| `AdminOperationLog`     | `admin_operation_logs`             | **85条** `role_change` | 操作审计日志     | ✅ **活跃使用中**             |
| `RoleChangeLog`         | `role_change_logs`                 | **0行**                | 角色变更专用日志 | ✅ 代码完整，但**未实际使用** |
| `UserRoleChangeRecord`  | `user_role_change_records`         | **4行**                | 角色变更事件ID   | ✅ **活跃使用中**             |
| `PermissionAuditLogger` | `logs/permission_config_audit.log` | 文件                   | 权限配置变更日志 | ✅ **活跃使用中**             |

**真实库数据**：

```json
{
  "admin_operation_logs": {
    "role_change": 99,
    "latest": "2026-01-09"
  },
  "role_change_logs": {
    "count": 0
  },
  "user_role_change_records": {
    "count": 4,
    "latest": "2026-01-09"
  }
}
```

**代码调用**：

- `UserRoleService.updateUserRole()` → 写 `AdminOperationLog`（通过 `AuditLogService.logOperation()`）
- `HierarchyManagementService` → 写 `RoleChangeLog`（直接 `RoleChangeLog.create()`）
- `routes/v4/auth/permissions.js` → 写 `PermissionAuditLogger`（文件）
- 部分关键操作 → 写 `UserRoleChangeRecord`（业务事件ID）

**影响**：

- ❌ 角色变更审计分散在多处，查询困难
- ❌ `role_change_logs` 表有完整的外键和索引，但**从未写入过数据**（资源浪费）
- ❌ 文件日志与 DB 日志不同步，无法统一查询
- ⚠️ `user_role_change_records` 与 `admin_operation_logs` 存在 99 vs 4 的数据差距，说明并非每次角色变更都生成业务事件记录

**解决方案**：

**✅ 已确定方案（2026-01-09 决策）**：

采用**"业务事件ID + 审计日志"标准组合**架构：

- ✅ **审计真相源**：`AdminOperationLog`（已活跃，保留）
  - 记录所有角色变更操作的审计日志
  - 支持结构化查询和统计
  - `operation_type = 'role_change'`
- ✅ **业务事件主键**：`UserRoleChangeRecord`（保留并强制使用）
  - **强制要求**：每次角色变更都必须生成 `user_role_change_records` 记录
  - 提供业务级的变更事件ID（`record_id`）
  - 用于幂等控制、关联查询、审批流程
  - `AdminOperationLog.target_id` 指向 `user_role_change_records.record_id`
- ✅ **文件日志层**：保留 `PermissionAuditLogger`
  - 记录高频权限检查（不进 DB）
  - 用于性能监控和异常检测
- ❌ **废弃**：`RoleChangeLog` 表和模型
  - 从未使用，可安全删除
  - 避免与 `user_role_change_records` 形成重复

**实施要点**：

1. **强制写入规则**：所有角色变更入口必须先创建 `UserRoleChangeRecord`，再写 `AdminOperationLog`
2. **关联关系**：`AdminOperationLog.target_id` = `UserRoleChangeRecord.record_id`
3. **幂等保证**：`UserRoleChangeRecord` 包含幂等键（`idempotency_key`）
4. **查询优化**：通过 `record_id` 可快速关联审计日志和业务事件

**数据迁移**：

- `role_change_logs`：无需迁移（空表，直接删除）
- `user_role_change_records`：保留现有 4 条数据，补齐历史 `admin_operation_logs` 中缺失的业务事件记录（可选）

---

### 2. 资产交易域（Assets & Transactions）

#### 2.1 交易流水双轨（P2）

**问题描述**：资产变动记录有两套实现

**证据链**：

| 模块               | 表名                 | 真实库行数 | 主要用途                 | 代码状态                      |
| ------------------ | -------------------- | ---------- | ------------------------ | ----------------------------- |
| `AssetTransaction` | `asset_transactions` | **846行**  | 统一资产流水（账本体系） | ✅ **活跃使用中**             |
| `TradeRecord`      | `trade_records`      | **2行**    | 用户间交易记录           | ⚠️ 残留使用                   |
| `TradeOrder`       | `trade_orders`       | **0行**    | C2C 市场交易订单         | ✅ 代码完整，**计划近期上线** |

**真实库数据**：

```json
{
  "asset_transactions": {
    "count": 846,
    "min_time": "2026-01-02 20:24:20",
    "max_time": "2026-01-07 08:56:03",
    "top_business_types": [
      { "business_type": "exchange_debit", "cnt": 378 },
      { "business_type": "test_recharge", "cnt": 377 },
      { "business_type": "admin_adjustment", "cnt": 90 },
      { "business_type": "lottery_consume", "cnt": 1 }
    ]
  },
  "trade_records": {
    "count": 2,
    "trade_types": [{ "trade_type": "inventory_transfer", "cnt": 2 }],
    "time_range": "2025-11-11 ~ 2025-12-12"
  },
  "trade_orders": {
    "count": 0
  }
}
```

**代码调用分析**：

1. **AssetTransaction（主干）**：
   - 写入方：`AssetService.changeBalance()`, `AssetService.freeze()`, `AssetService.settleFromFrozen()`
   - 调用方：`UnifiedLotteryEngine`（抽奖扣分）、`ExchangeService`（兑换扣材料）、`TradeOrderService`（市场交易）、`AssetConversionService`（材料转换）、`routes/v4/console/asset-adjustment`（管理员调整）
   - 查询方：`routes/v4/assets/transactions`（用户查询流水）、`ReportingService`（统计报表）

2. **TradeRecord（残留）**：
   - 写入方：`FeeCalculator.createTradeRecord()`（但该方法在 `TradeOrderService` 中**未被调用**）
   - 查询方：`BackpackService.getTransferHistory()`（查询物品转让历史）

3. **TradeOrder（未启用）**：
   - 写入方：`TradeOrderService.createOrder()`, `completeOrder()`, `cancelOrder()`
   - 调用方：`routes/v4/market/buy.js`（市场购买接口）
   - ⚠️ 但真实库 **0 行**，说明市场交易功能**未实际运行**

**影响**：

- ⚠️ `TradeRecord` 仅剩 2 条历史数据，但代码仍在维护（`FeeCalculator`、`BackpackService`）
- ⚠️ `TradeOrder` 代码完整但未启用，占用维护成本
- ⚠️ 同一笔交易可能同时写 `AssetTransaction` + `TradeOrder`（如市场购买），但目前未实际发生

**✅ 已确定方案（2026-01-09 决策）**

**架构定位**：

- ✅ **资产变动事实**：统一用 `AssetTransaction`（已是主干，保持不变）
- ✅ **订单事实**：
  - C2C 市场：用 `TradeOrder`（**近期上线**，需验收）
  - B2C 兑换：用 `ExchangeRecord`（**近期上线**，需验收）
  - 核销系统：用 `RedemptionOrder`（已活跃，188 行）
- ✅ **TradeRecord 定位**：退为"读模型"
  - 不再写入新数据
  - 保留 2 条历史数据（标记 `is_legacy = 1`）
  - `BackpackService.getTransferHistory()` 改为查询 `ItemInstanceEvent`

**C2C 市场上线验收口径**：

1. **数据验收**：
   - `trade_orders` 表至少产生 **非 0 数据**（建议至少 10 笔测试交易）
   - `asset_transactions` 中对应的 `business_type = 'market_trade'` 流水与订单数量一致
   - `item_instances` 的 `owner_user_id` 变更记录与交易订单一致

2. **对账口径**：

   ```sql
   -- 验收SQL：订单与流水对账
   SELECT
     COUNT(DISTINCT to.order_id) AS order_count,
     COUNT(DISTINCT at.transaction_id) AS transaction_count,
     SUM(CASE WHEN to.status = 'completed' THEN 1 ELSE 0 END) AS completed_orders
   FROM trade_orders to
   LEFT JOIN asset_transactions at
     ON at.business_type = 'market_trade'
     AND at.related_order_id = to.order_id
   WHERE to.created_at >= '2026-01-09';

   -- 预期结果：order_count = transaction_count，completed_orders > 0
   ```

3. **功能验收**：
   - ✅ 用户A挂牌出售物品（`market_listings` 创建）
   - ✅ 用户B购买挂牌（`trade_orders` 创建，状态 `pending`）
   - ✅ 扣减买家钻石（`asset_transactions` 写入 `market_trade_debit`）
   - ✅ 增加卖家钻石（`asset_transactions` 写入 `market_trade_credit`）
   - ✅ 转移物品所有权（`item_instances.owner_user_id` 更新）
   - ✅ 订单完成（`trade_orders.status = 'completed'`）

**迁移策略**：

- 保留 `trade_records` 表中的 2 条历史数据
- 新增 `is_legacy` 字段标记历史数据：
  ```sql
  ALTER TABLE trade_records ADD COLUMN is_legacy TINYINT(1) DEFAULT 0;
  UPDATE trade_records SET is_legacy = 1;
  ```
- 新交易不再写入 `trade_records`
- `BackpackService.getTransferHistory()` 改为查询 `ItemInstanceEvent`（已有 236 行数据）

#### 2.2 兑换系统语义混淆（P2）

**问题描述**：`ExchangeService` 与 `RedemptionService` 功能边界不清

**证据链**：

| 服务                | 表名                | 真实库行数 | 主要用途                 | 路由路径                    |
| ------------------- | ------------------- | ---------- | ------------------------ | --------------------------- |
| `ExchangeService`   | `exchange_records`  | **0行**    | B2C 材料兑换（官方商城） | `/api/v4/shop/exchange/*`   |
| `RedemptionService` | `redemption_orders` | **188行**  | 核销码系统（12位Base32） | `/api/v4/shop/redemption/*` |

**业务流程对比**：

**ExchangeService（B2C 材料兑换）**：

1. 用户浏览商品（`exchange_items`，24 件商品）
2. 选择商品并支付材料资产（如 100 个碎红水晶）
3. 通过 `AssetService.changeBalance()` 扣减材料（写 `asset_transactions`）
4. 创建兑换订单（写 `exchange_records`）
5. 发货和订单状态管理

**RedemptionService（核销码系统）**：

1. 用户已拥有物品实例（`item_instances`，431 件）
2. 生成 12 位核销码（写 `redemption_orders`，188 条）
3. 商家扫码核销（更新订单状态 + 物品状态）

**真实库数据表明**：

- ✅ `redemption_orders`: **188 行**（核销系统在跑）
- ❌ `exchange_records`: **0 行**（兑换系统未启用）
- ✅ `asset_transactions`: **378 条** `exchange_debit`（但没有对应的 `exchange_records`）

**矛盾点**：

- ⚠️ `asset_transactions` 有 378 条 `exchange_debit` 流水，但 `exchange_records` 是空表
- ⚠️ 说明材料扣减在执行，但订单记录未创建（可能是测试代码/或订单创建失败）

**影响**：

- ❌ 两个服务名字都叫"兑换"（Exchange/Redemption），但实际是两个完全不同的业务
- ❌ 前端/文档可能混淆两者的使用场景
- ⚠️ `ExchangeService` 代码完整但未实际运行，占用维护成本
- 🔴 **数据一致性严重问题**：有扣款流水但无订单记录，无法对账

**✅ 已确定方案（2026-01-09 决策）**

**架构定位**：

- ✅ `ExchangeService`（材料商城）：**要产生真实订单**
  - 职责：B2C 官方商城，用户用材料兑换实物商品
  - 路由：`/api/v4/shop/exchange/*`（保持不变，但文档明确说明）
  - **强制要求**：`exchange_records` 必须产生数据
- ✅ `RedemptionService`（核销服务）：保持原名
  - 职责：为已拥有的物品生成核销码，商家扫码核销
  - 路由：`/api/v4/shop/redemption/*`（保持不变）

**数据一致性修复（P0 优先级）**：

1. **问题定位**：
   - 378 条 `exchange_debit` 流水 vs 0 条 `exchange_records` 订单
   - 说明 `ExchangeService.exchangeItem()` 中的订单创建逻辑有 bug 或被跳过

2. **修复策略**：

   **A. 测试数据隔离**（推荐）：

   ```sql
   -- 1. 标记测试流水
   ALTER TABLE asset_transactions ADD COLUMN is_test_data TINYINT(1) DEFAULT 0;

   -- 2. 将现有 exchange_debit 标记为测试数据
   UPDATE asset_transactions
   SET is_test_data = 1
   WHERE business_type = 'exchange_debit'
     AND created_at < '2026-01-09';

   -- 3. 统计查询时排除测试数据
   SELECT COUNT(*) FROM asset_transactions
   WHERE business_type = 'exchange_debit'
     AND is_test_data = 0;  -- 生产数据
   ```

   **B. 测试数据迁移到测试库**（更彻底）：

   ```sql
   -- 1. 导出测试数据
   SELECT * FROM asset_transactions
   WHERE business_type = 'exchange_debit'
     AND created_at < '2026-01-09'
   INTO OUTFILE '/tmp/exchange_test_data.csv';

   -- 2. 删除生产库测试数据
   DELETE FROM asset_transactions
   WHERE business_type = 'exchange_debit'
     AND created_at < '2026-01-09';

   -- 3. 导入到测试库
   -- LOAD DATA INFILE '/tmp/exchange_test_data.csv'
   -- INTO TABLE restaurant_points_test.asset_transactions;
   ```

3. **修复订单创建逻辑**：
   ```javascript
   // services/ExchangeService.js
   async function exchangeItem(userId, itemId, quantity, options = {}) {
     const { transaction, idempotency_key } = options

     // 1. 扣减材料资产
     const assetTxn = await AssetService.changeBalance({
       user_id: userId,
       asset_code: item.cost_asset_code,
       amount: -item.cost_amount,
       business_type: 'exchange_debit',
       idempotency_key: `${idempotency_key}_asset`,
       transaction
     })

     // 2. 创建兑换订单（必需，不可跳过）
     const exchangeRecord = await ExchangeRecord.create({
       order_no: generateOrderNo(),
       user_id: userId,
       item_id: itemId,
       quantity: quantity,
       cost_asset_code: item.cost_asset_code,
       cost_amount: item.cost_amount,
       status: 'pending',
       asset_transaction_id: assetTxn.transaction_id, // 关联流水
       idempotency_key: idempotency_key,
       transaction
     })

     // 3. 验证订单已创建
     if (!exchangeRecord || !exchangeRecord.order_id) {
       throw new Error('兑换订单创建失败')
     }

     return { assetTxn, exchangeRecord }
   }
   ```

**上线验收口径**：

1. **数据验收**：
   - `exchange_records` 表至少产生 **非 0 数据**（建议至少 5 笔测试兑换）
   - `asset_transactions.exchange_debit` 与 `exchange_records` 数量一致（1:1 对应）
   - 每条 `exchange_records` 都有对应的 `asset_transaction_id`

2. **对账口径**：

   ```sql
   -- 验收SQL：订单与流水对账
   SELECT
     COUNT(DISTINCT er.order_id) AS order_count,
     COUNT(DISTINCT at.transaction_id) AS transaction_count,
     SUM(CASE WHEN er.status = 'completed' THEN 1 ELSE 0 END) AS completed_orders
   FROM exchange_records er
   LEFT JOIN asset_transactions at
     ON at.transaction_id = er.asset_transaction_id
   WHERE er.created_at >= '2026-01-09'
     AND at.is_test_data = 0;  -- 排除测试数据

   -- 预期结果：order_count = transaction_count，completed_orders > 0
   ```

3. **功能验收**：
   - ✅ 用户浏览商品（`GET /api/v4/shop/exchange/items`）
   - ✅ 用户兑换商品（`POST /api/v4/shop/exchange/exchange`）
   - ✅ 扣减材料资产（`asset_transactions` 写入 `exchange_debit`）
   - ✅ 创建兑换订单（`exchange_records` 写入，状态 `pending`）
   - ✅ 订单状态流转（`pending → processing → completed`）
   - ✅ 用户查询订单（`GET /api/v4/shop/exchange/orders`）

**强制规范**：

- ❌ **禁止**：仅扣减资产而不创建订单
- ❌ **禁止**：测试数据混入生产库（必须标记或隔离）
- ✅ **强制**：每笔兑换必须产生 `exchange_records` 记录
- ✅ **强制**：`exchange_records.asset_transaction_id` 必须关联对应的流水

---

### 3. 遗留空表（P2）

**问题描述**：数据库中存在多张从未使用的表

**证据链**：

| 表名                      | 真实库行数 | Sequelize 模型 | 代码引用                        | 建议           |
| ------------------------- | ---------- | -------------- | ------------------------------- | -------------- |
| `audit_records`           | **0行**    | ❌ 不存在      | ❌ 无引用                       | **可安全删除** |
| `role_change_logs`        | **0行**    | ✅ 存在        | ✅ `HierarchyManagementService` | **废弃或启用** |
| `content_review_records`  | **0行**    | ✅ 存在        | ✅ `ConsumptionService`         | **废弃或启用** |
| `merchant_points_reviews` | **0行**    | ✅ 存在        | ✅ `MerchantReviewService`      | **废弃或启用** |
| `trade_orders`            | **0行**    | ✅ 存在        | ✅ `TradeOrderService`          | **待启用**     |
| `exchange_records`        | **0行**    | ✅ 存在        | ✅ `ExchangeService`            | **待启用**     |
| `consumption_records`     | **0行**    | ✅ 存在        | ✅ `ConsumptionService`         | **待启用**     |

**分类处理**：

**A. 可立即删除（无代码引用）**：

- `audit_records`（无模型、无引用、0 行）

**B. ✅ 已决策（2026-01-09）**：

- `role_change_logs`：❌ **废弃删除**（改用 `UserRoleChangeRecord` + `AdminOperationLog` 组合）
- `content_review_records`：✅ **启用统一审批流**（作为唯一审批流入口）
- `merchant_points_reviews`：❌ **废弃删除**（迁移到 `ContentReviewRecord`）
- `consumption_records`：✅ **保留作为业务主表**（与 `ContentReviewRecord` 1:1 强制关联）

**C. ✅ 近期上线（已决策 2026-01-09）**：

- `trade_orders`：C2C 市场交易订单（**近期上线**，需验收至少 10 笔测试交易）
- `exchange_records`：B2C 材料兑换订单（**近期上线**，需验收至少 5 笔测试兑换，修复订单创建逻辑）

---

### 4. 路由挂载路径不一致（P3）

**问题描述**：代码注释与实际挂载路径不匹配

**证据**：

**代码注释**（`routes/v4/shop/exchange/exchange.js`）：

```javascript
/**
 * @route /api/v4/exchange_market
 * ...
 * @route POST /api/v4/exchange_market/exchange
 */
router.post('/exchange', ...)
```

**实际挂载路径**（`routes/v4/shop/index.js` + `app.js`）：

```javascript
// routes/v4/shop/index.js
router.use('/exchange', exchangeRoutes)

// app.js
app.use('/api/v4/shop', require('./routes/v4/shop'))
```

**实际生效路径**：`POST /api/v4/shop/exchange/exchange`

**影响**：

- ⚠️ 注释误导开发者（写的是 `/exchange_market`，实际是 `/shop/exchange`）
- ⚠️ API 文档可能不准确

**解决方案**：

- 批量更新所有 `routes/v4/shop/exchange/*.js` 的注释路径
- 或在 `routes/v4/shop/exchange/index.js` 顶部添加统一说明

---

## 🏗️ 业务逻辑与商业模式总结

### 核心业务流程

#### 1. 抽奖系统（Lottery）

- **用户端**：`/api/v4/lottery/*`
  - 用户消耗积分抽奖（写 `asset_transactions`，`business_type=lottery_consume`）
  - 中奖获得物品实例（写 `item_instances`）
  - 中奖获得材料资产（写 `asset_transactions`，`business_type=lottery_reward`）
- **管理端**：`/api/v4/console/lottery/*`
  - 配置奖品池、概率、保底机制
  - 强制中奖/不中奖（写 `admin_operation_logs`）
- **数据流**：`User` → `AssetService` → `AssetTransaction` → `UnifiedLotteryEngine` → `ItemInstance` / `AssetTransaction`

#### 2. 材料商城（Material Shop）

- **用户端**：`/api/v4/shop/exchange/*`
  - 浏览商品（查 `exchange_items`，24 件）
  - 用材料兑换商品（扣 `asset_transactions`，写 `exchange_records`）
- **管理端**：`/api/v4/console/material/*`
  - 配置商品、成本、库存
- **数据流**：`User` → `AssetService.changeBalance()` → `AssetTransaction` → `ExchangeService` → `ExchangeRecord`
- ⚠️ **当前状态**：`exchange_records` 是空表，但 `asset_transactions` 有 378 条 `exchange_debit`

#### 3. 核销系统（Redemption）

- **用户端**：`/api/v4/shop/redemption/*`
  - 为物品生成核销码（写 `redemption_orders`，188 条）
  - 查询核销订单
- **商家端**：`/api/v4/shop/redemption/fulfill`
  - 扫码核销（更新订单状态 + 物品状态）
- **数据流**：`ItemInstance` → `RedemptionService` → `RedemptionOrder` → `ItemInstance.status=used`

#### 4. C2C 市场（Market）

- **用户端**：`/api/v4/market/*`
  - 挂牌出售物品（写 `market_listings`，1 行）
  - 购买他人挂牌（写 `trade_orders`，0 行）
- **数据流**：`ItemInstance` → `MarketListingService` → `MarketListing` → `TradeOrderService` → `TradeOrder` + `AssetTransaction`
- ⚠️ **当前状态**：代码完整但**未实际运行**（`trade_orders` 是空表）

#### 5. 资产管理（Assets）

- **用户端**：`/api/v4/assets/*`
  - 查询余额（查 `account_asset_balances`）
  - 查询流水（查 `asset_transactions`）
- **管理端**：`/api/v4/console/asset-adjustment/*`
  - 调整用户资产（写 `asset_transactions`，`business_type=admin_adjustment`，90 条）
- **数据流**：`User` → `Account` → `AccountAssetBalance` → `AssetTransaction`

### 商业模式分析

**核心资产类型**：

1. **POINTS（积分）**：用户主要货币，用于抽奖
2. **DIAMOND（钻石）**：C2C 市场结算货币
3. **材料资产**：
   - `red_shard`（碎红水晶）：抽奖获得，用于兑换商品
   - 其他材料：可转换为钻石

**资金流向**（基于 `asset_transactions` 分析）：

1. **用户获得积分**：
   - 管理员调整（`admin_adjustment`，90 条）
   - 测试充值（`test_recharge`，377 条）
2. **用户消耗积分**：
   - 抽奖消耗（`lottery_consume`，1 条）
3. **用户获得材料**：
   - 抽奖奖励（`lottery_reward`）
4. **用户消耗材料**：
   - 兑换商品（`exchange_debit`，378 条）

**商业闭环**：

- 用户充值/获得积分 → 抽奖获得材料 → 兑换实物商品 → 核销提货
- C2C 市场：用户间交易物品（用钻石结算）

---

## 📋 去重整合方案（Consolidation Proposal）

### 方案总览

| 问题类别     | 优先级 | 整合目标                              | 预期收益             |
| ------------ | ------ | ------------------------------------- | -------------------- |
| 路由冲突     | P0     | 拆分/重命名 `/refresh` 接口           | 恢复权限缓存刷新功能 |
| 审计入口重复 | P1     | 统一到 `AuditLogService`              | 简化调用、统一词表   |
| 审核系统重叠 | P1     | 确定单一审核架构                      | 减少维护成本         |
| 角色审计重复 | P1     | 统一到 `AdminOperationLog` + 文件日志 | 删除空表、统一查询   |
| 交易流水双轨 | P2     | 确定 `AssetTransaction` 为主干        | 简化对账逻辑         |
| 兑换系统混淆 | P2     | 明确命名和职责                        | 减少理解成本         |
| 遗留空表     | P2     | 删除/废弃未使用的表                   | 减少数据库维护成本   |

---

### 方案1：修复路由冲突（P0）

**目标**：解决 `POST /api/v4/auth/refresh` 路由冲突

**实施步骤**：

1. **拆分权限管理路由**（推荐方案A）：

   ```javascript
   // app.js（新增挂载）
   app.use('/api/v4/permissions', require('./routes/v4/permissions'))

   // routes/v4/permissions/index.js（新建）
   const router = express.Router()
   router.use('/', require('./auth/permissions')) // 复用现有代码
   module.exports = router
   ```

2. **更新前端调用路径**：
   - `POST /api/v4/auth/refresh` → Token 刷新（保持不变）
   - `POST /api/v4/permissions/refresh` → 权限缓存刷新（新路径）

3. **兼容性处理**（可选）：
   - 保留旧路径 90 天，返回 301 重定向
   - 在响应头添加 `X-Deprecated-Path: true`

**验证方法**：

```bash
# 验证两个接口都可达
curl -X POST http://localhost:3000/api/v4/auth/refresh \
  -H "Cookie: refresh_token=xxx"

curl -X POST http://localhost:3000/api/v4/permissions/refresh \
  -H "Authorization: Bearer xxx" \
  -d '{"user_id": 123}'
```

**风险**：

- 需要更新前端代码（影响范围：权限管理相关页面）
- 需要更新 API 文档

---

### 方案2：统一审计入口（P1）

**目标**：确定唯一写入 `admin_operation_logs` 的入口

**实施步骤**：

1. **确定 `AuditLogService` 为唯一服务层入口**：
   - 保留 `services/AuditLogService.js`
   - 所有服务层调用统一使用 `AuditLogService.logOperation()`

2. **middleware/auditLog.js 改为薄封装**：

   ```javascript
   // middleware/auditLog.js（改造）
   const AuditLogService = require('../services/AuditLogService')

   exports.logOperation = async (req, operationType, ...) => {
     // 仅从 req 提取参数，转调 AuditLogService
     return AuditLogService.logOperation({
       operator_id: req.user.user_id,
       operation_type: operationType,
       ip_address: req.ip,
       user_agent: req.get('User-Agent'),
       ...
     })
   }
   ```

3. **统一 operation_type 词表**：

   ```javascript
   // constants/AuditOperationTypes.js（新建）
   module.exports = {
     POINTS_ADJUST: 'points_adjust',
     ASSET_ADJUSTMENT: 'asset_adjustment', // 新增
     EXCHANGE_AUDIT: 'exchange_audit',
     CONSUMPTION_AUDIT: 'consumption_audit',
     ROLE_CHANGE: 'role_change',
     INVENTORY_TRANSFER: 'inventory_transfer',
     LOTTERY_FORCE_WIN: 'lottery_force_win',
     LOTTERY_PROBABILITY_ADJUST: 'lottery_probability_adjust'
     // ... 其他类型
   }
   ```

4. **修复错误调用**：
   - `routes/v4/console/asset-adjustment.js:119`：`AuditLogService.log()` → `AuditLogService.logOperation()`
   - 添加 `asset_adjustment` 到 `validOperationTypes`

**验证方法**：

```javascript
// 测试脚本
const AuditLogService = require('./services/AuditLogService')
const result = await AuditLogService.logOperation({
  operator_id: 1,
  operation_type: 'asset_adjustment', // 新类型
  target_type: 'user_asset',
  target_id: 123,
  action: 'update',
  reason: '测试'
})
console.log('审计日志ID:', result.log_id)
```

**风险**：

- 需要回归测试所有审计日志记录点
- 需要确认 `operation_type` 词表完整性

---

### 方案3：审核系统收敛（P1）

**✅ 已确定方案（2026-01-09 决策）**

**目标**：建立统一审批流架构，废弃独立审核模块

**实施步骤**：

#### 3.1 启用统一审批流

**保留并启用**：`ContentReviewRecord` + `ContentAuditEngine`

- ✅ 作为**唯一审批流入口**
- ✅ 所有需要审批的业务统一使用
- ✅ 支持标准状态机：`pending → reviewing → approved/rejected`

**适用场景**：

- 消费审核（`ConsumptionService`）
- 商家积分审核（`MerchantReviewService`）
- 未来其他审批流（图片审核、反馈审核等）

#### 3.2 消费记录与审批流架构（✅ 已决策 2026-01-09）

**架构定位**：

- **`consumption_records`**：**业务主表（唯一事实源）**
  - 职责：记录消费/扣款/核销申请的业务事实
  - 内容：谁、对什么、金额/资产、发生时间、业务字段、幂等键
  - 用途：业务对账、统计报表、风控追责、资金闭环
- **`ContentReviewRecord`**：**审批流表（状态机）**
  - 职责：记录审批流程状态（`pending → reviewing → approved/rejected`）
  - 内容：审核人、审核时间、审核意见、审批节点
  - 用途：流程管理、权限控制、审批追溯

**1:1 强制关联规则**（✅ 已确定实现口径 2026-01-09）：

1. **创建规则**：
   - 每条需要审核的 `consumption_records` 必须对应一条 `ContentReviewRecord`
   - `ContentReviewRecord.auditable_type = 'consumption'`
   - `ContentReviewRecord.auditable_id = consumption_records.record_id`

2. **数据库硬约束**（✅ 已确定）：

   ```sql
   -- 在 content_review_records 表上添加唯一索引
   CREATE UNIQUE INDEX uk_content_review_auditable
   ON content_review_records(auditable_type, auditable_id);

   -- 防止同一个 consumption_records 被重复创建审批流
   ```

3. **状态管理**（✅ 已确定字段名）：
   - 审批状态以 `ContentReviewRecord.audit_status` 为准
   - `consumption_records` 只保留业务结果态：
     - `final_status`：业务最终状态（`pending_review/approved/rejected`）
     - `settled_at`：结算时间（审批通过/拒绝时落地）
   - 审批通过后才落地业务结果态

4. **审计记录**（✅ 已确定指向规则）：
   - `AdminOperationLog` 记录审批动作（不可变日志）
   - `target_type = 'ContentReviewRecord'`（固定指向审批流表）
   - `target_id = ContentReviewRecord.review_id`
   - `details` 字段记录关联的业务主表 ID（`consumption_record_id`）

**修复方案**：

```javascript
// services/ConsumptionService.js

// 1. 提交消费审核（同时创建业务记录和审批流）
async function submitConsumptionReview(consumptionData, options = {}) {
  const { transaction } = options

  // 1.1 创建消费记录（业务主表）
  const consumptionRecord = await ConsumptionRecord.create({
    user_id: consumptionData.user_id,
    amount: consumptionData.amount,
    asset_code: consumptionData.asset_code,
    description: consumptionData.description,
    final_status: 'pending_review', // 业务结果态：待审核
    idempotency_key: consumptionData.idempotency_key,
    transaction
  })

  // 1.2 创建审批流记录（强制 1:1 关联）
  const review = await ContentReviewRecord.create({
    auditable_type: 'consumption',
    auditable_id: consumptionRecord.record_id, // 关联业务主表
    audit_status: 'pending',
    submitted_at: new Date(),
    transaction
  })

  return { consumptionRecord, review }
}

// 2. 审核通过（更新审批流 + 落地业务结果态）
async function approveConsumption(reviewId, auditorId, options = {}) {
  const { transaction } = options

  // 2.1 获取审批流记录
  const review = await ContentReviewRecord.findByPk(reviewId, { transaction })
  if (!review || review.auditable_type !== 'consumption') {
    throw new Error('审批流记录不存在或类型不匹配')
  }

  // 2.2 更新审批流状态
  await review.update(
    {
      audit_status: 'approved',
      auditor_id: auditorId,
      audited_at: new Date()
    },
    { transaction }
  )

  // 2.3 执行业务逻辑（结算积分等）
  const consumptionRecord = await ConsumptionRecord.findByPk(review.auditable_id, { transaction })
  await settleConsumption(consumptionRecord, { transaction })

  // 2.4 更新业务主表结果态（✅ 字段名已确定）
  await consumptionRecord.update(
    {
      final_status: 'approved', // 业务最终状态
      settled_at: new Date() // 结算时间
    },
    { transaction }
  )

  // 2.5 写入审计日志（✅ 指向规则已确定：指向审批流表）
  await AuditLogService.logOperation({
    operator_id: auditorId,
    operation_type: 'consumption_audit',
    target_type: 'ContentReviewRecord', // 固定指向审批流表
    target_id: reviewId, // 审批流记录 ID
    action: 'approve',
    details: {
      consumption_record_id: consumptionRecord.record_id, // 业务主表 ID 记录在 details
      amount: consumptionRecord.amount
    },
    transaction
  })

  return { review, consumptionRecord }
}

// 3. 审核拒绝（类似流程）
async function rejectConsumption(reviewId, auditorId, reason, options = {}) {
  const { transaction } = options

  const review = await ContentReviewRecord.findByPk(reviewId, { transaction })

  // 更新审批流状态
  await review.update(
    {
      audit_status: 'rejected',
      auditor_id: auditorId,
      audited_at: new Date(),
      reject_reason: reason
    },
    { transaction }
  )

  // 更新业务主表结果态
  const consumptionRecord = await ConsumptionRecord.findByPk(review.auditable_id, { transaction })
  await consumptionRecord.update(
    {
      final_status: 'rejected',
      settled_at: new Date()
    },
    { transaction }
  )

  // 写入审计日志
  await AuditLogService.logOperation({
    operator_id: auditorId,
    operation_type: 'consumption_audit',
    target_type: 'ContentReviewRecord',
    target_id: reviewId,
    action: 'reject',
    details: { reason },
    transaction
  })

  return { review, consumptionRecord }
}
```

#### 3.3 迁移 MerchantReviewService

**废弃独立表**：`merchant_points_reviews`

**迁移到统一审批流**：

```javascript
// services/MerchantReviewService.js（改造）

// 1. 提交商家审核
async function submitMerchantReview(userId, amount, options = {}) {
  const { transaction } = options

  // 冻结积分
  await AssetService.freeze({
    user_id: userId,
    asset_code: 'POINTS',
    amount: amount,
    business_type: 'merchant_review_freeze',
    transaction
  })

  // 创建审批流记录（使用 ContentReviewRecord）
  const review = await ContentReviewRecord.create({
    auditable_type: 'merchant_points', // 区分业务类型
    auditable_id: userId,
    audit_status: 'pending',
    metadata: { frozen_amount: amount }, // 存储冻结金额
    submitted_at: new Date(),
    transaction
  })

  return review
}

// 2. 审核通过
async function approveMerchantReview(reviewId, auditorId, options = {}) {
  const { transaction } = options

  // 获取审批流记录
  const review = await ContentReviewRecord.findByPk(reviewId, { transaction })

  // 更新审批流状态
  await review.update(
    {
      audit_status: 'approved',
      auditor_id: auditorId,
      audited_at: new Date()
    },
    { transaction }
  )

  // 从冻结结算积分
  await AssetService.settleFromFrozen({
    user_id: review.auditable_id,
    asset_code: 'POINTS',
    amount: review.metadata.frozen_amount,
    business_type: 'merchant_review_settle',
    transaction
  })

  // 写入审计日志
  await AuditLogService.logOperation({
    operator_id: auditorId,
    operation_type: 'merchant_audit',
    target_type: 'ContentReviewRecord',
    target_id: reviewId,
    action: 'approve',
    transaction
  })
}
```

#### 3.4 废弃 MerchantPointsReview

**清理范围**：

- 删除模型：`models/MerchantPointsReview.js`
- 删除表：`DROP TABLE merchant_points_reviews`
- 修改服务：`services/MerchantReviewService.js`（业务逻辑保留，改用 `ContentReviewRecord`）
- 保留路由：`routes/v4/merchant/reviews.js`（路由保留，后端实现改用统一审批流）

**数据迁移**：无需迁移（表是空的）

#### 3.5 验收标准

**数据验收**：

- `content_review_records` 表产生非 0 数据
- 消费审核和商家审核都有对应记录
- `auditable_type` 正确区分业务类型（`consumption` / `merchant_points`）

**功能验收**：

```sql
-- 验收SQL：审批流数据统计
SELECT
  auditable_type,
  audit_status,
  COUNT(*) AS count
FROM content_review_records
WHERE created_at >= '2026-01-09'
GROUP BY auditable_type, audit_status;

-- 预期结果示例：
-- auditable_type     | audit_status | count
-- consumption        | pending      | 3
-- consumption        | approved     | 5
-- merchant_points    | pending      | 2
-- merchant_points    | approved     | 1
```

**对账验证**：

```sql
-- 审批流与审计日志对账
SELECT
  cr.auditable_type,
  COUNT(DISTINCT cr.review_id) AS review_count,
  COUNT(DISTINCT aol.log_id) AS audit_log_count
FROM content_review_records cr
LEFT JOIN admin_operation_logs aol
  ON aol.target_type = 'ContentReviewRecord'
  AND aol.target_id = cr.review_id
WHERE cr.created_at >= '2026-01-09'
GROUP BY cr.auditable_type;

-- 预期结果：review_count = audit_log_count（每个审批都有审计日志）
```

---

### 方案4：角色审计收敛（P1）

**✅ 已确定方案（2026-01-09 决策）**

**目标**：建立"业务事件ID + 审计日志"标准组合架构

**实施步骤**：

1. **废弃 role_change_logs 表**：

   ```sql
   -- 1. 确认表是空的
   SELECT COUNT(*) FROM role_change_logs;  -- 应为 0

   -- 2. 删除外键约束
   ALTER TABLE role_change_logs DROP FOREIGN KEY role_change_logs_ibfk_1;
   ALTER TABLE role_change_logs DROP FOREIGN KEY role_change_logs_ibfk_2;
   ALTER TABLE role_change_logs DROP FOREIGN KEY role_change_logs_ibfk_3;
   ALTER TABLE role_change_logs DROP FOREIGN KEY role_change_logs_ibfk_4;

   -- 3. 删除表
   DROP TABLE role_change_logs;
   ```

2. **删除 RoleChangeLog 模型**：
   - 删除文件：`models/RoleChangeLog.js`
   - 从 `models/index.js` 移除引用

3. **强化 UserRoleChangeRecord 使用**：
   - **强制要求**：所有角色变更入口必须先创建 `UserRoleChangeRecord`
   - 修改 `HierarchyManagementService`：
     - 移除 `RoleChangeLog.create()` 调用
     - 确保每次角色变更都先创建 `UserRoleChangeRecord`
     - 然后调用 `AuditLogService.logOperation({ operation_type: 'role_change', target_id: record_id, ... })`
4. **建立关联关系**：

   ```javascript
   // 标准角色变更流程
   async function changeUserRole(userId, newRole, options = {}) {
     const { transaction, operator_id, reason } = options

     // 1. 创建业务事件记录（幂等主键）
     const changeRecord = await UserRoleChangeRecord.create(
       {
         user_id: userId,
         old_role: currentRole,
         new_role: newRole,
         changed_by: operator_id,
         change_reason: reason,
         idempotency_key: generateIdempotencyKey()
       },
       { transaction }
     )

     // 2. 执行角色变更
     await UserRole.update(
       { role_name: newRole },
       {
         where: { user_id: userId },
         transaction
       }
     )

     // 3. 写入审计日志（关联业务事件ID）
     await AuditLogService.logOperation({
       operator_id: operator_id,
       operation_type: 'role_change',
       target_type: 'UserRoleChangeRecord',
       target_id: changeRecord.record_id, // 指向业务事件
       action: 'update',
       changes: { old_role: currentRole, new_role: newRole },
       reason: reason,
       transaction
     })

     return changeRecord
   }
   ```

5. **保留 PermissionAuditLogger（文件日志）**：
   - 用于记录高频权限检查（不进 DB）
   - 用于性能监控和异常检测

**验证方法**：

```javascript
// 测试角色变更审计
const UserRoleService = require('./services/UserRoleService')
const result = await UserRoleService.updateUserRole(123, 'admin', {
  transaction,
  operator_id: 1,
  reason: '测试角色变更'
})

// 验证业务事件记录已创建
const { UserRoleChangeRecord } = require('./models')
const changeRecord = await UserRoleChangeRecord.findByPk(result.record_id)
console.log('业务事件记录:', changeRecord)

// 验证审计日志已写入且关联正确
const { AdminOperationLog } = require('./models')
const auditLog = await AdminOperationLog.findOne({
  where: {
    operation_type: 'role_change',
    target_id: result.record_id
  }
})
console.log('审计日志:', auditLog)
console.log('关联验证:', auditLog.target_id === changeRecord.record_id)
```

**数据一致性修复**：

```javascript
// 可选：补齐历史数据（将现有 99 条 admin_operation_logs 补齐对应的 user_role_change_records）
const { AdminOperationLog, UserRoleChangeRecord } = require('./models')

const orphanLogs = await AdminOperationLog.findAll({
  where: {
    operation_type: 'role_change',
    target_type: { [Op.or]: [null, { [Op.ne]: 'UserRoleChangeRecord' }] }
  }
})

for (const log of orphanLogs) {
  const changeRecord = await UserRoleChangeRecord.create({
    user_id: log.target_id, // 假设 target_id 原本是 user_id
    old_role: log.changes?.old_role || 'unknown',
    new_role: log.changes?.new_role || 'unknown',
    changed_by: log.operator_id,
    change_reason: log.reason || '历史数据补录',
    created_at: log.created_at,
    is_legacy: true // 标记为历史补录数据
  })

  // 更新审计日志关联
  await log.update({
    target_type: 'UserRoleChangeRecord',
    target_id: changeRecord.record_id
  })
}
```

**风险**：

- 需要回归测试所有角色变更场景
- 需要确认 `HierarchyManagementService` 和 `UserRoleService` 的所有调用点
- 历史数据补录可选（如果需要完整的业务事件链）

---

### 方案5：交易流水统一真相模型（P2）

**目标**：确定 `AssetTransaction` 为资产变动唯一事实来源

**实施步骤**：

1. **确定事实模型**：
   - **资产变动事实**：`AssetTransaction`（已是主干，846 行）
   - **订单事实**：
     - C2C 市场：`TradeOrder`（代码已就绪，待启用）
     - B2C 兑换：`ExchangeRecord`（代码已就绪，待启用）
     - 核销系统：`RedemptionOrder`（已活跃，188 行）
   - **物品事实**：`ItemInstance` + `ItemInstanceEvent`（431/236 行）

2. **TradeRecord 退为读模型**：
   - 保留表和模型（保存 2 条历史数据）
   - 添加 `is_legacy` 字段标记历史数据
   - 禁止新写入（移除 `FeeCalculator.createTradeRecord()` 调用）
   - `BackpackService.getTransferHistory()` 改为查询 `ItemInstanceEvent`

3. **修复 FeeCalculator 集成**：
   - `TradeOrderService` 中已计算手续费（通过 `FeeCalculator.calculateItemFee()`）
   - 但未写入 `TradeRecord`（正确行为，因为已写 `TradeOrder` + `AssetTransaction`）
   - 删除 `FeeCalculator.createTradeRecord()` 方法（未被调用）

4. **调查 exchange_debit 与 exchange_records 不一致**：

   ```javascript
   // 检查脚本
   const { sequelize } = require('./config/database')

   const [exchangeDebits] = await sequelize.query(`
     SELECT COUNT(*) AS cnt
     FROM asset_transactions
     WHERE business_type = 'exchange_debit'
   `)

   const [exchangeRecords] = await sequelize.query(`
     SELECT COUNT(*) AS cnt
     FROM exchange_records
   `)

   console.log('exchange_debit 流水:', exchangeDebits[0].cnt) // 378
   console.log('exchange_records 订单:', exchangeRecords[0].cnt) // 0
   // 若不一致，说明订单创建逻辑有 bug
   ```

**验证方法**：

```javascript
// 验证新交易不再写 TradeRecord
const { TradeRecord } = require('./models')
const countBefore = await TradeRecord.count()

// 执行一笔市场交易
// ...

const countAfter = await TradeRecord.count()
assert(countBefore === countAfter, 'TradeRecord 不应再写入')
```

**风险**：

- `BackpackService.getTransferHistory()` 需要改造（查询 `ItemInstanceEvent` 替代 `TradeRecord`）
- 需要确认 2 条历史 `TradeRecord` 数据是否需要迁移到新模型

---

### 方案6：明确兑换系统命名（P2）

**目标**：消除 `ExchangeService` 与 `RedemptionService` 的语义混淆

**实施步骤**：

1. **重命名服务和路由**（可选，影响较大）：
   - `ExchangeService` → `MaterialShopService`
   - `/api/v4/shop/exchange/*` → `/api/v4/shop/material-shop/*`
   - 或保持路径不变，但在文档中明确说明

2. **更新代码注释**（最小改动）：

   ```javascript
   // routes/v4/shop/exchange/index.js
   /**
    * B2C材料兑换模块（Material Shop）
    *
    * 业务场景：用户使用材料资产兑换实物商品（官方商城）
    * 与核销系统的区别：
    * - ExchangeService：材料 → 商品（B2C，官方商城）
    * - RedemptionService：物品 → 核销码 → 提货（核销流程）
    */
   ```

3. **调查数据不一致**：
   - 为何有 378 条 `exchange_debit` 但 0 条 `exchange_records`？
   - 检查 `ExchangeService.exchangeItem()` 的订单创建逻辑
   - 若是测试数据，清理 `asset_transactions` 中的测试记录

**验证方法**：

```javascript
// 测试兑换流程完整性
const ExchangeService = require('./services/ExchangeService')
const { sequelize } = require('./config/database')

await sequelize.transaction(async transaction => {
  const result = await ExchangeService.exchangeItem(user_id, item_id, quantity, {
    transaction,
    idempotency_key: 'test_exchange_123'
  })

  // 验证订单已创建
  const { ExchangeRecord } = require('./models')
  const order = await ExchangeRecord.findOne({
    where: { idempotency_key: 'test_exchange_123' },
    transaction
  })
  assert(order !== null, 'ExchangeRecord 应该被创建')

  // 验证流水已写入
  const { AssetTransaction } = require('./models')
  const txn = await AssetTransaction.findOne({
    where: {
      business_type: 'exchange_debit',
      idempotency_key: { [Op.like]: '%test_exchange_123%' }
    },
    transaction
  })
  assert(txn !== null, 'AssetTransaction 应该被创建')
})
```

**风险**：

- 若重命名服务，影响范围较大（需要更新所有调用方）
- 建议先修复数据不一致，再考虑重命名

---

### 方案7：清理遗留空表（P2）

**目标**：删除从未使用的数据库表

**实施步骤**：

#### 7.1 立即删除（无代码引用）

**audit_records**：

```sql
-- 1. 确认无数据
SELECT COUNT(*) FROM audit_records;  -- 应为 0

-- 2. 确认无外键依赖
SELECT
  TABLE_NAME, CONSTRAINT_NAME, REFERENCED_TABLE_NAME
FROM information_schema.KEY_COLUMN_USAGE
WHERE REFERENCED_TABLE_NAME = 'audit_records';

-- 3. 删除表
DROP TABLE audit_records;
```

**验证**：

```bash
# 确认代码中无引用
grep -r "audit_records\|AuditRecord" services/ routes/ models/ middleware/
# 应无结果（或仅在迁移文件/备份文件中）
```

#### 7.2 需要决策后删除

**role_change_logs**（若决定废弃）：

```sql
-- 1. 删除外键
ALTER TABLE role_change_logs DROP FOREIGN KEY role_change_logs_ibfk_1;
ALTER TABLE role_change_logs DROP FOREIGN KEY role_change_logs_ibfk_2;
ALTER TABLE role_change_logs DROP FOREIGN KEY role_change_logs_ibfk_3;
ALTER TABLE role_change_logs DROP FOREIGN KEY role_change_logs_ibfk_4;

-- 2. 删除表
DROP TABLE role_change_logs;
```

**代码清理**：

- 删除 `models/RoleChangeLog.js`
- 从 `models/index.js` 移除引用
- 从 `HierarchyManagementService` 移除 `RoleChangeLog.create()` 调用

**content_review_records**（若决定废弃）：

```sql
DROP TABLE content_review_records;
```

**代码清理**：

- 删除 `models/ContentReviewRecord.js`
- 删除 `services/ContentAuditEngine.js`
- 从 `ConsumptionService` 移除 `ContentReviewRecord.create()` 调用

**merchant_points_reviews**（若决定废弃）：

```sql
DROP TABLE merchant_points_reviews;
```

**代码清理**：

- 删除 `models/MerchantPointsReview.js`
- 删除 `services/MerchantReviewService.js`
- 删除 `routes/v4/merchant/reviews.js`
- 从 `routes/v4/merchant/index.js` 移除挂载

**风险**：

- 若未来需要这些功能，需要重新实现
- 建议先标记为 `@deprecated`，观察 3 个月后再删除

---

## 📊 数据库表使用情况矩阵

### 活跃表（有数据且代码在用）

| 表名                       | 行数 | 最新时间   | 主要服务             | 主要路由                    | 业务域 |
| -------------------------- | ---- | ---------- | -------------------- | --------------------------- | ------ |
| `admin_operation_logs`     | 1046 | 2026-01-07 | `AuditLogService`    | `/api/v4/console/*`         | 审计   |
| `asset_transactions`       | 846  | 2026-01-07 | `AssetService`       | `/api/v4/assets/*`          | 资产   |
| `item_instances`           | 431  | -          | `ItemInstance`       | `/api/v4/backpack/*`        | 物品   |
| `api_idempotency_requests` | 306  | -          | `IdempotencyService` | 所有 API                    | 幂等   |
| `item_instance_events`     | 236  | -          | `ItemInstance`       | -                           | 物品   |
| `redemption_orders`        | 188  | -          | `RedemptionService`  | `/api/v4/shop/redemption/*` | 核销   |
| `exchange_items`           | 24   | -          | `ExchangeService`    | `/api/v4/shop/exchange/*`   | 兑换   |
| `users`                    | 23   | -          | `UserService`        | `/api/v4/user/*`            | 用户   |

### 空表（有代码但无数据）

| 表名                      | 行数 | 模型存在 | 服务存在 | 路由存在 | 建议                                       |
| ------------------------- | ---- | -------- | -------- | -------- | ------------------------------------------ |
| `exchange_records`        | 0    | ✅       | ✅       | ✅       | ✅ **近期上线**（已决策 2026-01-09）       |
| `trade_orders`            | 0    | ✅       | ✅       | ✅       | ✅ **近期上线**（已决策 2026-01-09）       |
| `content_review_records`  | 0    | ✅       | ✅       | ❌       | ✅ **启用统一审批流**（已决策 2026-01-09） |
| `merchant_points_reviews` | 0    | ✅       | ✅       | ✅       | ❌ **废弃**（已决策 2026-01-09）           |
| `role_change_logs`        | 0    | ✅       | ❌       | ❌       | ❌ **废弃**（已决策 2026-01-09）           |
| `audit_records`           | 0    | ❌       | ❌       | ❌       | **废弃**（立即删除）                       |
| `consumption_records`     | 0    | ✅       | ✅       | ✅       | **待启用** 或 废弃                         |

### 残留表（有少量历史数据）

| 表名            | 行数 | 最新时间   | 建议                           |
| --------------- | ---- | ---------- | ------------------------------ |
| `trade_records` | 2    | 2025-12-12 | 标记为 `is_legacy`，退为读模型 |

---

## 🎯 实施优先级与时间表

### 第一阶段（P0，立即执行，1-2天）

**目标**：修复影响功能可用性的问题

- [ ] 修复 `POST /api/v4/auth/refresh` 路由冲突
  - 拆分权限路由到 `/api/v4/permissions/*`
  - 更新前端调用路径
  - 回归测试 Token 刷新和权限缓存刷新

**验收标准**：

- ✅ `POST /api/v4/auth/refresh` 返回新 Token
- ✅ `POST /api/v4/permissions/refresh` 清除权限缓存
- ✅ 两个接口互不干扰

---

### 第二阶段（P1，1周内，3-5天）

**目标**：统一审计审核架构

- [ ] 统一审计入口到 `AuditLogService`
  - 改造 `middleware/auditLog.js` 为薄封装
  - 统一 `operation_type` 词表
  - 修复 `routes/v4/console/asset-adjustment.js` 错误调用
  - 回归测试所有审计日志记录点

- [ ] ✅ 决策审核系统架构（已决策 2026-01-09）
  - 启用 `ContentReviewRecord` + `ContentAuditEngine` 作为统一审批流
  - 消费审核和商家审核都纳入统一审批流
  - 废弃 `MerchantPointsReview` 独立表
  - 修复 `ConsumptionService` 审批流逻辑
  - 迁移 `MerchantReviewService` 到统一审批流

- [ ] ✅ 统一角色审计（已决策 2026-01-09）
  - 废弃 `role_change_logs` 表和模型
  - 强制使用 `UserRoleChangeRecord` 作为业务事件主键
  - 所有角色变更必须先创建 `UserRoleChangeRecord`，再写 `AdminOperationLog`
  - `AdminOperationLog.target_id` 指向 `UserRoleChangeRecord.record_id`
  - 保留 `PermissionAuditLogger` 用于高频检查

**验收标准**：

- ✅ 所有审计日志统一从 `AuditLogService.logOperation()` 写入
- ✅ `operation_type` 词表统一且完整
- ✅ 角色变更审计采用"业务事件ID + 审计日志"标准组合
  - `UserRoleChangeRecord` 作为业务事件主键（强制创建）
  - `AdminOperationLog` 作为审计真相源（关联 `target_id`）
  - `role_change_logs` 已废弃删除
- ✅ 审核系统架构明确（已决策 2026-01-09）
  - `ContentReviewRecord` + `ContentAuditEngine` 作为统一审批流
  - 消费审核和商家审核都纳入统一审批流
  - `merchant_points_reviews` 已废弃删除
  - `content_review_records` 表产生非 0 数据

---

### 第三阶段（P2，2-3周，长期优化）

**目标**：清理技术债务，优化数据模型

- [ ] ✅ 交易流水统一真相模型（已决策 2026-01-09）
  - 确定 `AssetTransaction` 为资产变动唯一事实来源
  - `TradeRecord` 退为读模型（标记 `is_legacy`）
  - 修复 `BackpackService.getTransferHistory()` 查询逻辑
  - C2C 市场（`trade_orders`）近期上线，验收口径已明确
  - B2C 兑换（`exchange_records`）近期上线，验收口径已明确

- [ ] ✅ 明确兑换系统命名（已决策 2026-01-09）
  - 兑换系统要产生真实订单（`exchange_records` 必须有数据）
  - 修复订单创建逻辑（确保扣款与订单 1:1 对应）
  - 测试数据标记隔离（`is_test_data = 1`）或迁移到测试库
  - 建立订单与流水对账机制

- [ ] 清理遗留空表
  - 删除 `audit_records`（立即）
  - 删除 `role_change_logs`（若决定废弃）
  - 删除 `content_review_records`（若决定废弃）
  - 删除 `merchant_points_reviews`（若决定废弃）

**验收标准**：

- ✅ 数据库中无未使用的表
- ✅ 交易流水查询逻辑统一
- ✅ 兑换系统订单创建逻辑修复
- ✅ C2C 市场和 B2C 兑换完成上线验收（至少产生非 0 测试数据）
- ✅ 测试数据与生产数据严格隔离（`is_test_data` 标记或迁移到测试库）

---

## 🛡️ 防止重复的治理规则

### 规则1：路由唯一性强制检查

**实施方法**：

- 升级 `scripts/check-route-duplicates.js` 支持递归解析 `router.use()`
- 在 CI 中强制执行：`npm run check-routes`
- 若发现重复路由，构建失败

**检查脚本增强**：

```javascript
// scripts/check-route-duplicates.js（增强版）
// 1. 递归解析所有 router.use() 挂载
// 2. 构建完整的 method + path 映射
// 3. 检测冲突（同一 method + path 被多次注册）
// 4. 检测遮蔽（同一挂载点下有多个子路由定义相同路径）
```

### 规则2：审计入口单一真相源

**强制规范**：

- ✅ 所有审计日志**必须**通过 `AuditLogService.logOperation()` 写入
- ❌ 禁止直接 `AdminOperationLog.create()`
- ❌ 禁止在多处定义 `operation_type` 词表

**代码审查检查点**：

```bash
# 检查是否有直接写 AdminOperationLog 的代码
grep -r "AdminOperationLog.create" services/ routes/ middleware/
# 应仅在 AuditLogService.js 中出现
```

### 规则3：数据模型变更评审

**强制流程**：

- 新增表前，检查是否已有类似功能的表
- 新增服务前，检查是否已有类似职责的服务
- 所有数据模型变更需要架构师评审

**评审检查清单**：

- [ ] 是否与现有表功能重复？
- [ ] 是否可以复用现有服务？
- [ ] 是否需要新增 `operation_type`？
- [ ] 是否需要新增路由域？

### 规则4：空表定期清理

**定期检查**（每季度）：

```sql
-- 查找空表
SELECT
  t.TABLE_NAME,
  t.TABLE_ROWS,
  t.CREATE_TIME,
  t.UPDATE_TIME
FROM information_schema.TABLES t
WHERE t.TABLE_SCHEMA = DATABASE()
  AND t.TABLE_TYPE = 'BASE TABLE'
  AND t.TABLE_ROWS = 0
ORDER BY t.CREATE_TIME DESC;
```

**清理流程**：

1. 确认表是否有代码引用（`grep -r "table_name"`）
2. 若无引用，直接删除
3. 若有引用但未使用，标记为 `@deprecated`，3 个月后删除
4. 若有引用且计划启用，添加到"待启用功能清单"

---

## 📈 预期收益

### 代码质量提升

- ✅ 减少 **4 个重复模块**（`middleware/auditLog` 薄封装、`ContentAuditEngine` 废弃、`RoleChangeLog` 废弃、`TradeRecord` 退为读模型）
- ✅ 统一 **3 个入口**（审计、审核、交易流水）
- ✅ 删除 **2-4 张空表**（`audit_records`、`role_change_logs`、可选 `content_review_records`、`merchant_points_reviews`）

### 维护成本降低

- ✅ 审计日志调用方式统一，减少学习成本
- ✅ 数据库表数量减少，减少维护成本
- ✅ 代码注释和实际路径一致，减少理解成本

### 功能可用性提升

- ✅ 修复权限缓存刷新接口不可达问题
- ✅ 统一审计词表，提升查询和统计准确性
- ✅ 明确兑换系统命名，减少业务混淆

---

## 🔍 附录：完整路由清单

### 认证授权域（/api/v4/auth）

- `POST /api/v4/auth/login` - 手机号密码登录
- `POST /api/v4/auth/quick-login` - 微信快捷登录
- `POST /api/v4/auth/decrypt-phone` - 解密手机号
- `GET /api/v4/auth/verify` - 验证 Token
- `POST /api/v4/auth/refresh` - 刷新 Token ⚠️ **冲突**
- `POST /api/v4/auth/logout` - 退出登录
- `GET /api/v4/auth/profile` - 获取用户信息
- `GET /api/v4/auth/permissions/me` - 获取我的权限
- `POST /api/v4/auth/permissions/check` - 检查权限
- `POST /api/v4/auth/permissions/refresh` - 刷新权限缓存 ⚠️ **被遮蔽**
- `POST /api/v4/auth/permissions/batch-check` - 批量检查权限
- `GET /api/v4/auth/permissions/admins` - 获取管理员列表
- `GET /api/v4/auth/permissions/statistics` - 权限统计

### 资产域（/api/v4/assets）

- `GET /api/v4/assets/balance` - 查询单个资产余额
- `GET /api/v4/assets/balances` - 查询所有资产余额
- `GET /api/v4/assets/transactions` - 查询资产流水

### 背包域（/api/v4/backpack）

- `GET /api/v4/backpack` - 查询用户背包（资产+物品）
- `GET /api/v4/backpack/assets` - 查询可叠加资产
- `GET /api/v4/backpack/items` - 查询非叠加物品
- `GET /api/v4/backpack/items/:item_instance_id` - 查询物品详情
- `GET /api/v4/backpack/items/:item_instance_id/history` - 查询物品历史

### 商城域（/api/v4/shop）

- **兑换子域**（`/shop/exchange`）：
  - `GET /api/v4/shop/exchange/items` - 商品列表
  - `GET /api/v4/shop/exchange/items/:item_id` - 商品详情
  - `POST /api/v4/shop/exchange/exchange` - 兑换商品
  - `GET /api/v4/shop/exchange/orders` - 订单列表
  - `GET /api/v4/shop/exchange/orders/:order_no` - 订单详情
  - `POST /api/v4/shop/exchange/orders/:order_no/status` - 更新订单状态
  - `GET /api/v4/shop/exchange/statistics` - 统计数据

- **核销子域**（`/shop/redemption`）：
  - `POST /api/v4/shop/redemption/orders` - 生成核销订单
  - `POST /api/v4/shop/redemption/fulfill` - 核销订单
  - `GET /api/v4/shop/redemption/orders/:order_id` - 查询订单详情
  - `POST /api/v4/shop/redemption/orders/:order_id/cancel` - 取消订单
  - `GET /api/v4/shop/redemption/items/:item_instance_id/order` - 查询物品的核销订单

- **资产转换子域**（`/shop/assets`）：
  - `POST /api/v4/shop/assets/convert` - 材料转换（碎红水晶→钻石）
  - `GET /api/v4/shop/assets/conversion-rules` - 转换规则查询

### 市场域（/api/v4/market）

- `GET /api/v4/market/listings` - 挂牌列表
- `POST /api/v4/market/listings` - 创建挂牌
- `GET /api/v4/market/listings/:listing_id` - 挂牌详情
- `DELETE /api/v4/market/listings/:listing_id` - 取消挂牌
- `POST /api/v4/market/buy` - 购买挂牌

### 抽奖域（/api/v4/lottery）

- `GET /api/v4/lottery/prizes` - 奖品列表
- `POST /api/v4/lottery/draw` - 执行抽奖
- `GET /api/v4/lottery/history` - 抽奖历史
- `GET /api/v4/lottery/points` - 用户积分
- `GET /api/v4/lottery/presets` - 抽奖预设

### 管理后台（/api/v4/console）

- **系统管理**（`/console/system`）
- **用户管理**（`/console/users`）
- **抽奖管理**（`/console/lottery`）
- **资产调整**（`/console/asset-adjustment`）
- **材料管理**（`/console/material`）
- **客服管理**（`/console/customer-service`）
- **数据分析**（`/console/analytics`）

---

## 🔄 数据迁移策略

### 无需迁移（空表直接删除）

- `audit_records`
- `role_change_logs`（若决定废弃）
- `content_review_records`（若决定废弃）
- `merchant_points_reviews`（若决定废弃）

### 需要标记（历史数据保留）

- `trade_records`（2 行）：
  ```sql
  ALTER TABLE trade_records ADD COLUMN is_legacy TINYINT(1) DEFAULT 0;
  UPDATE trade_records SET is_legacy = 1;
  ```

### 需要对账（数据不一致）

- `asset_transactions` 的 `exchange_debit`（378 条）vs `exchange_records`（0 条）：
  - 若是测试数据，清理 `asset_transactions`
  - 若是 bug，修复 `ExchangeService` 订单创建逻辑

---

## 📝 总结与建议

### 核心发现

1. ✅ **路由冲突**：`POST /api/v4/auth/refresh` 被注册两次，权限缓存刷新接口不可达
2. ✅ **审计入口重复**：`AuditLogService` 与 `middleware/auditLog.js` 同时写同一张表
3. ✅ **审核系统重叠**：三套审核机制并存，但只有一套在跑
4. ✅ **角色审计重复**：三套角色审计并行，但只有一套在用
5. ✅ **交易流水双轨**：`AssetTransaction` 已是主干，`TradeRecord` 残留
6. ✅ **遗留空表**：6 张空表占用资源

### 优先级建议

- **P0（立即）**：修复路由冲突（1-2 天）
- **P1（1周内）**：统一审计审核架构（3-5 天）
- **P2（2-3周）**：清理技术债务（长期优化）

### 关键决策点

✅ **已完成决策（2026-01-09）**：

1. ✅ **审核系统架构**：启用 `ContentReviewRecord` + `ContentAuditEngine` 作为统一审批流，消费审核和商家审核都纳入
2. ✅ **商户积分审核**：废弃 `MerchantPointsReview` 独立表，迁移到统一审批流
3. ✅ **TradeRecord 定位**：退为读模型（标记 `is_legacy`），不再写入新数据
4. ✅ **角色变更记录架构**：采用 `AdminOperationLog`（审计真相源）+ `UserRoleChangeRecord`（业务事件主键）组合，废弃 `role_change_logs`
5. ✅ **C2C 市场交易**：近期上线，验收口径已明确（至少 10 笔测试交易）
6. ✅ **兑换系统订单**：要产生真实订单，修复订单创建逻辑，测试数据严格隔离

✅ **已完成所有决策（2026-01-09）**

7. ✅ **consumption_records 架构**：保留作为业务主表，与 `ContentReviewRecord` 1:1 强制关联，审批通过才落业务结果态
8. ✅ **空表清理策略**：观察期 90 天（详见下方实施计划）

### 空表清理策略（✅ 已决策 2026-01-09）

**策略定义**：观察期 **90 天**

**分类处理**：

#### A. 立即清理（已决策废弃）

以下表/模型/服务立即进入清理流程：

| 表名                      | 模型文件                         | 服务文件                            | 清理原因                                                |
| ------------------------- | -------------------------------- | ----------------------------------- | ------------------------------------------------------- |
| `merchant_points_reviews` | `models/MerchantPointsReview.js` | `services/MerchantReviewService.js` | 已决策迁移到统一审批流                                  |
| `role_change_logs`        | `models/RoleChangeLog.js`        | -                                   | 已决策改用 `UserRoleChangeRecord` + `AdminOperationLog` |

**清理步骤**：

```sql
-- 1. 确认表是空的
SELECT COUNT(*) FROM merchant_points_reviews;  -- 应为 0
SELECT COUNT(*) FROM role_change_logs;         -- 应为 0

-- 2. 删除外键约束（如有）
-- ALTER TABLE merchant_points_reviews DROP FOREIGN KEY ...;
-- ALTER TABLE role_change_logs DROP FOREIGN KEY ...;

-- 3. 删除表
DROP TABLE IF EXISTS merchant_points_reviews;
DROP TABLE IF EXISTS role_change_logs;
```

```bash
# 4. 删除模型文件
rm -f models/MerchantPointsReview.js
rm -f models/RoleChangeLog.js

# 5. 删除服务文件（业务逻辑已迁移）
rm -f services/MerchantReviewService.js

# 6. 从 models/index.js 移除引用
# 手动编辑 models/index.js，删除相关 require 语句
```

#### B. 观察期 90 天（近期上线）

以下表设定观察期截止日期：**2026-04-09**（90天后）

**✅ 达标阈值已锁定（2026-01-09 决策）**：

| 表名                     | 当前状态 | 上线验收要求（最低阈值）           | 到期未达标则废弃 |
| ------------------------ | -------- | ---------------------------------- | ---------------- |
| `content_review_records` | 0 行     | **≥ 10 条**审批记录（消费+商家）   | ✅               |
| `exchange_records`       | 0 行     | **≥ 5 笔**兑换订单                 | ✅               |
| `trade_orders`           | 0 行     | **≥ 10 笔** C2C 交易订单           | ✅               |
| `consumption_records`    | 0 行     | **≥ 5 条**消费记录（与审批流关联） | ✅               |

**观察期检查点**：

1. **30 天检查**（2026-02-08）：

   ```sql
   -- 检查是否有数据产生
   SELECT
     'content_review_records' AS table_name,
     COUNT(*) AS count,
     MAX(created_at) AS latest
   FROM content_review_records
   UNION ALL
   SELECT 'exchange_records', COUNT(*), MAX(created_at) FROM exchange_records
   UNION ALL
   SELECT 'trade_orders', COUNT(*), MAX(created_at) FROM trade_orders
   UNION ALL
   SELECT 'consumption_records', COUNT(*), MAX(created_at) FROM consumption_records;
   ```

2. **60 天检查**（2026-03-10）：
   - 评估上线进度
   - 若仍为 0 行，发出预警通知

3. **90 天到期**（2026-04-09）：
   - 若未达标：进入废弃流程（删除表/模型/服务）
   - 若已达标：转为正式业务表，移除观察期标记

**观察期管理脚本**：

```javascript
// scripts/check-observation-tables.js
const OBSERVATION_TABLES = [
  { name: 'content_review_records', minCount: 10, deadline: '2026-04-09' },
  { name: 'exchange_records', minCount: 5, deadline: '2026-04-09' },
  { name: 'trade_orders', minCount: 10, deadline: '2026-04-09' },
  { name: 'consumption_records', minCount: 5, deadline: '2026-04-09' }
]

async function checkObservationTables() {
  const results = []
  const now = new Date()

  for (const table of OBSERVATION_TABLES) {
    const [rows] = await sequelize.query(`
      SELECT COUNT(*) AS count, MAX(created_at) AS latest 
      FROM ${table.name}
    `)

    const count = rows[0].count
    const deadline = new Date(table.deadline)
    const daysRemaining = Math.ceil((deadline - now) / (1000 * 60 * 60 * 24))

    const status =
      count >= table.minCount
        ? 'PASS'
        : daysRemaining <= 0
          ? 'EXPIRED'
          : daysRemaining <= 30
            ? 'WARNING'
            : 'OBSERVING'

    results.push({
      table: table.name,
      count: count,
      required: table.minCount,
      latest: rows[0].latest,
      daysRemaining: daysRemaining,
      status: status
    })
  }

  console.table(results)

  // 发出告警
  const warnings = results.filter(r => r.status === 'WARNING' || r.status === 'EXPIRED')
  if (warnings.length > 0) {
    console.warn(`\n⚠️ ${warnings.length} 个表即将到期或已过期，需要决策！`)
  }

  return results
}

// 定时任务：每周一执行
// cron: 0 9 * * 1
```

#### C. 保留（活跃使用中）

以下表已有数据且活跃使用，无需清理：

- `admin_operation_logs`（1046 行）
- `asset_transactions`（846 行）
- `redemption_orders`（188 行）
- `item_instances`（431 行）
- `user_role_change_records`（4 行，强制使用）
- 其他所有非 0 行的表

### 技术规范总结（✅ 所有实现口径已锁定 2026-01-09）

#### 1. 数据库约束规范

```sql
-- 1.1 审批流 1:1 唯一约束
CREATE UNIQUE INDEX uk_content_review_auditable
ON content_review_records(auditable_type, auditable_id);

-- 1.2 消费记录表结构（确保字段存在）
ALTER TABLE consumption_records
ADD COLUMN final_status VARCHAR(50) DEFAULT 'pending_review' COMMENT '业务最终状态',
ADD COLUMN settled_at DATETIME NULL COMMENT '结算时间（审批完成时落地）';

-- 1.3 测试数据隔离标记
ALTER TABLE asset_transactions
ADD COLUMN is_test_data TINYINT(1) DEFAULT 0 COMMENT '测试数据标记';

ALTER TABLE trade_records
ADD COLUMN is_legacy TINYINT(1) DEFAULT 0 COMMENT '历史遗留数据标记';
```

#### 2. 审计日志指向规范

**固定规则**：

- 审批类操作：`target_type = 'ContentReviewRecord'`，`target_id = review_id`
- 角色变更操作：`target_type = 'UserRoleChangeRecord'`，`target_id = record_id`
- 其他操作：根据业务主表决定

**关联业务主表**：

- 通过 `details` 字段记录业务主表 ID（如 `consumption_record_id`）
- 便于审计日志与业务数据双向追溯

#### 3. 观察期阈值规范（锁定）

| 表名                     | 最低阈值 | 检查日期   | 到期日期   |
| ------------------------ | -------- | ---------- | ---------- |
| `content_review_records` | ≥ 10 条  | 每周一检查 | 2026-04-09 |
| `exchange_records`       | ≥ 5 笔   | 每周一检查 | 2026-04-09 |
| `trade_orders`           | ≥ 10 笔  | 每周一检查 | 2026-04-09 |
| `consumption_records`    | ≥ 5 条   | 每周一检查 | 2026-04-09 |

**自动检查脚本**：`scripts/check-observation-tables.js`（每周一 09:00 执行）

#### 4. 字段命名规范（锁定）

- **审批状态字段**：`audit_status`（在 `ContentReviewRecord` 表）
- **业务结果态字段**：`final_status`（在业务主表，如 `consumption_records`）
- **结算时间字段**：`settled_at`（在业务主表）
- **测试数据标记**：`is_test_data`（在流水表）
- **历史数据标记**：`is_legacy`（在遗留表）

### 实施建议

- ✅ 优先修复 P0 问题（路由冲突）- **已完成**
- ✅ P1 问题需要业务方确认需求后再执行 - **已完成决策**
- ✅ P2 问题可以逐步优化，不影响现有功能 - **已明确方案**
- ✅ 立即清理已决策废弃的表（`merchant_points_reviews`、`role_change_logs`）
- ✅ 启动 90 天观察期，定期检查近期上线表的数据产生情况
- ✅ 执行数据库约束迁移（唯一索引、字段添加、标记字段）

---

**报告生成时间**：2026年01月08日  
**数据库连接**：`restaurant_points_dev`（真实库）  
**代码版本**：当前工作区最新代码  
**检查方法**：代码扫描 + 真实库数据查询 + 业务逻辑分析
