# 交易市场多币种扩展功能 - 实现状态与待办清单

**文档版本**: V1.0  
**创建时间**: 2026-01-14  
**关联文档**: `材料转换系统降维护成本方案-2026-01-13.md`  
**适用系统**: 餐厅积分抽奖系统 V4.5.0 交易市场模块

---

## 📊 当前状态总览

| 模块         | 状态      | 说明                                                                 |
| ------------ | --------- | -------------------------------------------------------------------- |
| 数据库表结构 | ✅ 已就绪 | `market_listings.price_asset_code`、`trade_orders.asset_code` 已存在 |
| 多币种配置   | ❌ 未实现 | `system_settings` 缺少所有多币种相关配置                             |
| 服务层逻辑   | ❌ 硬编码 | `TradeOrderService` 强制限制 DIAMOND                                 |
| 路由层逻辑   | ❌ 硬编码 | `sell.js` 强制传入 DIAMOND                                           |
| 手续费计算   | ❌ 未支持 | `FeeCalculator` 不支持多币种费率                                     |

---

## 🎯 核心决策（来源：2026-01-14 拍板）

### 交易市场域（C2C撮合交易）- 多币种扩展决策

1. **跨币支付**: ❌ **不允许**（同一订单只用一种结算币，分币种撮合）
2. **同物多币挂牌**: ❌ **不允许**（每个物品同时只能以一种币种挂牌，避免价格混乱）
   - **落地方式**：应用层事务校验 + 锁 `item_instances` 行（粒度明确，防并发穿透）
3. **手续费率模型**：
   - **DIAMOND**：✅ 保持现状分档逻辑（按 `itemValue` 分档 + `ceil` + 最低费）
   - **red_shard**：✅ 单一费率 5%，最低手续费 1 red_shard（不走 itemValue 分档）
4. **red_shard 价格区间**：`min_price = 1`, `max_price = 1,000,000`（硬兜底），超出区间**直接拒绝挂牌**
5. **白名单币种**：初期只开 DIAMOND + red_shard，新币种准入需满足：
   - `material_asset_types.is_tradable = 1`
   - `system_settings.allowed_settlement_assets` 在白名单
   - 该币种的费率、最低费、价格区间、频控阈值已配置齐全
6. **风控阈值（MVP 即上）**：按"用户+币种"维度限制：
   - 日挂单次数上限（例如：每日 20 次/币种）
   - 日成交次数上限（例如：每日 10 次/币种）
   - 日成交额上限（例如：每日 100,000 DIAMOND 或 50,000 red_shard）

---

## 📌 待办事项清单（按优先级排序）

### P0 - 数据库配置（立即可做，改数据不改代码）

| 序号 | 任务                                          | 状态      | SQL/备注                       |
| ---- | --------------------------------------------- | --------- | ------------------------------ |
| 1    | 在 `material_asset_types` 中添加 DIAMOND 记录 | ❌ 待完成 | 需要迁移，设置 `is_tradable=1` |
| 2    | 添加 `allowed_settlement_assets` 配置         | ❌ 待完成 | `["DIAMOND","red_shard"]`      |
| 3    | 添加 `fee_rate_DIAMOND` 配置                  | ❌ 待完成 | `0.05`（5%分档逻辑）           |
| 4    | 添加 `fee_rate_red_shard` 配置                | ❌ 待完成 | `0.05`（5%单一费率）           |
| 5    | 添加 `fee_min_DIAMOND` 配置                   | ❌ 待完成 | `1`                            |
| 6    | 添加 `fee_min_red_shard` 配置                 | ❌ 待完成 | `1`                            |
| 7    | 添加 `min_price_red_shard` 配置               | ❌ 待完成 | `1`（硬兜底下限）              |
| 8    | 添加 `max_price_red_shard` 配置               | ❌ 待完成 | `1000000`（硬兜底上限）        |

**SQL 参考**：

```sql
-- 1. 添加 DIAMOND 到 material_asset_types
INSERT INTO material_asset_types (asset_code, display_name, group_code, form, tier, is_tradable, is_enabled)
VALUES ('DIAMOND', '钻石', 'DIAMOND_GROUP', 'currency', 1, 1, 1)
ON DUPLICATE KEY UPDATE is_tradable=1, is_enabled=1;

-- 2. 添加多币种相关配置
INSERT INTO system_settings (category, setting_key, setting_value, data_type, description, is_visible, is_readonly)
VALUES
  ('marketplace', 'allowed_settlement_assets', '["DIAMOND","red_shard"]', 'json', '交易市场允许的结算币种白名单', 1, 0),
  ('marketplace', 'fee_rate_DIAMOND', '0.05', 'number', 'DIAMOND结算手续费率（5%，保持分档逻辑）', 1, 0),
  ('marketplace', 'fee_rate_red_shard', '0.05', 'number', 'red_shard结算手续费率（5%，单一费率）', 1, 0),
  ('marketplace', 'fee_min_DIAMOND', '1', 'number', 'DIAMOND最低手续费', 1, 0),
  ('marketplace', 'fee_min_red_shard', '1', 'number', 'red_shard最低手续费', 1, 0),
  ('marketplace', 'min_price_red_shard', '1', 'number', 'red_shard最低挂牌价（硬兜底）', 1, 0),
  ('marketplace', 'max_price_red_shard', '1000000', 'number', 'red_shard最高挂牌价（硬兜底）', 1, 0);
```

---

### P1 - 风控配置（MVP即上）

| 序号 | 任务                                     | 状态      | SQL/备注                 |
| ---- | ---------------------------------------- | --------- | ------------------------ |
| 9    | 添加 `daily_max_listings_DIAMOND` 配置   | ❌ 待完成 | `20`（日挂单上限）       |
| 10   | 添加 `daily_max_listings_red_shard` 配置 | ❌ 待完成 | `20`                     |
| 11   | 添加 `daily_max_trades_DIAMOND` 配置     | ❌ 待完成 | `10`（日成交上限）       |
| 12   | 添加 `daily_max_trades_red_shard` 配置   | ❌ 待完成 | `10`                     |
| 13   | 添加 `daily_max_amount_DIAMOND` 配置     | ❌ 待完成 | `100000`（日成交额上限） |
| 14   | 添加 `daily_max_amount_red_shard` 配置   | ❌ 待完成 | `50000`                  |

**SQL 参考**：

```sql
INSERT INTO system_settings (category, setting_key, setting_value, data_type, description, is_visible, is_readonly)
VALUES
  ('marketplace', 'daily_max_listings_DIAMOND', '20', 'number', 'DIAMOND日挂单次数上限', 1, 0),
  ('marketplace', 'daily_max_listings_red_shard', '20', 'number', 'red_shard日挂单次数上限', 1, 0),
  ('marketplace', 'daily_max_trades_DIAMOND', '10', 'number', 'DIAMOND日成交次数上限', 1, 0),
  ('marketplace', 'daily_max_trades_red_shard', '10', 'number', 'red_shard日成交次数上限', 1, 0),
  ('marketplace', 'daily_max_amount_DIAMOND', '100000', 'number', 'DIAMOND日成交额上限', 1, 0),
  ('marketplace', 'daily_max_amount_red_shard', '50000', 'number', 'red_shard日成交额上限', 1, 0);
```

---

### P2 - 服务层改造（需改代码）

| 序号 | 任务                                                     | 文件位置                                | 状态      | 改造说明                                      |
| ---- | -------------------------------------------------------- | --------------------------------------- | --------- | --------------------------------------------- |
| 15   | FeeCalculator 新增 `calculateFeeByAsset()` 方法          | `services/FeeCalculator.js`             | ❌ 待完成 | DIAMOND保持分档逻辑，red_shard用单一费率      |
| 16   | TradeOrderService 移除 DIAMOND 硬编码校验                | `services/TradeOrderService.js:125-138` | ❌ 待完成 | 改为从 `allowed_settlement_assets` 白名单校验 |
| 17   | TradeOrderService 移除 DIAMOND 硬编码校验                | `services/TradeOrderService.js:211-213` | ❌ 待完成 | 同上                                          |
| 18   | TradeOrderService 手续费计算改用 `calculateFeeByAsset()` | `services/TradeOrderService.js`         | ❌ 待完成 | 根据 `asset_code` 选择计费模式                |
| 19   | MarketListingService 新增币种白名单校验                  | `services/MarketListingService.js`      | ❌ 待完成 | 校验 `price_asset_code` 是否在白名单          |
| 20   | MarketListingService 新增价格区间校验                    | `services/MarketListingService.js`      | ❌ 待完成 | red_shard 价格必须在 [1, 1000000]             |
| 21   | MarketListingService 新增同物单币校验                    | `services/MarketListingService.js`      | ❌ 待完成 | 行锁防止同一物品多币种挂牌                    |

**FeeCalculator.calculateFeeByAsset() 设计参考**：

```javascript
/**
 * 根据结算币种计算手续费（多币种扩展版）
 *
 * @param {string} asset_code - 结算币种代码（DIAMOND/red_shard）
 * @param {number} itemValue - 商品价值（用于DIAMOND分档）
 * @param {number} sellingPrice - 用户定价
 * @returns {Object} { fee_amount, fee_rate, net_amount, calculation_mode }
 */
static calculateFeeByAsset(asset_code, itemValue, sellingPrice) {
  if (asset_code === 'DIAMOND') {
    // 保持现状分档逻辑（基于 itemValue 查表 + ceil + 最低费 1）
    return this.calculateItemFee(itemValue, sellingPrice);
  } else if (asset_code === 'red_shard') {
    // 单一费率模式（从 system_settings 读取 fee_rate_red_shard）
    const fee_rate = await SystemSettings.get('marketplace', 'fee_rate_red_shard') || 0.05;
    const fee_min = await SystemSettings.get('marketplace', 'fee_min_red_shard') || 1;

    let fee = Math.floor(sellingPrice * fee_rate); // floor 取整（不是 ceil）
    if (fee < fee_min) fee = fee_min;

    return {
      fee: fee,
      rate: fee_rate,
      net_amount: sellingPrice - fee,
      calculation_mode: 'flat' // 单一费率
    };
  } else {
    throw new Error(`不支持的结算币种: ${asset_code}`);
  }
}
```

**同物单币校验设计参考**：

```javascript
// 在 MarketListingService.createListing() 事务内
const item = await ItemInstance.findOne({
  where: { item_instance_id },
  lock: transaction.LOCK.UPDATE, // 🔥 行级锁（防并发双挂）
  transaction
})

const existingListing = await MarketListing.findOne({
  where: {
    offer_item_instance_id: item_instance_id,
    status: { [Op.in]: ['on_sale', 'locked'] }
  },
  transaction
})

if (existingListing && existingListing.price_asset_code !== price_asset_code) {
  throw new Error(`该物品已以 ${existingListing.price_asset_code} 挂牌，不允许更换币种`)
}
```

---

### P3 - 路由层改造

| 序号 | 任务                                 | 文件位置                       | 状态      | 改造说明                           |
| ---- | ------------------------------------ | ------------------------------ | --------- | ---------------------------------- |
| 22   | sell.js 接受 `price_asset_code` 参数 | `routes/v4/market/sell.js:165` | ❌ 待完成 | 移除硬编码 DIAMOND，从请求参数获取 |
| 23   | sell.js 接受 `price_asset_code` 参数 | `routes/v4/market/sell.js:373` | ❌ 待完成 | 同上                               |
| 24   | 新增风控校验中间件                   | `routes/v4/market/`            | ❌ 待完成 | 日挂单/成交次数/额度限制           |

**路由层改造参考**：

```javascript
// routes/v4/market/sell.js
// 改造前：
price_asset_code: 'DIAMOND'

// 改造后：
price_asset_code: req.body.price_asset_code || 'DIAMOND' // 默认 DIAMOND，兼容旧版
```

**API 参数变更**：

```
POST /api/v4/market/listings

Request Body:
{
  "item_instance_id": 123,
  "price_amount": 1000,
  "price_asset_code": "red_shard"  // 新增参数，可选，默认 DIAMOND
}
```

---

### P4 - 测试与验证

| 序号 | 任务                   | 状态      | 备注                             |
| ---- | ---------------------- | --------- | -------------------------------- |
| 25   | 编写多币种交易集成测试 | ❌ 待完成 | 测试 red_shard 挂牌/购买流程     |
| 26   | 验证手续费计算正确性   | ❌ 待完成 | DIAMOND分档 vs red_shard单一费率 |
| 27   | 验证风控限制生效       | ❌ 待完成 | 日限次/限额                      |

---

## 📊 实现进度统计

| 类别                | 已完成   | 待完成 | 完成率 |
| ------------------- | -------- | ------ | ------ |
| 数据库表结构        | 2/2      | 0      | 100%   |
| 数据库配置（P0+P1） | 0/14     | 14     | 0%     |
| 服务层改造（P2）    | 0/7      | 7      | 0%     |
| 路由层改造（P3）    | 0/3      | 3      | 0%     |
| 测试验证（P4）      | 0/3      | 3      | 0%     |
| **总计**            | **2/29** | **27** | **7%** |

---

## 🚀 建议实施顺序

### 阶段一：数据库迁移（预计 1-2 小时）

1. 创建迁移文件 `add-marketplace-multi-currency-config.js`
2. 添加 DIAMOND 到 `material_asset_types`
3. 添加所有 `system_settings` 配置项（P0 + P1 共 14 项）
4. 执行迁移并验证

### 阶段二：FeeCalculator 扩展（预计 2-3 小时）

1. 新增 `calculateFeeByAsset()` 方法
2. 支持从 `system_settings` 读取币种费率
3. 编写单元测试验证 DIAMOND分档 和 red_shard单一费率

### 阶段三：Service 层改造（预计 3-4 小时）

1. 改造 `TradeOrderService` 移除硬编码
2. 改造 `MarketListingService` 新增校验逻辑
3. 实现同物单币校验（行锁防并发）

### 阶段四：Route 层改造（预计 1-2 小时）

1. 改造 `sell.js` 接受 `price_asset_code` 参数
2. 新增风控校验中间件

### 阶段五：集成测试（预计 2-3 小时）

1. 编写端到端测试用例
2. 验证完整流程
3. 验证边界情况和错误处理

---

## 🔐 风险控制与安全建议

### 防套利机制

- **价格硬兜底**：red_shard 价格必须在 [1, 1000000]，超出直接拒绝
- **日限次/限额**：按"用户+币种"维度限制挂单和成交
- **同物单币**：禁止同一物品以多种币种挂牌，避免价格混乱

### 灰度与回滚

- **灰度上线**：初期只开放 DIAMOND + red_shard，其他币种需配置齐全后才能加入白名单
- **紧急下线**：从 `allowed_settlement_assets` 移除某币种即可立即禁止新挂牌
- **数据兼容**：所有改造保持向后兼容，默认仍为 DIAMOND

---

## 📝 更新记录

| 日期       | 版本 | 更新内容                           | 作者         |
| ---------- | ---- | ---------------------------------- | ------------ |
| 2026-01-14 | V1.0 | 初始版本，基于文档分析生成待办清单 | AI Assistant |

---

**文档结束**
