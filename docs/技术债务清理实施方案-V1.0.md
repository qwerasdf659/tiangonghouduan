# 🔧 技术债务清理实施方案 V1.0

> **文档版本**: V1.5  
> **创建时间**: 2026-01-24  
> **最后更新**: 2026-01-26 22:00（全面真实数据库验证）  
> **目标**: 彻底清理迁移/兼容残留，统一技术栈，降低维护复杂度  
> **适用范围**: 后端代码、数据库（不含前端项目）  
> **决策状态**: 敏感操作中间件采用方案A（严格模式），前端已更新可清理兼容层  
> **验证方式**: Node.js + Sequelize 连接真实数据库验证（非备份文件）

---

## 📋 目录

0. [项目现状总结（基于实际数据）](#零项目现状总结基于实际数据)
0.5. [**实际验证结果（2026-01-24）**](#零五实际验证结果2026-01-24)
1. [清理范围概述](#一清理范围概述)
2. [必须保留的业务双轨设计](#二必须保留的业务双轨设计)
3. [迁移/兼容残留清理清单](#三迁移兼容残留清理清单)
4. [数据库层面清理建议](#四数据库层面清理建议)
5. [功能合并收缩建议](#五功能合并收缩建议)
6. [实施检查清单](#六实施检查清单)
7. [风险评估与回滚方案](#七风险评估与回滚方案)
8. [预期收益](#八预期收益)
9. [后端重构方案（纯后端精简版）](#九后端重构方案纯后端精简版)

---

## 零、项目现状总结（基于实际数据）

> **重要**: 本章节数据来自 2026-01-24 对真实数据库的查询，非历史文档。

### 0.1 技术架构概况

| 维度 | 现状 |
|------|------|
| **后端框架** | Node.js 20+ + Express 4.18 |
| **数据库** | MySQL + Sequelize ORM 6.x |
| **缓存** | Redis (ioredis) 用于缓存和分布式锁 |
| **认证** | JWT + RBAC（UUID角色系统，role_level >= 100 为管理员） |
| **抽奖引擎** | 统一抽奖引擎 V4.6 Pipeline 架构 |
| **API版本** | V4 RESTful 扁平化设计 (`/api/v4/*`) |
| **数据库表** | 69 张表（已完成 V4 迁移） |
| **时区** | 全链路北京时间 (Asia/Shanghai) |

### 0.2 数据库实际数据统计

> ⚠️ **数据更新于 2026-01-26**：以下数据通过 Node.js 脚本连接真实数据库查询获得

#### 核心业务数据

| 数据类型 | 数量 | 说明 |
|----------|------|------|
| **用户总数** | 31 | 按 role_level 分层管理 |
| **活跃抽奖活动** | 4 个 | BASIC_LOTTERY 及测试活动 |
| **物品实例** | 2,190 个 | voucher: 2,188, product: 2 |
| **数据库表数量** | 69 张 | 已完成 V4 迁移 |

#### 角色系统（10个角色）

| 角色 | role_level | 说明 |
|------|------------|------|
| admin | 100 | 管理员 |
| regional_manager | 80 | 区域经理 |
| business_manager | 60 | 业务经理 |
| sales_staff | 40 | 销售员工 |
| merchant_manager | 40 | 商户经理 |
| ops | 30 | 运营 |
| merchant_staff | 20 | 商户员工 |
| campaign_2 | 10 | 活动角色 |
| user | 0 | 普通用户 |
| system_job | -1 | 系统任务 |

#### 资产类型定义（material_asset_types）

> ✅ **2026-01-26 真实数据库验证**

| asset_code | display_name |
|------------|--------------|
| POINTS | 普通积分 |
| BUDGET_POINTS | 预算积分 |
| DIAMOND | 钻石 |
| red_shard | 红色碎片 |

#### 账户类型分布

| account_type | 数量 | 说明 |
|--------------|------|------|
| user | 21 | 用户账户 |
| system | 6 | 系统账户（FEE/CUSTODY/DESTROY/EMISSION等） |

#### 资产交易类型(Top10)

> ✅ **2026-01-26 真实数据库验证**

| business_type | asset_code | 数量 |
|---------------|------------|------|
| exchange_debit | red_shard | 2,174 |
| test_recharge | red_shard | 2,148 |
| admin_adjustment | red_shard | 486 |
| market_listing_withdraw_unfreeze | red_shard | 410 |
| market_listing_freeze | red_shard | 410 |
| admin_adjustment | POINTS | 9 |
| opening_balance | POINTS | 7 |
| merchant_points_reward | POINTS | 6 |
| consumption_budget_allocation | BUDGET_POINTS | 5 |
| consumption_reward | POINTS | 5 |

#### 物品实例状态分布

| status | item_type | 数量 |
|--------|-----------|------|
| used | voucher | 1,137 |
| available | voucher | 515 |
| locked | voucher | 536 |
| available | product | 2 |

### 0.3 功能开关状态（feature_flags）

> ✅ **2026-01-26 真实数据库验证**

| flag_key | 状态 | 说明 |
|----------|------|------|
| lottery_pity_system | ✅ 启用 | 抽奖保底系统 |
| lottery_luck_debt | ✅ 启用 | 运气债务机制 |
| lottery_anti_empty_streak | ✅ 启用 | 防空奖连击 |
| lottery_anti_high_streak | ✅ 启用 | 防高奖连击 |
| lottery_bxpx_matrix | ✅ 启用 | BXPX矩阵策略 |
| lottery_budget_tier | ✅ 启用 | 预算分层 |
| lottery_pressure_tier | ✅ 启用 | 压力分层 |

### 0.4 数据库表清单（69张表）

> ✅ **2026-01-26 真实数据库验证**

已确认存在的表：
```
account_asset_balances, accounts, admin_operation_logs, administrative_regions,
api_idempotency_requests, asset_group_defs, asset_transactions, authentication_sessions,
category_defs, chat_messages, consumption_records, content_review_records,
customer_service_sessions, exchange_items, exchange_records, feature_flags,
feedbacks, image_resources, item_instance_events, item_instances,
item_templates, lottery_campaign_pricing_config, lottery_campaign_quota_grants, lottery_campaign_user_quota,
lottery_campaigns, lottery_clear_setting_records, lottery_daily_metrics, lottery_draw_decisions,
lottery_draw_quota_rules, lottery_draws, lottery_hourly_metrics, lottery_management_settings,
lottery_presets, lottery_prizes, lottery_strategy_config, lottery_tier_matrix_config,
lottery_tier_rules, lottery_user_daily_draw_quota, lottery_user_experience_state, lottery_user_global_state,
market_listings, material_asset_types, material_conversion_rules, merchant_operation_logs,
popup_banners, preset_budget_debt, preset_debt_limits, preset_inventory_debt,
products, rarity_defs, redemption_orders, risk_alerts,
roles, sequelizemeta, store_staff, stores,
system_announcements, system_dictionaries, system_dictionary_history, system_settings,
trade_orders, user_hierarchy, user_premium_status, user_risk_profiles,
user_role_change_records, user_roles, user_status_change_records, users,
websocket_startup_logs
```

### 0.5 商业模式理解

基于数据分析，项目是一个**餐厅积分抽奖系统**：

1. **积分获取**：用户通过消费、签到等方式获取积分
2. **积分消耗**：用户使用积分进行抽奖（每次消耗一定积分）
3. **奖品类型**：
   - 积分奖品（中奖返还更多积分）
   - 优惠券（存入背包，可核销）
   - 谢谢惠顾（未中奖）
4. **背包系统**：
   - `assets[]`：可叠加资产（积分、钻石等）
   - `items[]`：不可叠加物品（优惠券实例）
5. **核销流程**：用户展示二维码 → 商户扫码核销 → 订单完成
6. **管理功能**：
   - 抽奖概率配置
   - 预设中奖（管理员干预）
   - 资产调整
   - 审计日志

---

## 零五、实际验证结果（2026-01-24）

> **重要**: 本章节记录通过 Node.js 脚本连接真实数据库的验证结果，确认清理项当前状态。

### 0.5.1 数据库层面验证结果

#### 旧表清理状态（✅ 全部已清理）

| 旧表名 | 状态 | 说明 |
|--------|------|------|
| `user_prize_budget` | ✅ 不存在 | 已清理 |
| `user_wallet` | ✅ 不存在 | 已清理 |
| `lottery_records` | ✅ 不存在 | 已清理 |
| `prize_records` | ✅ 不存在 | 已清理 |
| `user_inventory` | ✅ 不存在 | 已被 `item_instances` 替代 |
| `points_transactions` | ✅ 不存在 | 已被 `asset_transactions` 替代 |
| `user_points_accounts` | ✅ 不存在 | 已被 `accounts` + `account_asset_balances` 替代 |
| `trade_records` | ✅ 不存在 | 已清理 |

#### V1遗留字段清理状态（✅ 全部已清理）

| 表名 | 字段 | 状态 |
|------|------|------|
| `consumption_records` | `qr_code_version` | ✅ 已清理 |
| `consumption_records` | `is_legacy_v1` | ✅ 已清理 |

#### 空表分析（2026-01-26 真实数据库验证）

> ✅ **通过 Node.js 逐表 COUNT(*) 验证**

| 表名 | 状态 | 说明 |
|------|------|------|
| `lottery_campaign_quota_grants` | 🟡 空表 | 活动配额授权 - 预留功能 |
| `lottery_campaign_user_quota` | 🟡 空表 | 用户活动配额 - 预留功能 |
| `lottery_daily_metrics` | 🟡 空表 | 每日统计 - 数据尚未生成 |
| `lottery_draw_decisions` | 🟡 空表 | 抽奖决策 - 正常业务预留 |
| `lottery_hourly_metrics` | 🟡 空表 | 小时统计 - 数据尚未生成 |
| `lottery_user_experience_state` | 🟡 空表 | 用户体验状态 - 预留功能 |
| `lottery_user_global_state` | 🟡 空表 | 用户全局状态 - 预留功能 |
| `preset_budget_debt` | 🟡 空表 | 预设预算欠账 - 业务需要 |
| `preset_inventory_debt` | 🟡 空表 | 预设库存欠账 - 业务需要 |
| `system_dictionary_history` | 🟡 空表 | 字典历史 - 待评估 |

**空表总数**: 10张（均为业务预留功能，**无需删除**）

**有数据的表**: 59张（总计69张表）

#### 账户双轨验证（✅ 正常运行）

| account_type | 数量 | 说明 |
|--------------|------|------|
| `user` | 21 个 | 用户账户 |
| `system` | 6 个 | 系统账户（FEE/CUSTODY/DESTROY/EMISSION等） |

#### 物品实例状态分布

| status | item_type | 数量 |
|--------|-----------|------|
| `used` | voucher | 1,137 |
| `locked` | voucher | 536 |
| `available` | voucher | 515 |
| `available` | product | 2 |

#### 核销订单状态分布

> ✅ **2026-01-26 真实数据库验证**

| status | 数量 |
|--------|------|
| `expired` | 510 |
| `pending` | 506 |
| `fulfilled` | 3 |
| `cancelled` | 1 |

### 0.5.2 代码层面验证结果（2026-01-26 更新）

#### 已确认存在的兼容代码（待清理）

| 文件 | 行号 | 问题 | 状态 | 决策 |
|------|------|------|------|------|
| `middleware/sensitiveOperation.js` | L64-72 | 旧Token兼容（缺少session_token时允许通过） | ⏳ 待清理 | **采用方案A：立即严格模式** |
| `services/PrizePoolService.js` | L144-153 | `value_points`/`budget_cost_points`/`probability` 字段兼容 | ⏳ 待清理 | 前端已更新，可清理 |
| `routes/v4/console/marketplace.js` | L40-62 | 路径映射中间件（`/exchange-items` → `/exchange_market/items`） | ⏳ 待清理 | 前端已更新，可清理 |
| `routes/v4/shop/audit/index.js` | L123-128, L180-186 | 响应字段别名（`operator`, `store`, `target_user`） | ⏳ 待清理 | 前端已更新，可清理 |
| `routes/v4/lottery/campaigns.js` | L158-178 | 定价配置格式兼容（数组→对象转换） | ⏳ 待清理 | 前端已更新，可清理 |

#### ❌ 文档记录有误的项（无需清理）

| 原文档记录 | 实际状态 | 说明 |
|------------|----------|------|
| `app.js` L524-600 `PAGE_REDIRECT_MAP` | **不存在** | 实际代码中无此内容，无需清理 |
| `sharedComponents` 别名 | **被105处正常使用** | 是合理的代码抽象层（getter懒加载），不应清理 |

### 0.5.3 根目录残留文件验证

> ✅ **2026-01-26 文件系统验证**

| 文件 | 状态 | 处理方式 |
|------|------|----------|
| `app.js.backup.20260108_125922` | ⏳ 存在 | ❌ 待删除 |
| `数据库迁移脚本.sql` | ⏳ 存在 | ❌ 待删除 |

**已删除的临时文件**（V1.3 执行完成）:
- ~~`db_analyze.js`~~ ✅ 已删除
- ~~`generate_api_business_content.py`~~ ✅ 已删除
- ~~`analyze_project.js/2/3`~~ ✅ 已删除
- ~~`test_material_api.sh`~~ ✅ 已删除
- ~~`test-config-api-auth.sh`~~ ✅ 已删除

**保留的文件**（正常使用）:
- `deploy.sh` - 部署脚本
- `test_admin_api.sh` - 管理API测试脚本

### 0.5.4 scripts/migration 残留脚本

> ✅ **2026-01-26 文件系统验证**

| 文件 | 状态 | 处理方式 |
|------|------|----------|
| `scripts/migration/analyze-old-codes.sql` | ⏳ 存在 | ❌ 待删除 |
| `scripts/migration/EXECUTE_TONIGHT.sh` | ⏳ 存在 | ❌ 待删除 |
| ~~`scripts/migration/add_opening_balance.js`~~ | ✅ 已删除 | - |

### 0.5.5 验证结论

> ✅ **2026-01-26 真实数据库验证**

| 维度 | 状态 | 说明 |
|------|------|------|
| **数据库旧表** | ✅ 已清理 | 7个旧表均已确认不存在 |
| **空表** | ✅ 业务预留 | 10张空表均为预留功能，不删除 |
| **账户双轨** | ✅ 正常运行 | user(21) + system(6) 设计正确 |
| **代码兼容层** | ⏳ 待清理 | 5处兼容代码确认存在（已决策可清理） |
| **残留文件** | ⏳ 部分完成 | 已删除8个，仍有5个待删除 |
| **迁移文件归档** | ⏳ 待执行 | 160个迁移文件待归档75个到archive/ |

---

## 一、清理范围概述

### 1.1 清理原则

| 原则 | 说明 |
|------|------|
| **暴力重构** | 不兼容旧接口，直接删除旧方案 |
| **业务优先** | 保证业务功能不变 |
| **代码对齐** | 文档和代码完全对齐，无残留 |
| **一次投入** | 项目未上线，可一次性彻底清理 |

### 1.2 清理范围

- ✅ 后端代码（services、routes、models）
- ✅ 数据库（废弃表、废弃字段）
- ✅ 脚本文件（临时脚本、旧迁移脚本）
- ✅ 规范文档（`.cursor/rules/`）
- ❌ 前端项目（不在本次清理范围内）

---

## 二、必须保留的业务双轨设计

以下是**业务设计必须保留**的双轨架构，**不清理**：

| 双轨设计 | 位置 | 保留原因 |
|----------|------|----------|
| **背包双轨架构** | `BackpackService.js` | `assets[]`（可叠加资产）+ `items[]`（不可叠加物品）是业务模型设计 |
| **账户双轨架构** | `Account` 模型 | user账户 + system账户 区分用户和系统资产 |
| **分析双轨查询** | `LotteryAnalyticsService.js` | 近期查明细表(`lottery_draws`) + 历史查聚合表(`hourly_metrics`) 是性能优化设计 |
| **决策来源三轨** | `UnifiedLotteryEngine` | `preset/override/normal` 是抽奖决策优先级机制，管理员干预功能 |
| **抽奖fallback保底** | 抽奖引擎多处 | 档位降级路径 `high → mid → low → fallback` 是业务功能 |
| **临时补偿次数** | `LotteryUserDailyDrawQuota` | `bonus_draw_count` 是客服加次数功能，业务设计 |
| **V2动态二维码** | `routes/v4/shop/consumption/qrcode.js` | 当前版本，V1已移除，非双轨 |

### 业务双轨详细说明

#### 2.1 背包双轨架构

```javascript
// BackpackService.js 返回结构
{
  assets: [           // 可叠加资产（积分、钻石、材料）
    { asset_code: 'POINTS', balance: 1000 },
    { asset_code: 'DIAMOND', balance: 50 }
  ],
  items: [            // 不可叠加物品（优惠券、奖品实例）
    { item_instance_id: 'xxx', item_type: 'COUPON' }
  ]
}
```

#### 2.2 账户类型双轨

```javascript
// Account 模型 account_type 字段
- 'user': 用户账户（用户资产）
- 'system': 系统账户（手续费/发放/销毁/托管）
```

#### 2.3 分析时间双轨

```javascript
// LotteryAnalyticsService.js 查询策略
- 24小时内数据: 查询 lottery_draws 表（实时性优先）
- 7-90天历史数据: 查询 lottery_hourly_metrics 表（性能优先）
- 90天以上数据: 查询 lottery_daily_metrics 表（存储效率优先）
```

---

## 三、迁移/兼容残留清理清单

### 【中间件层面】

#### 3.0 敏感操作旧Token兼容（P0 安全相关）

| 文件 | 行号 | 问题 | 处理方式 |
|------|------|------|----------|
| `middleware/sensitiveOperation.js` | 第60-73行 | 旧Token兼容逻辑（缺少session_token时允许通过） | ❌ **删除兼容分支，统一严格模式** |

**详细说明**:
```javascript
// 待删除代码位置：middleware/sensitiveOperation.js 第60-73行
// 当前逻辑：缺少 session_token 时仅警告但允许通过
// 目标：统一使用严格模式，缺少 session_token 直接拒绝

// 🔴 当前兼容代码（需删除）
if (!sessionToken) {
  logger.warn(`⚠️ [SensitiveOp] 敏感操作缺少session_token...`)
  // 暂时允许通过，后续可改为拒绝：
  return next()  // ← 这是兼容旧Token的逻辑，需删除
}

// ✅ 改为严格模式
if (!sessionToken) {
  return res.apiError('会话信息缺失，请重新登录', 'SESSION_REQUIRED', null, 401)
}
```

**方案选择**:
- ✅ **方案A：修改 `requireValidSession` 为严格模式（已决策采用）**
- 方案B：所有敏感操作改用 `requireValidSessionStrict`

> **决策记录 (2026-01-26)**：用户确认前端已更新，选择方案A立即强制严格模式。

---

### 【服务层面】

#### 3.0.1 奖品创建字段兼容

| 文件 | 行号 | 问题 | 处理方式 |
|------|------|------|----------|
| `services/PrizePoolService.js` | 第141-153行 | 多字段兼容逻辑 | ❌ **删除旧字段兼容** |

**详细说明**:
```javascript
// 待清理代码位置：services/PrizePoolService.js 第141-153行

// 🔴 当前兼容代码（需删除旧字段支持）
prize_value_points:
  parseInt(
    prizeData.prize_value_points ??
      prizeData.value_points ??      // ← 旧字段兼容，需删除
      prizeData.budget_cost_points ?? // ← 旧字段兼容，需删除
      0
  ) || 0,
win_probability: prizeData.win_probability || prizeData.probability || 0, // probability 是旧字段

// ✅ 清理后（只接受规范字段）
prize_value_points: parseInt(prizeData.prize_value_points ?? 0) || 0,
win_probability: prizeData.win_probability || 0,
```

**前置条件**：~~确认前端已统一使用 `prize_value_points` 和 `win_probability`~~
> ✅ **已确认 (2026-01-26)**：用户确认前端已更新，可以清理

---

#### 3.0.2 API响应格式字段别名

| 文件 | 行号 | 问题 | 处理方式 |
|------|------|------|----------|
| `routes/v4/shop/audit/index.js` | 第122-128行, 180-186行 | 响应字段别名兼容 | ❌ **删除别名字段** |

**详细说明**:
```javascript
// 待清理代码位置：routes/v4/shop/audit/index.js

// 🔴 当前兼容代码（添加了别名字段）
const logs = result_data.items.map(log => ({
  ...log,
  operator: log.operator_info,     // ← 别名字段，需删除
  store: log.store_info,           // ← 别名字段，需删除
  target_user: log.target_user_info // ← 别名字段，需删除
}))

// ✅ 清理后（直接返回，前端使用 operator_info 等字段名）
const logs = result_data.items
```

**前置条件**：~~确认前端已使用 `operator_info`、`store_info`、`target_user_info`~~
> ✅ **已确认 (2026-01-26)**：用户确认前端已更新，可以清理

---

#### 3.0.3 路由层路径映射兼容

| 文件 | 行号 | 问题 | 处理方式 |
|------|------|------|----------|
| `routes/v4/console/marketplace.js` | 第40-62行 | 前端路径简化兼容中间件 | ❌ **删除路径映射** |

**详细说明**:
```javascript
// 待清理代码位置：routes/v4/console/marketplace.js 第40-62行

// 🔴 当前兼容中间件（路径重写）
const pathMappings = {
  '/exchange-items': '/exchange_market/items',
  '/exchange-orders': '/exchange_market/orders',
  '/exchange-stats': '/exchange_market/statistics',
  '/trade-orders': '/trade_orders'
}
// ... 路径重写逻辑
```

**前置条件**：~~确认前端已使用规范路径~~
> ✅ **已确认 (2026-01-26)**：用户确认前端已更新，可以清理

---

### 【代码层面】

#### 3.1 需要清理的历史注释

| 文件 | 行号/位置 | 清理内容 |
|------|-----------|----------|
| `services/index.js` | 第17行 | `🆕 积分操作统一使用 AssetService（已移除 PointsService）` |
| `services/IdempotencyService.js` | 第78-79行 | `【已拍板决策 2026-01-19】：路径双轨清理` |
| `services/IdempotencyService.js` | 第614-615, 796-797行 | 强制不兼容策略注释 |
| `routes/v4/shop/exchange/exchange.js` | 第23, 29, 57, 114行 | 路径双轨清理历史注释 |
| `services/ConsumptionService.js` | 第280, 376行 | 架构决策和历史兼容注释 |
| `services/UserRoleService.js` | 多处 | `从 UserPermissionModule 迁移` 注释 |
| `services/ChatWebSocketService.js` | 第174行 | `删除 is_admin 兼容` 注释 |
| `services/ImageService.js` | 第476, 480行 | 架构决策/兼容代码清理注释 |
| `services/DataSanitizer.js` | 第64行 | `兼容字段清理` 注释 |
| `models/index.js` | 第17行 | `已移除 PointsService` 注释 |

#### 3.2 无需清理的代码

- **实际双轨代码**: 背包双轨、账户双轨、分析双轨 — 这些是业务设计，保留
- **路由代码**: 项目已完成路径双轨清理，只保留了 canonical 路径，无需再删除路由

#### 3.3 app.js 中的页面重定向残留

> ❌ **2026-01-26 验证结果**：`PAGE_REDIRECT_MAP` 在当前 `app.js` 中**不存在**，此项无需处理。

~~| 行号 | 内容 | 处理方式 |~~
~~|------|------|----------|~~
~~| 524-581 | `PAGE_REDIRECT_MAP` 旧页面映射 | ❌ **删除整段** |~~
~~| 583-600 | 重定向中间件 `/admin` | ❌ **删除整段** |~~

#### 3.4 路由层兼容代码

| 文件 | 行号 | 问题 | 处理方式 |
|------|------|------|----------|
| `routes/v4/lottery/campaigns.js` | L158-178 | "兼容旧的 API 返回结构"（数组→对象转换） | ❌ **删除兼容逻辑** |

> ❌ **2026-01-26 验证结果**：`sharedComponents` 是被 **105处代码正常使用** 的合理抽象层（getter懒加载机制），**不应清理**。

~~| `routes/v4/console/shared/middleware.js` | 第74行 | "兼容旧代码：提供 sharedComponents 别名" | ❌ **删除别名** |~~

#### 3.5 遗留抽奖Strategy

| 文件 | 状态 | 处理方式 |
|------|------|----------|
| `services/UnifiedLotteryEngine/strategies/ManagementStrategy.js` | 仍被管理API使用 | ✅ **保留** |
| `services/UnifiedLotteryEngine/strategies/` 目录下其他文件 | 待确认 | ⏳ **检查引用后决定** |

**确认命令**:
```bash
# 检查 strategies 目录下各文件的引用情况
grep -rln "BasicGuaranteeStrategy" --include="*.js" .
grep -rln "from.*strategies" --include="*.js" .
```

---

### 【脚本和临时文件】

> ✅ **2026-01-26 文件系统验证** - 已部分清理

#### 待删除文件（仍存在）

| 文件 | 问题 | 状态 |
|------|------|------|
| `数据库迁移脚本.sql` | 引用已不存在的旧表 | ⏳ 待删除 |
| `app.js.backup.20260108_125922` | 备份文件 | ⏳ 待删除 |
| `migrations/20251206_dual_account_system.sql` | 已被Sequelize迁移替代 | ⏳ 待删除 |
| `scripts/migration/EXECUTE_TONIGHT.sh` | 旧迁移执行脚本 | ⏳ 待删除 |
| `scripts/migration/analyze-old-codes.sql` | 旧分析脚本 | ⏳ 待删除 |

#### 已删除文件（V1.3 清理完成）

| 文件 | 状态 |
|------|------|
| `db_analyze.js` | ✅ 已删除 |
| `generate_api_business_content.py` | ✅ 已删除 |
| `analyze_project.js` / `analyze_project2.js` / `analyze_project3.js` | ✅ 已删除 |
| `test_material_api.sh` / `test-config-api-auth.sh` | ✅ 已删除 |
| `scripts/migration/add_opening_balance.js` | ✅ 已删除 |

#### 保留的文件（正常使用）

| 文件 | 用途 |
|------|------|
| `deploy.sh` | 部署脚本 |
| `test_admin_api.sh` | 管理API测试脚本 |

---

### 【规范层面】

| 文件 | 行号 | 问题 | 处理方式 |
|------|------|------|----------|
| `.cursor/rules/08-生产质量保证标准.mdc` | 第453行 | 引用 `UserInventory` 旧表示例 | ❌ **更新示例代码** |
| `.cursor/rules/08-生产质量保证标准.mdc` | 第442行 | `MIGRATION_CUTOFF_DATE` 迁移双轨示例 | ❌ **删除该示例** |

**更新说明**:
- 移除所有引用已删除旧表的示例代码
- 移除迁移双轨的示例（因为项目采用暴力重构策略，不保留迁移双轨）

---

## 四、数据库层面清理建议

### 4.1 数据库表状态

| 表名 | 当前状态 | 建议动作 |
|------|----------|----------|
| `trade_records` | `is_legacy=1`，只读 | ✅ 保留为历史归档，不删除 |
| `user_points_accounts` | 已标记废弃 | ⏳ 确认不存在后无需操作 |
| `points_transactions` | 已被 `asset_transactions` 替代 | ⏳ 确认不存在后无需操作 |
| `user_inventory` | 已被 `item_instances` + `redemption_orders` 替代 | ⏳ 确认不存在后无需操作 |

### 4.2 需确认的潜在旧表

执行以下SQL检查旧表是否存在：

```sql
-- 检查这些旧表是否存在（如存在且无引用则删除）
SHOW TABLES LIKE 'user_prize_budget';
SHOW TABLES LIKE 'user_wallet';
SHOW TABLES LIKE 'lottery_records';
SHOW TABLES LIKE 'prize_records';
SHOW TABLES LIKE 'user_inventory';
SHOW TABLES LIKE 'points_transactions';
SHOW TABLES LIKE 'user_points_accounts';
```

**处理原则**:
- 如果表存在但代码中无引用 → 创建迁移删除表
- 如果表存在且仍有引用 → 分析引用来源，决定是否需要清理引用代码

### 4.3 V1二维码遗留字段

| 位置 | 问题 | 处理方式 |
|------|------|----------|
| `consumption_records` 表 | `qr_code_version`、`is_legacy_v1` 字段 | ❌ **创建迁移删除字段** |
| `migrations/20260112190000-mark-v1-qrcode-as-legacy.js` | V1标记迁移（已执行完毕） | ⏳ **归档到 archive/ 或删除** |

**待创建的清理迁移**:
```javascript
// migrations/YYYYMMDDHHMMSS-remove-v1-qrcode-legacy-fields.js
module.exports = {
  async up(queryInterface, Sequelize) {
    await queryInterface.removeColumn('consumption_records', 'qr_code_version');
    await queryInterface.removeColumn('consumption_records', 'is_legacy_v1');
  },
  async down(queryInterface, Sequelize) {
    // 回滚逻辑（如需要）
  }
};
```

---

## 五、功能合并收缩建议

### 5.1 可合并的API路由

| 合并建议 | 当前路由 | 合并后 |
|----------|----------|--------|
| **资产调整统一** | `/console/asset-adjustment/`, `/console/material/*/adjust`, `/console/diamond-accounts/adjust` | 统一到 `/console/asset-adjustment/adjust` |
| **抽奖监控统一** | `/console/lottery-monitoring/`, `/console/lottery-strategy-stats/` | 统一到 `/console/lottery-analytics/` |

### 5.2 迁移文件归档

**现状**: `migrations/` 目录有 **160 个迁移文件**
- 2025年迁移：75个
- 2026年迁移：83个（当前开发活跃）

**决策 (2026-01-26)**：✅ **执行归档**

**归档方案**:
1. 创建 `migrations/archive/` 目录
2. 将2025年的75个迁移文件移动到 archive 目录
3. 保留2026年的迁移文件用于当前开发

**操作命令**:
```bash
# 创建归档目录
mkdir -p migrations/archive

# 移动2025年的迁移到归档
mv migrations/2025*.js migrations/archive/
```

**预期收益**:
- 降低开发时的心智负担（从160个减少到83个）
- 加快 `npx sequelize-cli db:migrate:status` 执行速度
- 保持迁移目录整洁
- 历史迁移仍可在 archive 目录查阅

### 5.3 备份目录清理

| 目录 | 处理方式 |
|------|----------|
| `backups/` | ⏸️ **暂不处理**（空间充足，低优先级） |

---

## 六、实施检查清单

### Phase 0：安全相关清理（P0 优先级）

安全相关的兼容代码，必须优先处理：

- [ ] **【P0 - 已决策】** `middleware/sensitiveOperation.js` L60-73 - 删除旧Token兼容，**采用方案A严格模式**（前端已更新）

### Phase 1：清理历史注释（低风险）

纯文本修改，无功能影响：

- [ ] 清理 `services/index.js` 第17行注释
- [ ] 清理 `services/IdempotencyService.js` 第78-79, 614-615, 796-797行注释
- [ ] 清理 `routes/v4/shop/exchange/exchange.js` 第23, 29, 57, 114行注释
- [ ] 清理 `services/ConsumptionService.js` 第280, 376行注释
- [ ] 清理 `services/UserRoleService.js` 迁移相关注释
- [ ] 清理 `services/ChatWebSocketService.js` 第174行注释
- [ ] 清理 `services/ImageService.js` 第476, 480行注释
- [ ] 清理 `services/DataSanitizer.js` 第64行注释
- [ ] 清理 `models/index.js` 第17行注释

### Phase 2：验证废弃表（需验证）

仅确认，不删除：

- [ ] 执行 SQL 检查旧表是否存在
- [ ] 确认 `trade_records` 保持只读归档状态
- [ ] 记录检查结果

### Phase 3：代码清理（中风险）

> ✅ **决策状态 (2026-01-26)**：用户确认前端已更新，以下兼容代码可以安全清理

**服务层清理**:
- [ ] `services/PrizePoolService.js` L141-153 - 删除字段兼容逻辑（`value_points`, `budget_cost_points`, `probability`）

**路由层清理**:
- ~~[ ] 删除 `app.js` 第524-600行~~ → **❌ 无需处理**：`PAGE_REDIRECT_MAP` 不存在
- [ ] 删除 `routes/v4/lottery/campaigns.js` L158-178 兼容逻辑（数组→对象转换）
- ~~[ ] 删除 `routes/v4/console/shared/middleware.js` 兼容别名~~ → **❌ 不应清理**：`sharedComponents` 被正常使用
- [ ] 删除 `routes/v4/console/marketplace.js` L40-62 路径映射中间件
- [ ] 删除 `routes/v4/shop/audit/index.js` L122-128, L180-186 响应字段别名

**引擎层清理**:
- [ ] 确认并清理 `services/UnifiedLotteryEngine/strategies/` 遗留文件

### Phase 4：脚本和文件清理

- [ ] 删除 `数据库迁移脚本.sql`
- [ ] 删除 `migrations/20251206_dual_account_system.sql`
- [ ] 删除 `scripts/migration/EXECUTE_TONIGHT.sh`
- [ ] 删除 `scripts/migration/analyze-old-codes.sql`
- [ ] 删除 `test_material_api.sh`
- [ ] 删除 `test-config-api-auth.sh`
- [ ] 删除 `app.js.backup.*`
- [ ] 评估删除 `db_analyze.js`
- [ ] 评估删除 `generate_api_business_content.py`

### Phase 5：数据库清理

- [ ] 创建迁移删除 `consumption_records` 表的 V1 相关字段
- [ ] 检查并删除数据库中的旧表（执行 4.2 节SQL）

### Phase 6：规范更新

- [ ] 更新 `.cursor/rules/08-生产质量保证标准.mdc` 移除旧表引用
- [ ] 更新 `.cursor/rules/08-生产质量保证标准.mdc` 移除迁移双轨示例
- [ ] 全局搜索确认无其他文档引用已删除的表/接口

### Phase 7：验证与测试

- [ ] 运行完整测试套件 `npm test`
- [ ] 验证所有 API 端点正常工作
- [ ] 检查数据库迁移状态 `npx sequelize-cli db:migrate:status`

### Phase 8（可选）：API路由合并

需要前后端配合时再执行：

- [ ] 资产调整路由统一
- [ ] 抽奖监控路由统一

---

## 七、风险评估与回滚方案

### 7.1 风险评估

| 风险项 | 风险等级 | 缓解措施 |
|--------|----------|----------|
| 删除代码导致功能异常 | 中 | 每次删除后立即运行测试 |
| 数据库迁移失败 | 低 | 备份数据库后再执行迁移 |
| 遗漏引用导致运行时错误 | 中 | 使用 grep 全局搜索确认无引用 |

### 7.2 回滚方案

1. **代码回滚**: 使用 Git 回滚到清理前的 commit
   ```bash
   git revert <cleanup-commit-hash>
   ```

2. **数据库回滚**: 使用 Sequelize 回滚迁移
   ```bash
   npx sequelize-cli db:migrate:undo
   ```

3. **完整回滚**: 从备份恢复
   ```bash
   # 恢复数据库备份
   mysql -u root -p database_name < backups/pre-cleanup-backup.sql
   ```

---

## 八、预期收益

### 8.1 量化收益

| 指标 | 清理前 | 清理后 | 改善幅度 |
|------|--------|--------|----------|
| 代码行数（预估） | - | -500行 | 减少冗余代码 |
| 迁移文件数量 | 130+ | 20-30 | 减少 80% |
| 临时脚本文件 | 10+ | 0 | 100% 清理 |
| 旧表/字段 | 若干 | 0 | 100% 清理 |

### 8.2 质化收益

- ✅ **降低维护复杂度**: 无需维护兼容层代码
- ✅ **减少认知负担**: 开发者无需理解新旧两套方案
- ✅ **提高代码质量**: 统一技术栈，代码更加一致
- ✅ **降低Bug风险**: 减少因兼容逻辑导致的边界问题
- ✅ **文档对齐**: 文档与代码完全一致，无过时内容

---

## 九、后端重构方案（纯后端精简版）

> 本节为重构方案的精简执行摘要，适合快速执行参考。

### 一、业务双轨（保留）

| 双轨 | 文件 | 保留理由 |
|------|------|----------|
| **背包：assets + items** | `services/BackpackService.js` | 可叠加 vs 实例化资产 |
| **账户：user + system** | `services/AssetService.js` | 标准账务架构 |
| **审核 + 审计** | `ContentReviewRecord` + `AdminOperationLog` | 不同业务语义 |
| **决策来源三轨** | `UnifiedLotteryEngine` | preset/override/normal 优先级机制 |
| **抽奖保底机制** | `TierPickStage.js` | high → mid → low → fallback 降级路径 |
| **临时补偿次数** | `LotteryUserDailyDrawQuota` | bonus_draw_count 客服功能 |

---

### 二、清理项

#### 🔴 2.0 P0 安全相关（优先执行）

| 文件 | 清理内容 | 决策 |
|------|----------|------|
| `middleware/sensitiveOperation.js` | L60-73 删除旧Token兼容 | ✅ **已决策：采用方案A严格模式** |

#### 🗑️ 2.1 根目录残留

```bash
rm -f app.js.backup.20260108_125922
rm -f 数据库迁移脚本.sql
rm -f test_material_api.sh test-config-api-auth.sh
rm -rf backups/
```

#### 🗑️ 2.2 服务层字段兼容

| 文件 | 清理内容 |
|------|----------|
| `services/PrizePoolService.js` | L141-153 删除 `value_points`、`budget_cost_points`、`probability` 兼容 |

#### 🗑️ 2.3 路由层兼容代码

> ✅ **决策状态 (2026-01-26)**：用户确认前端已更新，可安全清理

| 文件 | 清理内容 | 状态 |
|------|----------|------|
| `routes/v4/console/marketplace.js` | L40-62 删除路径映射中间件 | ⏳ 待执行 |
| `routes/v4/shop/audit/index.js` | L122-128, L180-186 删除响应字段别名 | ⏳ 待执行 |
| `routes/v4/lottery/campaigns.js` | L158-178 删除定价配置格式兼容 | ⏳ 待执行 |
| ~~`routes/v4/console/shared/middleware.js`~~ | ~~L74 删除 `sharedComponents` 别名~~ | ❌ **不应清理** - 被105处正常使用 |

#### 🗑️ 2.4 过时注释清理

| 文件 | 清理内容 |
|------|----------|
| `services/index.js` | L18/L128 过时注释 |
| `services/IdempotencyService.js` | L78-79, L614-615, L796-797 决策注释 |
| `services/ConsumptionService.js` | L280, L376 架构决策注释 |
| `services/ChatWebSocketService.js` | L174 `is_admin 兼容` 注释 |
| `scripts/maintenance/scheduled_tasks.js` | L34/L50 移除说明 |
| `scripts/verify-doc-decisions.js` | 旧表检查逻辑（整个脚本可删） |

#### 🗑️ 2.5 数据库验证

```sql
-- 确认无 PascalCase 残留
SELECT target_type FROM admin_operation_logs WHERE target_type REGEXP '^[A-Z]' LIMIT 5;

-- 确认旧表不存在
SHOW TABLES LIKE 'user_inventory';
SHOW TABLES LIKE 'points_transactions';
SHOW TABLES LIKE 'user_points_accounts';
```

---

### 三、合并收缩

#### 3.1 路由合并

| 从 | 到 |
|----|----|
| `/api/v4/merchant-points` | `/api/v4/shop/consumption` |
| `/api/v4/activities` | `/api/v4/lottery/campaigns` |
| `/api/v4/debug-control` | `/api/v4/system/debug` |

#### 3.2 配置合并

- `config/marketplace.config.js` → 合并入 `config/business.config.js`
- `config/segment_rules.js` → 合并入 `config/business.config.js`

---

### 四、规范对齐

更新 `.cursor/rules/01-核心开发质量标准.mdc`：
- `/api/v2/*` 示例 → `/api/v4/*`

---

### 执行总结

| 类别 | 数量 |
|------|------|
| P0 安全修复 | 1 处（敏感操作中间件） |
| 删除残留文件 | 5-10 个 |
| 服务层字段兼容 | 1 处（PrizePoolService） |
| 路由层兼容代码 | 4 处 |
| 清理过时注释 | ~50 行 |
| 合并路由域 | 3 个 |
| 合并配置文件 | 2 个 |

---

## 附录

### A. 全局搜索命令参考

```bash
# 搜索旧表引用
grep -rln "user_prize_budget\|user_wallet\|lottery_records\|user_inventory" --include="*.js" .

# 搜索兼容相关注释
grep -rln "兼容\|deprecated\|legacy\|旧" --include="*.js" .

# 搜索 PointsService 引用
grep -rln "PointsService" --include="*.js" .

# 搜索 UserPermissionModule 引用
grep -rln "UserPermissionModule" --include="*.js" .

# 搜索旧字段兼容（服务层）
grep -rln "value_points\|budget_cost_points\|probability" --include="*.js" services/

# 搜索 sharedComponents 使用情况
grep -rln "sharedComponents" --include="*.js" routes/

# 搜索敏感操作中间件使用
grep -rln "requireValidSession\|requireValidSessionStrict" --include="*.js" routes/

# 搜索路径映射逻辑
grep -rln "pathMappings\|pathMapping" --include="*.js" routes/
```

### B. 相关文档

- `.cursor/rules/08-生产质量保证标准.mdc` - 生产质量保证标准

---

## 附录 C：项目结构概览

### 路由结构 (`/routes/v4/`)

```
routes/v4/
├── auth/           # 认证相关 (登录、注册、Token)
├── console/        # 管理后台
│   ├── asset-adjustment/   # 资产调整
│   ├── lottery-monitoring/ # 抽奖监控
│   ├── marketplace/        # 市场管理
│   ├── shared/             # 共享组件
│   └── ...
├── lottery/        # 抽奖系统
├── market/         # 市场交易
├── shop/           # 商户功能
│   ├── audit/      # 审计日志
│   ├── consumption/# 消费积分
│   └── exchange/   # 兑换功能
├── system/         # 系统管理
├── user/           # 用户相关
├── assets/         # 资产管理
├── backpack/       # 背包系统
├── merchant-points/# 商户积分
├── activities/     # 活动管理
└── debug-control/  # 调试控制
```

### 服务层结构 (`/services/`)

```
services/
├── AssetService.js           # 资产管理核心服务
├── BackpackService.js        # 背包双轨架构
├── ConsumptionService.js     # 消费积分服务
├── IdempotencyService.js     # 幂等性控制
├── ImageService.js           # 图片管理
├── LotteryAnalyticsService.js# 抽奖统计分析
├── PrizePoolService.js       # 奖池管理
├── UserRoleService.js        # 用户角色管理
├── ChatWebSocketService.js   # WebSocket聊天
├── UnifiedLotteryEngine/     # 统一抽奖引擎 V4.6
│   ├── stages/               # Pipeline 阶段
│   │   ├── ValidationStage.js
│   │   ├── TierPickStage.js
│   │   ├── SettlementStage.js
│   │   └── ...
│   └── strategies/           # 抽奖策略
└── index.js                  # 服务聚合导出
```

### 模型层结构 (`/models/`)

```
models/
├── User.js                   # 用户模型
├── Role.js                   # 角色模型
├── UserRole.js               # 用户角色关联
├── Account.js                # 账户模型（双轨：user/system）
├── AccountAssetBalance.js    # 账户资产余额
├── AssetTransaction.js       # 资产交易流水
├── ItemInstance.js           # 物品实例
├── ItemTemplate.js           # 物品模板
├── RedemptionOrder.js        # 核销订单
├── LotteryCampaign.js        # 抽奖活动
├── LotteryPrize.js           # 奖品配置
├── LotteryDraw.js            # 抽奖记录
├── FeatureFlag.js            # 功能开关
└── index.js                  # 模型聚合 + 关联定义
```

---

**文档维护者**: AI Assistant  
**最后更新**: 2026-01-26 22:00  
**状态**: Phase 1 已执行，继续执行中  
**版本说明**: 
- V1.0 - 初始版本，新增第零章"项目现状总结"（基于实际数据库数据），新增附录C项目结构概览
- V1.1 - 新增第0.5章"实际验证结果"，记录通过Node.js脚本连接真实数据库的验证结论
- V1.2 - **基于2026-01-26真实验证更新**：
  - ✅ 用户决策：敏感操作中间件采用方案A（立即严格模式）
  - ✅ 用户确认：前端已更新，兼容代码可安全清理
  - ❌ 修正错误：`PAGE_REDIRECT_MAP` 实际不存在，无需清理
  - ❌ 修正错误：`sharedComponents` 是合理抽象层（105处使用），不应清理
- V1.3 - **2026-01-26 Phase 1 执行**：
  - ✅ **已删除8个临时文件**（db_analyze.js、generate_api_business_content.py、analyze_project*.js、test脚本、add_opening_balance.js）
  - ✅ **保留文件**：`deploy.sh`、`test_admin_api.sh`
- V1.4 - **2026-01-26 决策补充**：
  - ✅ **迁移文件归档**：将2025年的75个迁移文件归档到 `migrations/archive/`（待执行）
  - ⏸️ **backups目录**：暂不处理（空间充足）
- V1.5 - **2026-01-26 全面真实数据库验证**：
  - 📊 **资产类型**：更正为实际4种（POINTS、BUDGET_POINTS、DIAMOND、red_shard）
  - 📊 **功能开关**：更正为实际7个抽奖相关开关（lottery_pity_system等）
  - 📊 **空表**：更正为实际10张空表（包含 preset_budget_debt/preset_inventory_debt）
  - 📊 **表总数**：69张（59张有数据 + 10张空表）
  - ✅ **代码兼容层**：全部5处已通过文件系统验证确认存在
