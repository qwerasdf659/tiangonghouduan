# 🔧 项目彻底重构方案 - 双轨清理与技术债务消除

> **创建时间**：2026年01月21日  
> **最后更新**：2026年01月21日 23:00（北京时间）  
> **文档版本**：V5.0（全部12项决策已确认完成）  
> **目标**：在保证业务功能不变的前提下，彻底移除迁移残留、统一技术方案、降低长期维护成本  
> **验证方式**：Node.js 连接真实数据库（非备份文件）+ 代码静态分析 + grep 深度代码扫描  
> **项目健康度**：✅ **优秀** - 数据库层和服务架构已高度统一，仅剩注释层残留

---

## ⚡ 待决策事项快速汇总（请优先阅读）

### 📋 12项决策一览表

| # | 决策项 | 推荐 | 风险 | 您的决定 |
|---|--------|------|------|---------|
| **1** | 审计日志映射代码 `TARGET_TYPE_LEGACY_MAPPING` | ✅ **A 删除** | 🟢 零风险 | ✅ 已确认 |
| **2** | 前端 `name` 字段兼容 | ✅ **A 删除** | 🟢 低风险 | ✅ 已确认 |
| **3** | `rarity` fallback 逻辑 | ✅ **A 简化** | 🟢 低风险 | ✅ 已确认 |
| **4** | `empty→fallback` 分离统计 | ✅ **B 分离** | 🟡 低风险 | ✅ 已确认 |
| **5** | 服务合并策略（3个服务） | ✅ **A 执行合并** | 🟡 中等 | ✅ 已确认 |
| **6** | 备份保留策略（20个目录） | ✅ **A 保留2个** | 🟢 零风险 | ____ |
| **7** | 测试脚本处理（15个脚本） | ✅ **A 删9移6** | 🟢 零风险 | ✅ 已确认 |
| **8** | 根目录备份文件（3个文件） | ✅ **B 全保留** | 🟢 零风险 | ✅ 已确认 |
| **9** | 空表处理策略 | ✅ **B 保留** | 🟢 零风险 | ✅ 已确认 |
| **10** | `products` 表处理（52条数据） | ✅ **B 保留** | 🟢 零风险 | ✅ 已确认 |
| **11** | `@deprecated` 方法（3处） | ✅ **A 立即删除** | 🟢 低风险 | ✅ 已确认 |
| **12** | 迁移文件归档（172个文件） | ✅ **C 删除清理类** | 🟢 零风险 | ✅ 已确认 |

### 🎉 所有决策已完成确认

1. ~~**决策2**：前端是否已统一使用 `campaign_name`？~~ ✅ 已确认删除 `name` 字段
2. ~~**决策5**：是否执行3项服务合并？~~ ✅ 已确认执行合并
3. ~~**决策10**：`products` 表52条数据是否有业务用途？~~ ✅ 已确认保留（"双空间商品"功能）

**✅ 全部12项决策已确认完成，可进入执行阶段**

### 🚀 快速确认模板

如果您同意所有推荐选项，可直接复制并修改：

```
决策1: ✅ A（删除映射代码）- 已确认
决策2: ✅ A（删除name字段）- 已确认
决策3: ✅ A（简化rarity逻辑）- 已确认
决策4: ✅ B（分离统计）- 已确认
决策5: ✅ A（执行合并）- 已确认
决策6: ✅ 已确认（删除15个/保留5个）
决策7: ✅ A（删9移6）- 已确认
决策8: ✅ B（保留根目录备份）- 已确认
决策9: ✅ B（保留空表）- 已确认
决策10: ✅ B（保留products表）- 已确认
决策11: ✅ A（删除deprecated方法）- 已确认
决策12: ✅ C（删除21个清理类迁移文件）- 已确认
```

### 🎯 预期收益

| 维度 | 预期效果 |
|------|---------|
| **代码可读性** | 删除约 100-200 行无用代码和注释 |
| **维护成本** | 消除历史包袱，新人上手更快 |
| **磁盘空间** | 清理备份后释放约 500MB-1GB |
| **技术债务** | 降低长期维护复杂度 |

---

## 〇、当前项目状态总结

### 📊 技术栈概览

| 技术领域 | 当前方案 | 版本/状态 |
|---------|---------|----------|
| **后端运行时** | Node.js | 20+ (>=20.18.0) |
| **Web框架** | Express | 4.x |
| **数据库** | MySQL + Sequelize ORM | 172个迁移文件（数据库记录279条） |
| **缓存** | Redis（分布式锁 + L2缓存） | ioredis |
| **认证** | JWT | jsonwebtoken |
| **授权** | RBAC（10个角色定义） | 自研 |
| **API架构** | V4 RESTful 扁平化路由 | `/api/v4/{resource}` |
| **时区** | 北京时间全链路 | Asia/Shanghai |
| **部署** | Docker + Kubernetes + Sealos | 云原生 |

### 📈 数据库真实数据统计（2026-01-21 实测）

以下为通过 Node.js 连接真实数据库获取的各表记录数：

**数据库表总数：67张表**

#### 核心业务数据表

| 表名 | 记录数 | 说明 | 状态 |
|------|--------|------|------|
| `users` | 28 | 用户主表 | ✅ 正常运营 |
| `roles` | 10 | RBAC角色定义 | ✅ 正常 |
| `user_roles` | 20 | 用户角色关联 | ✅ 正常 |
| `accounts` | 23 | 统一账户 | ✅ 正常 |
| `account_asset_balances` | 23 | 账户资产余额 | ✅ 正常 |
| `asset_transactions` | 5,075 | 资产流水 | ✅ 活跃运营 |
| `item_templates` | 16 | 物品模板 | ✅ 正常 |
| `item_instances` | 1,992 | 物品实例 | ✅ 活跃运营 |
| `item_instance_events` | 1,031 | 物品事件日志 | ✅ 正常 |

#### 抽奖系统表

| 表名 | 记录数 | 说明 | 状态 |
|------|--------|------|------|
| `lottery_campaigns` | 1 | 抽奖活动 | ✅ 正常 |
| `lottery_prizes` | 16 | 奖品定义 | ✅ 正常 |
| `lottery_draws` | 2 | 抽奖记录 | ✅ 待运营 |
| `lottery_presets` | 2 | 抽奖预设 | ✅ 正常 |
| `lottery_management_settings` | 2,338 | 抽奖管理配置 | ✅ 活跃配置 |
| `lottery_strategy_config` | 17 | 策略配置 | ✅ 正常 |
| `lottery_tier_rules` | 9 | 档位规则 | ✅ 正常 |
| `lottery_tier_matrix_config` | 12 | 档位矩阵配置 | ✅ 正常 |
| `lottery_campaign_pricing_config` | 4 | 定价配置 | ✅ 正常 |
| `lottery_draw_quota_rules` | 5 | 抽奖配额规则 | ✅ 正常 |
| `lottery_user_daily_draw_quota` | 7 | 用户每日配额 | ✅ 正常 |
| `lottery_clear_setting_records` | 492 | 清空设置记录 | ✅ 正常 |

#### 商城与交易表

| 表名 | 记录数 | 说明 | 状态 |
|------|--------|------|------|
| `exchange_items` | 26 | 兑换商品 | ✅ 正常 |
| `exchange_records` | 7 | 兑换记录 | ✅ 待运营 |
| `products` | 52 | 商品信息 | ✅ 正常 |
| `market_listings` | 38 | 市场挂牌 | ✅ 活跃运营 |
| `trade_orders` | 0 | 交易订单 | ⏳ 待使用 |
| `redemption_orders` | 932 | 核销订单 | ✅ 活跃运营 |

#### 门店与层级表

| 表名 | 记录数 | 说明 | 状态 |
|------|--------|------|------|
| `stores` | 4 | 门店 | ✅ 正常 |
| `store_staff` | 2 | 门店员工 | ✅ 正常 |
| `user_hierarchy` | 9 | 用户层级关系 | ✅ 正常 |
| `administrative_regions` | 44,703 | 行政区域（中国） | ✅ 基础数据 |

#### 配置与系统表

| 表名 | 记录数 | 说明 | 状态 |
|------|--------|------|------|
| `system_settings` | 39 | 系统配置 | ✅ 正常 |
| `feature_flags` | 7 | 功能开关 | ✅ 正常 |
| `popup_banners` | 2 | 弹窗横幅 | ✅ 正常 |
| `system_announcements` | 9 | 系统公告 | ✅ 正常 |
| `material_asset_types` | 4 | 材料资产类型 | ✅ 正常 |
| `material_conversion_rules` | 1 | 材料转换规则 | ✅ 待配置 |
| `rarity_defs` | 5 | 稀有度定义 | ✅ 正常 |
| `category_defs` | 6 | 分类定义 | ✅ 正常 |
| `asset_group_defs` | 8 | 资产组定义 | ✅ 正常 |

#### 审计与日志表

| 表名 | 记录数 | 说明 | 状态 |
|------|--------|------|------|
| `admin_operation_logs` | 3,977 | 管理员操作日志 | ✅ 活跃记录 |
| `merchant_operation_logs` | 56 | 商户操作日志 | ✅ 正常 |
| `user_role_change_records` | 175 | 角色变更记录 | ✅ 正常 |
| `user_status_change_records` | 174 | 状态变更记录 | ✅ 正常 |
| `content_review_records` | 266 | 内容审核记录 | ✅ 正常 |
| `api_idempotency_requests` | 1,222 | API幂等请求 | ✅ 正常 |
| `websocket_startup_logs` | 1,009 | WebSocket启动日志 | ✅ 正常 |

#### 其他功能表

| 表名 | 记录数 | 说明 | 状态 |
|------|--------|------|------|
| `consumption_records` | 10 | 消费记录 | ✅ 待运营 |
| `user_premium_status` | 1 | 高级会员状态 | ✅ 待运营 |
| `feedbacks` | 26 | 用户反馈 | ✅ 正常 |
| `chat_messages` | 5 | 聊天消息 | ✅ 待运营 |
| `customer_service_sessions` | 1 | 客服会话 | ✅ 待运营 |
| `user_risk_profiles` | 3 | 用户风险画像 | ✅ 正常 |
| `risk_alerts` | 1 | 风险告警 | ✅ 正常 |
| `sequelizemeta` | 279 | 迁移记录 | ✅ 系统表 |

#### 暂未使用/待清理表

| 表名 | 记录数 | 说明 | 状态 |
|------|--------|------|------|
| `authentication_sessions` | 0 | 认证会话 | ⏳ 待使用或清理 |
| `image_resources` | 0 | 图片资源 | ⏳ 待使用或清理 |
| `lottery_daily_metrics` | 0 | 每日指标聚合 | ⏳ 待使用 |
| `lottery_hourly_metrics` | 0 | 每小时指标 | ⏳ 待使用 |
| `lottery_draw_decisions` | 0 | 抽奖决策记录 | ⏳ 待使用 |
| `lottery_campaign_quota_grants` | 0 | 配额授予 | ⏳ 待使用 |
| `lottery_campaign_user_quota` | 0 | 用户活动配额 | ⏳ 待使用 |
| `lottery_user_experience_state` | 0 | 用户体验状态 | ⏳ 待使用 |
| `lottery_user_global_state` | 0 | 用户全局状态 | ⏳ 待使用 |
| `preset_budget_debt` | 0 | 预设预算债务 | ⏳ 待使用 |
| `preset_inventory_debt` | 0 | 预设库存债务 | ⏳ 待使用 |

**数据分析结论**：
- 核心业务数据活跃：用户28个、物品实例近2000个、资产流水5000+条、核销订单932条
- 市场交易功能已启用：市场挂牌38条
- 抽奖系统配置完善：2338条管理配置、17条策略配置
- 审计日志完整：近4000条管理员操作日志
- 数据量适合一次性重构，无大规模数据迁移风险

---

### 🏗️ 服务层架构统计

**服务文件统计**（`services/` 目录）：

| 服务类别 | 服务文件 | 说明 |
|---------|---------|------|
| **抽奖引擎** | `UnifiedLotteryEngine/` | V4.6 Pipeline 架构，含 6 个 Stage |
| **资产底座** | `AssetService.js`, `AssetConversionService.js` | 统一资产账本系统 |
| **背包服务** | `BackpackService.js` | 双轨查询聚合（assets + items） |
| **市场服务** | `MarketListingService.js`, `TradeOrderService.js` | 市场挂牌与交易 |
| **核销服务** | `RedemptionService.js` | 核销码生成与验证 |
| **会员服务** | `PremiumService.js`, `UserPremiumQueryService.js` | 高级会员功能 |
| **兑换服务** | `ExchangeService.js` | 积分兑换商品 |
| **定价服务** | `LotteryPricingService.js` | 抽奖定价计算（单一真相源） |
| **配额服务** | `LotteryQuotaService.js` | 抽奖次数配额管理 |
| **监控服务** | `LotteryMonitoringService.js`, `LotteryStrategyStatsService.js` | 抽奖监控统计 |
| **审计服务** | `AuditLogService.js` | 统一审计日志 |
| **幂等服务** | `IdempotencyService.js` | API 幂等性保障 |
| **缓存服务** | `CacheService.js` | Redis L2 缓存封装 |
| **WebSocket** | `ChatWebSocketService.js` | 聊天实时通讯 |

**模型统计**（`models/index.js` 导出 67 个模型）：

| 领域 | 模型数量 | 核心模型 |
|------|---------|---------|
| 用户与权限 | 8 | `User`, `Role`, `UserRole`, `AuthenticationSession` |
| 抽奖系统 | 15 | `LotteryCampaign`, `LotteryPrize`, `LotteryDraw`, `LotteryPreset` 等 |
| 物品系统 | 6 | `ItemTemplate`, `ItemInstance`, `ItemInstanceEvent` |
| 资产系统 | 5 | `Account`, `AccountAssetBalance`, `AssetTransaction` |
| 市场交易 | 3 | `MarketListing`, `TradeOrder`, `RedemptionOrder` |
| 门店层级 | 4 | `Store`, `UserHierarchy`, `StoreStaff`, `AdministrativeRegion` |
| 系统配置 | 8 | `SystemSettings`, `FeatureFlag`, `PopupBanner` 等 |
| 审计日志 | 6 | `AdminOperationLog`, `MerchantOperationLog` 等 |

### 🔌 API 路由架构

**V4 RESTful 路由结构**（`routes/v4/`）：

```
/api/v4/
├── auth/           # 认证相关（登录、刷新、登出）
├── permissions/    # 权限查询
├── console/        # 控制台（管理后台专用）
├── lottery/        # 抽奖系统
├── market/         # 市场交易
├── shop/           # 商城（兑换商品）
├── system/         # 系统配置
├── user/           # 用户管理
├── assets/         # 资产管理
├── backpack/       # 背包查询
├── merchant-points/ # 商户积分
├── activities/     # 活动列表
└── debug-control/  # 调试控制（开发环境）
```

### ✅ 已完成的清理工作（279个迁移记录已执行）

基于对 279 个迁移文件的深度分析，以下清理工作已完成：

| 清理项 | 迁移文件 | 状态 | 替代方案 |
|--------|---------|------|---------|
| `user_points_accounts` 表删除 | `20251230200000-drop-deprecated-points-tables.js` | ✅ 已执行 | `account_asset_balances` |
| `points_transactions` 表删除 | `20251230200000-drop-deprecated-points-tables.js` | ✅ 已执行 | `asset_transactions` |
| `trade_records` 表删除 | `20260108210000-drop-trade-records-table.js` | ✅ 已执行 | `trade_orders` |
| `item_template_aliases` 表删除 | `20260114001257-drop-item-template-aliases-cleanup.js` | ✅ 已执行 | `item_templates` |
| 废弃图片字段删除 | `20260114000000-drop-column-deprecated-image-fields.js` | ✅ 已执行 | `image_resources` + `primary_image_id` |
| 旧审计表删除 | `20260109120000-drop-deprecated-audit-tables.js` | ✅ 已执行 | `admin_operation_logs` |
| `lottery_campaigns.draw_pricing` JSON清理 | `20260120193900-cleanup-legacy-draw-pricing-json.js` | ✅ 已执行 | `lottery_campaign_pricing_config` |
| 孤儿数据清理 | `20260121060000-cleanup-orphan-records.js` | ✅ 已执行 | 硬删除 |

### ✅ 已高度统一的架构（无需进一步合并）

| 领域 | 统一方案 | 状态 |
|------|---------|------|
| **幂等性服务** | `IdempotencyService.js` - 显式 canonical_operation 映射 | ✅ 已统一 |
| **定价服务** | `LotteryPricingService.js` - 单一定价真相源 | ✅ 已统一 |
| **资产底座** | `AssetService` + `AccountAssetBalance` + `AssetTransaction` | ✅ 已统一 |
| **背包查询** | `BackpackService.js` - 统一查询聚合服务 | ✅ 已统一 |
| **抽奖引擎** | `UnifiedLotteryEngine` V4.6 Pipeline 架构 | ✅ 已统一 |

---

### 🔍 数据库验证结果（2026-01-21 最新验证）

通过 Node.js 连接真实数据库进行验证，确认以下清理状态：

#### 旧表删除验证（全部通过）

| 表名 | 验证结果 | 替代方案 |
|------|---------|---------|
| `user_points_accounts` | ✅ 已删除 | `account_asset_balances` |
| `points_transactions` | ✅ 已删除 | `asset_transactions` |
| `trade_records` | ✅ 已删除 | `trade_orders` |
| `audit_records` | ✅ 已删除 | `admin_operation_logs` |
| `user_inventory` / `user_inventories` | ✅ 已删除 | `item_instances` |
| `lottery_histories` / `prize_records` | ✅ 已删除 | `lottery_draws` |
| `merchant_points_reviews` | ✅ 已删除 | `content_review_records` |
| `role_change_logs` | ✅ 已删除 | `user_role_change_records` |
| `item_template_aliases` | ✅ 已删除 | `item_templates` |
| `points_logs` | ✅ 已删除 | `asset_transactions` |

#### 字段清理验证（全部通过）

| 字段 | 验证结果 | 说明 |
|------|---------|------|
| `lottery_campaigns.draw_pricing` | ✅ 已删除 | 迁移到 `lottery_campaign_pricing_config` 表 |
| `asset_transactions.user_id` | ✅ 已删除 | 完全迁移到 `account_id` 体系 |

#### 🎉 审计日志 target_type 验证（重要发现）

**验证结果**：✅ **数据库中所有 `target_type` 已经是 snake_case 标准格式**

```
📊 admin_operation_logs target_type 分布（真实数据）:
   ✅ 标准格式: lottery_management_setting = 1329条
   ✅ 标准格式: user = 814条
   ✅ 标准格式: account_asset_balance = 498条
   ✅ 标准格式: lottery_clear_setting_record = 492条
   ✅ 标准格式: customer_service_session = 349条
   ✅ 标准格式: user_role_change_record = 175条
   ✅ 标准格式: user_status_change_record = 174条
   ✅ 标准格式: consumption_record = 81条
   ✅ 标准格式: asset_transaction = 34条
   ✅ 标准格式: feature_flag = 28条
   ✅ 标准格式: exchange_record = 2条
   ✅ 标准格式: item_instance = 1条
```

**结论**：
- 无 PascalCase 格式的旧 `target_type`（如 `UserPointsAccount`、`ItemInstance` 等）
- `TARGET_TYPE_LEGACY_MAPPING` 映射表实际上已无用武之地
- **可以直接删除映射代码，无需执行数据迁移脚本**

---

## 一、双轨架构边界划分

### ✅ 业务设计双轨（必须保留）

| 双轨名称 | 位置 | 设计理由 | 保留依据 |
|---------|------|---------|---------|
| **背包双轨架构** | `services/BackpackService.js` | assets（可叠加资产）+ items（不可叠加物品）是两种本质不同的资产类型 | 业务模型决定，合并会导致逻辑混乱 |
| **市场挂牌双轨** | `services/MarketListingService.js` + `models/MarketListing.js` | `listing_kind='item_instance'`（不可叠加物品）vs `listing_kind='fungible_asset'`（可叠加资产） | 交易标的本质不同：物品实例需锁定、资产需冻结余额 |
| **监控查询双轨** | `services/LotteryStrategyStatsService.js` | 24h内查明细表，历史查聚合表 | 性能优化设计，非迁移残留 |

**说明**：
- **背包双轨**是业务架构的核心设计，`assets[]` 存储可叠加的材料/积分，`items[]` 存储不可叠加的物品实例
- **市场挂牌双轨**是交易业务的核心设计：
  - `item_instance`：挂牌物品实例（NFT类），挂牌时锁定物品状态，成交后所有权转移
  - `fungible_asset`：挂牌可叠加资产（如材料碎片），挂牌时冻结卖家余额，成交后划转资产
- **监控查询双轨**是性能优化手段，近期数据查明细表保证实时性，历史数据查聚合表保证性能

---

### ❌ 迁移/兼容残留（必须清理）

| 残留类型 | 位置 | 具体内容 | 清理动作 |
|---------|------|---------|---------|
| **旧模型名映射** | `constants/AuditTargetTypes.js` | `UserPointsAccount → account_asset_balance`<br>`UserInventory → item_instance`<br>`PointsTransaction → asset_transaction`（共47个映射） | 删除 `TARGET_TYPE_LEGACY_MAPPING` 中所有旧模型名映射 |
| **废弃注释残留** | 多个服务文件 | `@deprecated`、`已迁移`、`旧链路`等注释 | 删除所有描述迁移历史的注释块 |
| **字段兼容注释** | `services/PrizePoolService.js:139` | `兼容字段：prize_value_points / value_points / budget_cost_points` | 确认数据已统一后删除注释 |
| **前端兼容字段** | `services/ActivityService.js:95` | `name: campaign.campaign_name // 兼容前端期望的 name 字段` | ✅ 已确认删除（决策2） |
| **rarity fallback** | `services/MarketListingService.js:1032` | `rarity: listing.offer_item_rarity \|\| listing.offerItem?.meta?.rarity \|\| 'common'` | ✅ 已确认删除 fallback |

---

## 二、代码层面清理清单

### 2.1 @deprecated 标记清理

#### UnifiedLotteryEngine.js
```
文件: services/UnifiedLotteryEngine/UnifiedLotteryEngine.js
行号: 937-941

问题: getDrawPricing() 方法已迁移到 LotteryPricingService
状态: 标记为 @deprecated 2026-01-21
动作: 彻底删除 getDrawPricing 方法（不保留桩方法）
```

#### PricingStage.js
```
文件: services/UnifiedLotteryEngine/pipeline/stages/PricingStage.js
行号: 203-209

问题: "旧方法已迁移至 LotteryPricingService" 注释块
动作: 删除整个注释块
```

#### EligibilityStage.js
```
文件: services/UnifiedLotteryEngine/pipeline/stages/EligibilityStage.js
行号: 250-257

问题: _getDailyDrawsCount() 方法已废弃，移到 LotteryQuotaService
动作: 删除废弃方法声明和注释
```

---

### 2.2 历史迁移注释清理

需要清理的迁移历史注释模式：
- `旧链路`、`原策略`、`已迁移`、`从XXX迁移`
- `Phase X 迁移`、`2026-01-XX 迁移`
- `@see docs/XXX迁移方案.md`（但保留架构设计文档引用）

**涉及文件清单**：

| 文件 | 行号 | 注释内容 |
|------|------|---------|
| `services/index.js` | 7-17 | V4架构移除向后兼容代码说明 |
| `services/index.js` | 107-113 | V4.6管线编排器迁移说明 |
| `services/UnifiedLotteryEngine/UnifiedLotteryEngine.js` | 6-8 | 策略模式已废弃说明 |
| `services/UnifiedLotteryEngine/UnifiedLotteryEngine.js` | 142-146 | V4.6管线编排器迁移说明 |
| `services/UnifiedLotteryEngine/UnifiedLotteryEngine.js` | 234-235 | 旧实现Strategy链式执行说明 |
| `services/UnifiedLotteryEngine/UnifiedLotteryEngine.js` | 369 | 已删除字段strategy_used说明 |
| `services/UnifiedLotteryEngine/UnifiedLotteryEngine.js` | 632 | 已删除enabled_strategies兼容字段说明 |
| `services/IdempotencyService.js` | 78 | 路径双轨清理决策说明 |
| `services/UserRoleService.js` | 616-881 | 从UserPermissionModule迁移注释（6处） |
| `services/lottery/LotteryPricingService.js` | 23-27 | 技术债务说明 |
| `services/ExchangeService.js` | 60 | 暴力移除旧方案说明 |
| `services/ExchangeService.js` | 1110, 1255 | 已删除兼容逻辑说明 |
| `services/PopupBannerService.js` | 604 | 已删除URL兼容逻辑说明 |

---

### 2.3 兼容代码清理

| 文件 | 行号 | 内容 | 决策需求 |
|------|-----|------|---------|
| `services/IdempotencyService.js` | 428 | 尾斜杠兼容匹配逻辑 | 确认所有客户端已统一后删除 |
| `services/MarketListingService.js` | 1032 | rarity字段fallback到meta | 确认数据已统一后删除 |
| `services/LotteryMetricsCollector.js` | 90 | empty归入fallback_tier_count | ✅ 已确认分离统计（新增 empty_count） |
| `services/ActivityService.js` | 95 | name字段兼容前端 | 确认前端是否已统一使用campaign_name |

---

## 三、数据库层面清理清单

### 3.1 已确认删除的表（迁移已执行）

这些表已通过迁移删除，需确认代码中无引用残留：

| 表名 | 删除迁移 | 替代方案 |
|------|---------|---------|
| `user_points_accounts` | 20251230200000 | `account_asset_balances` |
| `points_transactions` | 20251230200000 | `asset_transactions` |
| `trade_records` | 20260108210000 | `trade_orders` |
| `audit_records` | 20260109124000 | `admin_operation_logs` |
| `merchant_points_reviews` | 20260109120000 | `content_review_records` |
| `role_change_logs` | 20260109120000 | `user_role_change_records` |
| `item_template_aliases` | 20260114001257 | `item_templates` |

**验证命令**：
```bash
# 搜索是否有代码引用已删除的表
grep -r "user_points_accounts\|points_transactions\|trade_records\|audit_records\|merchant_points_reviews\|role_change_logs\|item_template_aliases" --include="*.js" services/ routes/ models/
```

---

### 3.2 审计日志 target_type 规范化

**现状**：
- `constants/AuditTargetTypes.js` 中保留了 `TARGET_TYPE_LEGACY_MAPPING` 映射表（共47个映射条目）
- 映射旧模型名（PascalCase）到新标准码（snake_case）
- 历史审计记录可能包含旧格式的 target_type

**清理方案**：

**步骤1：执行数据迁移**
```sql
-- 规范化历史审计记录的 target_type
UPDATE admin_operation_logs 
SET target_type = 'account_asset_balance' 
WHERE target_type IN ('UserPointsAccount', 'AccountAssetBalance');

UPDATE admin_operation_logs 
SET target_type = 'asset_transaction' 
WHERE target_type IN ('PointsTransaction', 'AssetTransaction');

UPDATE admin_operation_logs 
SET target_type = 'item_instance' 
WHERE target_type IN ('UserInventory', 'ItemInstance');

-- 其他映射关系同理...
```

**步骤2：删除代码中的映射**
```javascript
// 删除 constants/AuditTargetTypes.js 中的 TARGET_TYPE_LEGACY_MAPPING
// 删除 normalizeTargetType() 函数中的映射逻辑
```

---

### 3.3 需要确认的数据字段

| 表名 | 字段 | 问题 | 建议 |
|------|------|------|------|
| `lottery_prizes` | `prize_value_points` / `budget_cost_points` | 存在两个语义相近的字段 | 确认后统一为一个 |
| `lottery_campaigns` | `draw_pricing` (JSON) | 已迁移到 `lottery_campaign_pricing_config` 表 | 验证数据完整后删除 |

---

### 3.4 空表清理建议

以下表记录数为0，需确认是否保留：

| 表名 | 状态 | 建议 |
|------|------|------|
| `authentication_sessions` | 0条 | 如不使用可删除模型和表 |
| `image_resources` | 0条 | 如不使用可删除模型和表 |
| `lottery_daily_metrics` | 0条 | 保留（监控统计功能待启用） |
| `lottery_hourly_metrics` | 0条 | 保留（监控统计功能待启用） |
| `lottery_draw_decisions` | 0条 | 保留（决策记录功能待启用） |
| `preset_budget_debt` | 0条 | 保留（预设债务功能） |
| `preset_inventory_debt` | 0条 | 保留（预设债务功能） |
| `trade_orders` | 0条 | 保留（交易功能待上线） |

---

## 四、文件层面清理清单

### 4.1 根目录备份文件删除

**2026-01-21 实测确认存在的备份文件**：

```bash
# 根目录备份文件（建议删除）
/home/devbox/project/app.js.backup                    # 27KB, 2025-12-25
/home/devbox/project/app.js.backup.20260108_125922    # 35KB, 2026-01-08
/home/devbox/project/ecosystem.config.js.backup       # 1.8KB, 2025-12-25
```

> 注：`.env.backup.20251225` 未在根目录找到（可能已被清理或在其他位置）

### 4.2 backups 目录历史备份清理

```bash
/home/devbox/project/backups/
├── backup_2025-10-14/           # ✅ 已确认删除
├── backup_2025-10-14_latest/    # ✅ 已确认删除
├── backup_2025-10-25/           # ✅ 已确认删除
├── backup_2025-10-29/           # ✅ 已确认删除
├── backup_2025-11-04/           # ✅ 已确认删除
├── backup_2025-11-08/           # ✅ 已确认删除
├── backup_2025-11-11/           # ✅ 已确认删除
├── backup_2025-12-19/           # ✅ 已确认删除
├── backup_2025-12-23/           # ✅ 已确认删除
├── backup_2025-12-24/           # ✅ 已确认删除
├── backup_2025-12-27/           # ✅ 已确认删除
├── backup_2025-12-31/           # ✅ 已确认删除
├── backup_2025-12-31_complete/  # ✅ 已确认删除
├── backup_2026-01-02/           # ✅ 已确认删除
├── backup_2026-01-02_complete/  # ✅ 已确认删除
├── backup_2026-01-04_complete/  # 🔒 保留
├── backup_2026-01-08_complete/  # 🔒 保留
├── backup_2026-01-13_complete/  # 🔒 保留
├── backup_2026-01-18_complete/  # 🔒 保留
└── backup_2026-01-20_complete/  # 🔒 保留
```

**已确认策略**：删除所有2025年备份和2026-01-02备份（共15个），保留2026-01-04及之后的完整备份（共5个）

---

### 4.3 根目录测试脚本整理

根目录下存在 **15个临时测试脚本**，经数据库实际验证和代码对比分析：

#### 🗑️ **建议删除（9个）** - 功能重复或已过时

| 脚本 | 删除原因 | scripts目录已有替代 |
|------|----------|---------------------|
| `test-exchange-orders-api.js` | 与scripts下3个兑换测试脚本功能重复 | `scripts/test_exchange_market_api.js` |
| `test-db-orders.js` | 数据库直连测试，功能被替代 | `scripts/test_exchange_service_direct.js` |
| `test-listing-stats-api.js` | 兑换统计测试，功能重复 | `scripts/test_exchange_market_admin_api.js` |
| `test-marketplace-data.js` | 市场数据查询测试，功能重复 | `scripts/maintenance/truncate_market_listings.js` |
| `test-marketplace-stats.js` | 市场统计API测试，功能重复 | `scripts/test_exchange_market_admin_api.js` |
| `test-frontend-api-paths.js` | API路径检查，功能重复 | `scripts/check_api_consistency.js` |
| `seed-test-exchange-orders.js` | 测试数据脚本，数据库已有940条正式数据 | - |
| `test-presets-api.js` | ⚠️ 引用表名`lottery_budget_presets`已不存在（实际为`lottery_presets`） | - |
| `test-create-listing-data.js` | 测试数据脚本，`market_listings`已有38条正式数据 | - |

#### 📁 **建议移动到scripts（6个）** - 有独立价值

| 脚本 | 用途 | 目标目录 |
|------|------|----------|
| `test-api-full.js` | 完整API联动测试 | `scripts/integration/` |
| `test-config-tools-api.js` | 配置工具API测试 | `scripts/admin_tools/` |
| `test-config-tools-complete.js` | 配置工具完整测试 | `scripts/admin_tools/` |
| `test-user-management-api.js` | 用户管理API测试 | `scripts/admin_tools/` |
| `test_budget_ratio.js` | 预算系数动态配置测试（依赖ConsumptionService，功能活跃） | `scripts/` |
| `test-material-apis.js` | 材料系统API测试（`material_asset_types`有4条数据） | `scripts/` |

#### 📊 数据库验证结果（2026-01-21）

```
✅ exchange_items: 26条数据 - 兑换商品功能活跃
✅ redemption_orders: 940条数据 - 兑换订单功能活跃
✅ market_listings: 38条数据 - 市场挂牌功能活跃
✅ material_asset_types: 4条数据 - 材料系统功能活跃
✅ users: 28条数据 - 用户管理功能活跃
❌ lottery_budget_presets: 表不存在（实际为lottery_presets）
```

**✅ 已确认**：执行方案A（删除9个、移动6个）

---
 
---

## 五、服务合并收缩建议

### 5.1 可合并的服务（降低维护成本）

| 当前服务 | 建议合并到 | 理由 | 预估工作量 |
|---------|-----------|------|-----------|
| `LotteryMonitoringService` + `LotteryStrategyStatsService` | 合并为 `LotteryAnalyticsService` | 都是抽奖监控统计，职责相近 | 中等 |
| `UserPremiumQueryService` | 合并到 `PremiumService` | 查询服务可作为主服务的方法 | 较小 |
| `TradeOrderQueryService` | 合并到 `TradeOrderService` | 查询服务可作为主服务的方法 | 较小 |

---

### 5.2 已经合理分离的服务（保持现状）

| 服务组合 | 保持分离理由 |
|---------|-------------|
| `AssetService` + `AssetConversionService` | 写操作与转换操作分离，职责清晰 |
| `MarketListingService` + `TradeOrderService` | 挂牌与订单是不同的业务实体 |
| `BackpackService` | 查询聚合服务，职责单一 |
| `RedemptionService` + `BackpackService` | 核销与查询分离，合理 |

---

## 六、需要拍板决定的事项

### 🔴 P0 - 立即决策（影响后续执行）

#### 决策1：审计日志 `target_type` 映射代码处理

**背景**：`constants/AuditTargetTypes.js` 中保留了 `TARGET_TYPE_LEGACY_MAPPING`（47个映射条目），用于将旧模型名（如 `UserPointsAccount`）映射到新标准码（如 `account_asset_balance`）。

**🎉 2026-01-21 验证结果**（最新实测）：
```
📊 admin_operation_logs 数据分布（共 4,029 条）:
   ✅ lottery_management_setting: 1,345条
   ✅ user: 822条
   ✅ account_asset_balance: 498条
   ✅ lottery_clear_setting_record: 498条
   ✅ customer_service_session: 349条
   ✅ user_role_change_record: 177条
   ✅ user_status_change_record: 176条
   ✅ consumption_record: 81条
   ✅ feature_flag: 46条
   ✅ asset_transaction: 34条
   ✅ exchange_record: 2条
   ✅ item_instance: 1条
   
   ❌ PascalCase 格式: 0条（已通过 Node.js 连接真实数据库验证）
```

**结论**：**所有 `target_type` 已经是 snake_case 标准格式，无需数据迁移**

**选项**：
- **A. 直接删除映射代码**（✅ 强烈推荐，零风险）
  - 删除 `TARGET_TYPE_LEGACY_MAPPING` 常量（约50行代码）
  - 简化 `normalizeTargetType()` 函数逻辑
  
- **B. 保留映射表**
  - 风险：永久维护无用代码

**您的决定**：✅ **A - 已确认删除**

---

#### 决策2：前端兼容字段 `name` 的处理

**位置**：`services/ActivityService.js:95`
```javascript
name: campaign.campaign_name, // 兼容前端期望的 name 字段
```

**🎉 2026-01-21 前端代码分析结果**：
- 仅 `campaigns.html` 第277、352行使用 `.name`（需修改）
- 5处使用 `name || campaign_name` 兼容写法（需清理）
- 9个文件已统一使用 `.campaign_name`（新代码）

**执行清单**：
1. 修改 `public/admin/campaigns.html` 第277、352行：`campaign.name` → `campaign.campaign_name`
2. 清理5处兼容写法：`lottery-quota.js:166`、`lottery-metrics.html:289`、`pricing-config.html:222`、`tier-matrix.html:211`、`asset-adjustment.js:99`
3. 删除 `services/ActivityService.js:95` 的 `name` 字段

**您的决定**：✅ **A - 已确认删除 `name` 字段，统一使用 `campaign_name`**

---

#### 决策3：`rarity` 字段 fallback 逻辑处理

**位置**：`services/MarketListingService.js:1032`
```javascript
rarity: listing.offer_item_rarity || listing.offerItem?.meta?.rarity || 'common'
```

**🎉 2026-01-21 验证发现**：
- `market_listings` 表中 **没有** `offer_item_meta` 字段
- 只有 `offer_item_rarity` 快照字段
- fallback 到 `offerItem?.meta?.rarity` 是通过关联查询 `item_instances.meta`
- 当前 `market_listings` 表有 **38条记录**

**选项**：
- **A. 简化逻辑，仅使用 `offer_item_rarity || 'common'`**（✅ 推荐）
  - 删除 `listing.offerItem?.meta?.rarity` fallback
  - 理由：快照字段应是唯一真相源
- **B. 保留完整 fallback 链**
  - 理由：防御性编程

**您的决定**：✅ **A - 已确认删除 fallback 逻辑**

**执行清单**：
1. 修改 `services/MarketListingService.js:1032`：
   - 原代码：`rarity: listing.offer_item_rarity || listing.offerItem?.meta?.rarity || 'common'`
   - 新代码：`rarity: listing.offer_item_rarity || 'common'`

**📋 关联业务决策（2026-01-21 确认）**：
- ✅ **稀有度系统保留使用**：`rarity_defs`（5个等级）+ `item_templates`（16个模板）继续使用
- ✅ **后续物品创建规范**：创建物品实例时必须关联 `item_template_id`，稀有度从模板继承
- ⚠️ **当前数据状态**：2010个历史物品实例未关联模板（`item_template_id = NULL`），属于遗留数据
- 📝 **建议**：在物品创建服务中增加 `item_template_id` 必填校验（或至少记录警告日志）

---

#### 决策4：`LotteryMetricsCollector` 的 `empty → fallback` 分离统计

**位置**：`services/LotteryMetricsCollector.js:90`
```javascript
empty: 'fallback_tier_count' // 兼容处理：empty 归入 fallback
```

**问题**：
- `empty`（系统异常空奖）和 `fallback`（正常兜底奖品）被混在一起统计
- 运营无法区分"用户抽到兜底奖"和"系统出问题没奖发"

**选项**：
- **A. 业务设计，保留但修改注释**
- **B. 分离统计，新增 `empty_count` 独立字段**（✅ 已选择）

**您的决定**：✅ **B - 分离统计**

**执行清单**：
1. 修改 `services/LotteryMetricsCollector.js`：
   - 新增 `empty: 'empty_count'` 独立映射
   - `fallback` 保持 `fallback_tier_count`
2. `empty` 触发运营告警（日志级别 WARN）
3. 运营后台增加"异常空奖"监控面板

**📋 业务价值**：
- ✅ 区分"正常兜底"和"系统异常"
- ✅ 便于运营监控和问题排查
- ✅ 为将来扩展（告警系统）预留能力

---

### 🟡 P1 - 短期决策（可在执行过程中决定）

#### 决策5：服务合并策略

现有文档建议的合并：
| 当前服务 | 建议合并到 | 工作量 |
|---------|-----------|-------|
| `LotteryMonitoringService` + `LotteryStrategyStatsService` | `LotteryAnalyticsService` | 中等 |
| `UserPremiumQueryService` | `PremiumService` | 较小 |
| `TradeOrderQueryService` | `TradeOrderService` | 较小 |

**选项**：
- **A. 执行合并，降低长期维护成本**
- **B. 暂不合并，保持现状**

**您的决定**：✅ **A - 已确认执行合并**

**📋 执行清单**：

1. **合并 `LotteryMonitoringService` + `LotteryStrategyStatsService` → `LotteryAnalyticsService`**
   - 创建新服务文件 `services/LotteryAnalyticsService.js`
   - 迁移 `LotteryMonitoringService` 的查询方法（`getHourlyMetrics`, `getExperienceState` 等）
   - 迁移 `LotteryStrategyStatsService` 的统计方法（`getTodayStats`, `getHourlyStats`, `getDailyStats` 等）
   - 保留双轨查询策略（24h内查明细表、历史查聚合表）
   - 删除原 `LotteryMonitoringService.js` 和 `LotteryStrategyStatsService.js`

2. **合并 `UserPremiumQueryService` → `PremiumService`**
   - 将 `getPremiumStatuses()`, `getPremiumStatus()` 方法移入 `PremiumService`
   - 保持方法签名不变，确保调用方无需修改
   - 删除原 `UserPremiumQueryService.js`

3. **合并 `TradeOrderQueryService` → `TradeOrderService`**
   - 将 `getOrders()`, `getOrderById()` 等查询方法移入 `TradeOrderService`
   - 保持方法签名不变，确保调用方无需修改
   - 删除原 `TradeOrderQueryService.js`

**📊 当前数据量（2026-01-21 验证）**：
- `lottery_draws`: 2条
- `lottery_hourly_metrics`: 0条（待运营）
- `lottery_daily_metrics`: 0条（待运营）
- `user_premium_status`: 1条
- `trade_orders`: 0条（待使用）
- `market_listings`: 38条

**⚠️ 注意事项**：
- 更新 `services/index.js` 导出
- 更新所有路由层的 `require` 引用
- 保持原方法签名，降低调用方改动

---

#### 决策6：备份保留策略

当前 `backups/` 目录有 **20个备份目录**。

**✅ 已确认**：
- **删除**：所有2025年备份 + 2026-01-02备份（共15个目录）
- **保留**：2026-01-04及之后的完整备份（共5个目录）

**您的决定**：✅ 已确认保留5个2026年备份

---

#### 决策7：根目录测试脚本处理

当前根目录有 **15个临时测试脚本**，经数据库实际验证（2026-01-21）：

**分析结果**：
- 🗑️ **9个建议删除**：功能重复或引用已过时的表名
- 📁 **6个建议移动**：有独立价值，移动到 `scripts/` 统一管理

**选项**：
- **A. 删除9个 + 移动6个**（推荐，详见§4.3节分析）
- **B. 全部删除**
- **C. 全部移动**
- **D. 保持现状**

**您的决定**：✅ **A - 已确认删9移6**

**📋 执行清单**：

**🗑️ 删除清单（9个）**：
| 脚本 | 删除原因 |
|------|----------|
| `test-exchange-orders-api.js` | `scripts/test_exchange_market_api.js` 已覆盖 |
| `test-db-orders.js` | `scripts/test_exchange_service_direct.js` 已覆盖 |
| `test-listing-stats-api.js` | `scripts/test_exchange_market_admin_api.js` 已覆盖 |
| `test-marketplace-data.js` | 简单数据库查询，无独立价值 |
| `test-marketplace-stats.js` | 与admin API测试脚本重复 |
| `test-frontend-api-paths.js` | `scripts/check_api_consistency.js` 已覆盖 |
| `seed-test-exchange-orders.js` | 数据库已有940条正式数据 |
| `test-create-listing-data.js` | 数据库已有38条正式数据 |
| `test-presets-api.js` | API功能已被 `tests/` 正式测试覆盖 |

**📁 移动清单（6个）**：
| 脚本 | 目标目录 | 原因 |
|------|----------|------|
| `test-api-full.js` | `scripts/integration/` | 完整API联动测试，有独立价值 |
| `test-config-tools-api.js` | `scripts/admin_tools/` | 配置管理测试，运维有用 |
| `test-config-tools-complete.js` | `scripts/admin_tools/` | 配置完整测试，运维有用 |
| `test-user-management-api.js` | `scripts/admin_tools/` | 用户管理测试，运维有用 |
| `test_budget_ratio.js` | `scripts/` | 预算系数测试，功能活跃 |
| `test-material-apis.js` | `scripts/` | 材料系统测试，功能活跃 |

---

#### 决策8：根目录备份文件处理

**2026-01-21 实测确认**，当前根目录有以下备份文件：
- `app.js.backup` (27KB)
- `app.js.backup.20260108_125922` (35KB)
- `ecosystem.config.js.backup` (1.8KB)

**选项**：
- **A. 全部删除**（推荐，已有 backups/ 目录的完整备份）
- **B. 保留部分备份**

**您的决定**：✅ **B - 已确认全部保留**

**📋 保留说明**：
- `app.js.backup` (27KB) - 保留
- `app.js.backup.20260108_125922` (35KB) - 保留
- `ecosystem.config.js.backup` (1.8KB) - 保留

> 注：这些备份文件作为额外的安全保障保留在根目录

---

#### 决策9：空表处理策略

以下表记录数为0：
- `authentication_sessions` - 认证会话表
- `image_resources` - 图片资源表

**选项**：
- **A. 删除不使用的空表及对应模型**
- **B. 保留所有表，等待后续功能启用**（推荐）

**您的决定**：✅ **B - 已确认保留空表**

**📋 保留说明**：
- `authentication_sessions`：预留给未来会话管理功能
- `image_resources`：预留给未来图片资源管理功能
- 其他空表（`lottery_hourly_metrics`、`trade_orders` 等）：功能待启用，保持现状

---

#### 决策10：products 表处理

**现状**（2026-01-21 深度代码验证）：
- `products` 表有 **52条数据**
- `models/Product.js` 模型定义完整，支持：
  - `space` 字段：幸运空间(`lucky`)/臻选空间(`premium`)/两者皆有(`both`)
  - `premium_exchange_points`/`premium_stock`：臻选空间专属配置
  - `getSpaceInfo()` 实例方法：根据空间返回差异化商品展示
  - `getProductsBySpace()` 类方法：按空间筛选商品列表
- **服务层代码扫描**：`grep "Product\." services/` → **无匹配结果**
  - `MarketListingService.js`：无 `Product.` 引用
  - `ExchangeService.js`：无 `Product.` 引用（使用 `ExchangeItem` 模型）
- **路由层代码扫描**：仅 `routes/v4/console/images.js` 有关联定义（图片管理）
- **对比**：`exchange_items` 表有26条数据，被 `ExchangeService.js` 实际使用

**分析结论**：
- `products` 是早期设计的"幸运空间/臻选空间"双空间商品系统
- `exchange_items` 是后来专门为材料兑换设计的表，已在生产运营
- `Product` 模型功能完整但**从未被服务层调用**，属于待启用功能

**您的决定**：✅ **B - 已确认保留**

**📋 业务规划说明**：
- **臻选空间（premium）**：用于中高级材料或积分兑换商品
- **幸运空间（lucky）**：所有用户可见的普通商品
- **双空间（both）**：两个空间都展示的商品

**📋 后续开发清单**：
1. 开发 `ProductService.js` 服务层
2. 创建 `/api/v4/products` 路由
3. 前端商城页面对接
4. 根据 `user_premium_status` 控制臻选空间访问权限

---

#### 决策11：@deprecated 方法处理

**现状**：3个 `@deprecated` 标记的方法：

| 文件 | 方法 | 说明 |
|------|------|------|
| `UnifiedLotteryEngine.js:939` | `getDrawPricing()` | 已迁移到 LotteryPricingService |
| `PricingStage.js:205` | 旧方法注释块 | 已迁移到 LotteryPricingService |
| `EligibilityStage.js:257` | `_getDailyDrawsCount()` | 已移到 LotteryQuotaService |

**选项**：
- **A. 立即删除**（✅ 推荐）
  - 理由：迁移已完成，无调用方
- **B. 保留一个版本**
  - 理由：观察稳定性后再删除

**您的决定**：✅ **A - 已确认立即删除**

**📋 执行清单**：
1. 删除 `services/UnifiedLotteryEngine/UnifiedLotteryEngine.js:937-944` 的 `@deprecated` 注释块
2. 删除 `services/UnifiedLotteryEngine/pipeline/stages/PricingStage.js:203-214` 的旧方法迁移说明注释块
3. 删除 `services/UnifiedLotteryEngine/pipeline/stages/EligibilityStage.js:250-259` 的废弃方法声明和注释

**📊 清理规模**：约 30 行注释代码

---

#### 决策12：迁移文件归档

**现状**（2026-01-21 文件系统验证）：
- `migrations/` 目录有 **172个迁移文件**（数据库记录279条，107个已删除）
- 其中 **21个**是清理类迁移（drop 7个 + cleanup 14个）
- 这些迁移已执行完成，不会再次运行
- 项目已有隐式策略：执行完的旧迁移直接删除文件，保留数据库记录

**选项**：
- **A. 创建 `migrations/archived/` 目录归档**
  - 需要额外配置 Sequelize 排除子目录
  - 增加维护复杂度
- **B. 保持现状**
  - 172个文件不动
- **C. 删除21个清理类迁移文件**（✅ 推荐）
  - 延续已有策略（已删107个）
  - 零配置修改
  - `sequelizemeta`表保留完整记录

**您的决定**：✅ **C - 已确认删除21个清理类迁移文件**

**📋 执行清单**：

**🗑️ drop类（7个）**：
| 文件名 | 说明 |
|--------|------|
| `20251014183000-drop-index-duplicate-indexes-cleanup.js` | 删除重复索引 |
| `20251230200000-drop-deprecated-points-tables.js` | 删除旧积分表 |
| `20260108210000-drop-trade-records-table.js` | 删除trade_records表 |
| `20260109120000-drop-deprecated-audit-tables.js` | 删除旧审计表 |
| `20260109124000-drop-audit-records-table.js` | 删除audit_records表 |
| `20260114000000-drop-column-deprecated-image-fields.js` | 删除废弃图片字段 |
| `20260114001257-drop-item-template-aliases-cleanup.js` | 删除别名表 |

**🧹 cleanup类（14个）**：
| 文件名 | 说明 |
|--------|------|
| `20251109234500-cleanup-users-table-duplicate-indexes.js` | 清理用户表重复索引 |
| `20251109234600-cleanup-user-inventory-duplicate-verification-code-indexes.js` | 清理库存验证码索引 |
| `20251228212949-unified-asset-domain-phase1-cleanup-credits.js` | 统一资产域清理 |
| `20260101000001-semantic-cleanup-remove-is-winner-add-reward-tier.js` | 语义清理 |
| `20260101211702-data-cleanup-and-reward-tier-fix.js` | 数据清理 |
| `20260101222406-fix-empty-prize-type-and-cleanup-enum.js` | 枚举清理 |
| `20260102500000-cleanup-redundant-indexes-audit.js` | 冗余索引清理 |
| `20260105162900-cleanup-misnamed-idempotency-indexes.js` | 幂等索引命名修复 |
| `20260107172335-cleanup-image-review-and-restructure-audit.js` | 图片审核清理 |
| `20260108111444-cleanup-orphan-frozen-v2.js` | 孤儿冻结清理v2 |
| `20260108240000-cleanup-orphan-frozen-balances.js` | 孤儿冻结余额清理 |
| `20260109100000-add-asset-orphan-cleanup-operation-type.js` | 资产孤儿清理操作类型 |
| `20260120193900-cleanup-legacy-draw-pricing-json.js` | 清理draw_pricing字段 |
| `20260121060000-cleanup-orphan-records.js` | 孤儿记录清理 |

**📊 清理效果**：迁移文件从172个减少到151个

---

## 七、执行顺序建议

```
Phase 0: 决策确认（0.5天）
  └─ 您确认上述12项决策

Phase 1: 数据层清理（0.5天）
  ├─ [如决策1=A] 删除 TARGET_TYPE_LEGACY_MAPPING 映射代码（数据已100%标准化，无需迁移）
  ├─ 验证已有迁移已全部执行（279个迁移记录）
  ├─ ✅ 删除21个清理类迁移文件（决策12 已确认）
  └─ 运行数据完整性验证脚本

Phase 2: 代码层清理（1-2天）
  ├─ 删除 @deprecated 注释块
  ├─ 删除迁移历史注释
  ├─ [如决策1=A] 删除 TARGET_TYPE_LEGACY_MAPPING
  ├─ [如决策2=A] 删除 ActivityService.js 中的 name 字段
  ├─ [如决策3=A] 删除 MarketListingService.js 中的 fallback
  └─ [如决策4=A] 修改 LotteryMetricsCollector.js 注释

Phase 3: 文件层清理（0.5天）✅ 全部已确认
  ├─ ✅ 保留根目录备份文件（决策8 已确认保留）
  ├─ ✅ 清理 backups/ 历史备份（决策6 已确认）
  └─ ✅ 整理根目录测试脚本：删9移6（决策7 已确认）

Phase 4: 服务合并（1天）✅ 已确认
  └─ 执行3项服务合并（LotteryAnalytics、Premium、TradeOrder）

Phase 5: 全量验证（1天）
  ├─ npm test 跑通所有测试
  ├─ 核心业务端到端测试
  └─ API 兼容性验证
```

---

## 八、风险提示

### ✅ 已降级为零风险的操作
1. **审计日志映射代码删除**：~~历史数据的 `target_type` 修改是不可逆操作~~
   - **2026-01-21 验证结果**：数据库中 0 条旧格式数据，**可直接删除映射代码，无需数据迁移**
   
2. **JSON字段删除**：`lottery_campaigns.draw_pricing` 删除前需验证数据完整
   - 缓解：对比新旧表数据一致性（迁移已完成）

### 🟡 中风险操作
3. **前端兼容字段删除**（决策2）：可能影响未更新的前端版本
   - 缓解：与前端团队确认，或设置过渡期

4. **服务合并**（决策5 ✅ 已确认）：可能影响现有调用方
   - 缓解：保持原方法签名，仅合并文件
   - 状态：将执行3项服务合并，保持API兼容

5. ~~**products 表处理**~~（决策10 ✅ 已确认保留）：用于"双空间商品"功能
   - 状态：保留表和模型，待开发 ProductService 和路由

---

## 九、验收标准

### 代码层验收
- [ ] 无 `@deprecated` 标记的代码存在
- [ ] 无 `旧链路`、`已迁移`、`Phase X 迁移` 等注释
- [ ] `AuditTargetTypes.js` 无 `TARGET_TYPE_LEGACY_MAPPING`
- [ ] 所有测试通过 `npm test`

### 数据层验收
- [ ] `admin_operation_logs.target_type` 全部为 snake_case 格式
- [ ] 无引用已删除表的代码
- [ ] `lottery_campaigns.draw_pricing` 字段已删除（如决策执行）

### 文件层验收
- [ ] 根目录无 `.backup` 文件
- [ ] `backups/` 目录仅保留指定备份
- [ ] 根目录无临时测试脚本

---

## 十、决策确认模板

### 📋 12项决策汇总表

请逐一确认以下决策（回复编号+选项，如 "决策1: A"）：

| 决策项 | 选项 | 推荐 | 风险等级 | 验证状态 | 您的决定 |
|--------|------|------|---------|---------|---------|
| **决策1**：审计日志映射代码 | A(删除) / B(保留) | ✅ A | 🟢 零风险 | ✅ 已验证（0条旧格式） | ✅ A 已确认 |
| **决策2**：前端 `name` 字段 | A(删除) / B(保留) | ✅ A | 🟢 低风险 | ✅ 前端分析完成 | ✅ A 已确认 |
| **决策3**：`rarity` fallback 逻辑 | A(简化) / B(保留) | ✅ A | 🟢 低风险 | ✅ 已验证（无 offer_item_meta 字段） | ✅ A 已确认 |
| **决策4**：`empty→fallback` 分离 | A(合并) / B(分离) | ✅ B | 🟡 低风险 | ✅ 便于监控排查 | ✅ B 已确认 |
| **决策5**：服务合并策略 | A(执行合并) / B(暂不合并) | ✅ A | 🟡 中等 | ✅ 可合并3个服务 | ✅ A 已确认 |
| **决策6**：备份保留策略 | 删除15个 / 保留5个 | ✅ 已确认 | 🟢 零风险 | ✅ 删除2025年+保留2026年 | ✅ 已确认 |
| **决策7**：测试脚本处理 | A(删9移6) / B(全删) / C(全移) / D(保持) | ✅ A | 🟢 零风险 | ✅ 9个重复/6个有价值 | ✅ A 已确认 |
| **决策8**：根目录备份文件 | A(全删) / B(保留) | ✅ B | 🟢 零风险 | ✅ 确认3个备份文件 | ✅ B 已确认 |
| **决策9**：空表处理策略 | A(删除) / B(保留) | ✅ B | 🟢 零风险 | ✅ 功能待启用 | ✅ B 已确认 |
| **决策10**：products 表处理 | A(删除) / B(保留) | ✅ B | 🟢 零风险 | ✅ 深度验证：保留用于双空间商品 | ✅ B 已确认 |
| **决策11**：@deprecated 方法处理 | A(立即删除) / B(保留一版本) | ✅ A | 🟢 低风险 | ✅ 迁移已完成 | ✅ A 已确认 |
| **决策12**：迁移文件归档 | A(归档) / B(保持) / C(删除清理类) | ✅ C | 🟢 零风险 | ✅ 删除21个清理类迁移文件 | ✅ C 已确认 |

---

### ⚠️ 需要您特别确认的3个问题

#### ~~问题1：前端是否已统一使用 `campaign_name`？（对应决策2）~~ ✅ 已确认

**✅ 已确认**：选择 A - 删除 `name` 字段，统一使用 `campaign_name`

**执行清单**：
1. 修改 `campaigns.html` 第277、352行
2. 清理5处兼容写法
3. 删除 `ActivityService.js:95` 的 `name` 字段

---

#### 问题2：`empty → fallback` 是业务设计还是迁移兼容？（对应决策4）

**当前代码**：`services/LotteryMetricsCollector.js:90`
```javascript
empty: 'fallback_tier_count' // 兼容处理：empty 归入 fallback
```

- **如果是业务设计**（空奖归入兜底统计是合理的）→ 选 A，保留逻辑但修改注释措辞
- **如果是迁移兼容** → 选 B，执行清理

---

#### ~~问题3：`products` 表的52条数据是否有业务用途？（对应决策10）~~ ✅ 已确认

**✅ 已确认**：选择 B - 保留 `products` 表，"双空间商品"功能计划启用

**📋 业务规划**：
- **臻选空间（premium）**：用于中高级材料或积分兑换商品
- **幸运空间（lucky）**：所有用户可见的普通商品
- **双空间（both）**：两个空间都展示的商品

**📋 后续开发清单**：
1. 开发 `ProductService.js` 服务层
2. 创建 `/api/v4/products` 路由
3. 前端商城页面对接
4. 根据 `user_premium_status` 控制臻选空间访问权限

---

### 📝 推荐说明

| 决策 | 推荐理由 |
|------|---------|
| 决策1 ✅ A 已确认 | 数据库验证已确认：4,029条审计日志100%为snake_case格式，映射代码已无用（零风险） |
| 决策3 ✅ A 已确认 | `market_listings` 表无 `offer_item_meta` 字段，fallback逻辑实际无意义 |
| 决策4 ✅ B 已确认 | 分离 `empty` 和 `fallback` 统计，便于区分系统异常和正常兜底 |
| 决策5 ✅ A 已确认 | 执行3项服务合并，降低长期维护成本 |
| 决策6 ✅ 已确认 | 删除15个旧备份（2025年+2026-01-02），保留5个2026年备份 |
| 决策7 ✅ A 已确认 | 删除9个重复脚本 + 移动6个有价值脚本到 `scripts/` |
| 决策8 ✅ B 已确认 | 保留3个根目录备份文件 |
| 决策9 ✅ B 已确认 | 保留空表，功能待后续启用 |
| 决策11 ✅ A 已确认 | 迁移已完成，无调用方，删除3处迁移说明注释（约30行） |
| 决策12 ✅ C 已确认 | 删除21个清理类迁移文件（drop 7个 + cleanup 14个），延续已有策略 |

---

### 🚀 快速确认模板

如果您同意所有推荐选项，可直接复制并填写以下模板：

```
决策1: ✅ A（删除映射代码）- 已确认
决策2: ✅ A（删除name字段）- 已确认
决策3: ✅ A（简化rarity逻辑）- 已确认
决策4: ✅ B（分离统计）- 已确认
决策5: ✅ A（执行合并）- 已确认
决策6: ✅ 已确认（删除15个/保留5个）
决策7: ✅ A（删9移6）- 已确认
决策8: ✅ B（保留根目录备份）- 已确认
决策9: ✅ B（保留空表）- 已确认
决策10: ✅ B（保留products表）- 已确认
决策11: ✅ A（删除deprecated方法）- 已确认
决策12: ✅ C（删除21个清理类迁移文件）- 已确认
```

---

## 附录：清理脚本模板

### A1. 审计日志 target_type 迁移脚本

> ⚠️ **2026-01-21 验证结论**：经真实数据库验证，所有 `target_type` 已经是 snake_case 标准格式，**此脚本无需执行**。
> 
> 可直接删除 `constants/AuditTargetTypes.js` 中的 `TARGET_TYPE_LEGACY_MAPPING` 映射表。

```sql
-- ✅ 已验证：此脚本无需执行（数据已标准化）
-- 保留脚本仅供参考，实际数据已全部为 snake_case 格式

-- 验证查询（用于确认数据状态）
SELECT target_type, COUNT(*) as count 
FROM admin_operation_logs 
WHERE target_type REGEXP '^[A-Z]'  -- 查找 PascalCase 格式
GROUP BY target_type;

-- 预期结果：0 条记录（已验证通过）
```

### A2. 备份清理脚本

```bash
#!/bin/bash
# scripts/cleanup/clean-old-backups.sh

# 删除根目录备份文件（2026-01-21 实测存在的文件）
rm -f /home/devbox/project/app.js.backup
rm -f /home/devbox/project/app.js.backup.20260108_125922
rm -f /home/devbox/project/ecosystem.config.js.backup

# 保留指定备份，删除其余
cd /home/devbox/project/backups

# 列出要删除的目录
KEEP_DIRS="backup_2026-01-04_complete backup_2026-01-20_complete"

for dir in backup_*; do
  if [[ ! " $KEEP_DIRS " =~ " $dir " ]]; then
    echo "删除: $dir"
    rm -rf "$dir"
  else
    echo "保留: $dir"
  fi
done

echo "清理完成"
```

### A3. 测试脚本整理脚本

```bash
#!/bin/bash
# scripts/cleanup/organize-test-scripts.sh

# 创建目标目录
mkdir -p /home/devbox/project/scripts/adhoc-tests

# 移动根目录测试脚本
cd /home/devbox/project
mv test-*.js scripts/adhoc-tests/ 2>/dev/null
mv test_*.js scripts/adhoc-tests/ 2>/dev/null
mv seed-*.js scripts/adhoc-tests/ 2>/dev/null

echo "测试脚本已移动到 scripts/adhoc-tests/"
ls -la scripts/adhoc-tests/
```

### A4. 数据完整性验证脚本

```javascript
// scripts/verify-data-integrity.js
require('dotenv').config();
const { sequelize } = require('../config/database');

async function verifyDataIntegrity() {
  console.log('🔍 开始数据完整性验证...\n');
  
  const checks = [];
  
  // 1. 检查审计日志 target_type 格式
  const [legacyTypes] = await sequelize.query(`
    SELECT target_type, COUNT(*) as count 
    FROM admin_operation_logs 
    WHERE target_type REGEXP '^[A-Z]'
    GROUP BY target_type
  `);
  
  if (legacyTypes.length > 0) {
    checks.push({
      name: '审计日志 target_type 格式',
      status: 'WARNING',
      detail: `发现 ${legacyTypes.length} 种 PascalCase 格式的 target_type`
    });
  } else {
    checks.push({
      name: '审计日志 target_type 格式',
      status: 'PASS',
      detail: '所有 target_type 均为 snake_case 格式'
    });
  }
  
  // 2. 检查 market_listings 的 rarity 字段
  const [[rarityCheck]] = await sequelize.query(`
    SELECT COUNT(*) as count 
    FROM market_listings 
    WHERE offer_item_rarity IS NULL
  `);
  
  checks.push({
    name: 'market_listings.offer_item_rarity 完整性',
    status: rarityCheck.count > 0 ? 'WARNING' : 'PASS',
    detail: rarityCheck.count > 0 
      ? `${rarityCheck.count} 条记录 offer_item_rarity 为空`
      : '所有记录均有 offer_item_rarity 值'
  });
  
  // 输出结果
  console.log('验证结果:');
  checks.forEach(check => {
    const icon = check.status === 'PASS' ? '✅' : '⚠️';
    console.log(`${icon} ${check.name}: ${check.detail}`);
  });
  
  process.exit(checks.every(c => c.status === 'PASS') ? 0 : 1);
}

verifyDataIntegrity();
```

---

## 十一、项目健康状态总结

### ✅ 整体评估：项目架构已非常健康

基于 2026-01-21 的全面分析和数据库验证，项目当前状态如下：

#### 数据库层面（优秀 ✅）
- ✅ 所有旧表已删除（12张旧表全部清理）
- ✅ 所有废弃字段已删除
- ✅ 审计日志 `target_type` 100% 标准化（snake_case 格式）
- ✅ 279个迁移记录已全部执行（文件172个，待删除21个清理类）
- ✅ 67张表，数据完整健康

#### 服务架构层面（良好 ✅）
- ✅ 统一的资产底座（AssetService + 账本系统）
- ✅ 统一的抽奖引擎（V4.6 Pipeline 架构）
- ✅ 统一的幂等性服务（IdempotencyService）
- ✅ 统一的定价服务（LotteryPricingService）
- ✅ 清晰的双轨业务设计（背包、市场、监控）

#### 待清理项（代码注释层面）
- ✅ `TARGET_TYPE_LEGACY_MAPPING` 映射代码（47个条目）→ **决策1 已确认删除**
- ✅ `@deprecated` 注释块（3处）→ **决策11 已确认删除**
- ⚠️ 迁移历史注释（约15处）
- ✅ 根目录备份文件（3个）→ **决策8 已确认保留**
- ✅ 根目录测试脚本（9个删除+6个移动）→ **决策7 已确认**
- ✅ 历史备份目录（15个已确认删除）→ **决策6 已确认**
- ✅ `products` 表（52条数据）→ **决策10 已确认保留**（双空间商品功能待开发）

### 🎯 收益预期

| 维度 | 预期效果 |
|------|---------|
| **代码可读性** | 删除约100-200行无用代码和注释 |
| **维护成本** | 消除历史包袱，新人上手更快 |
| **磁盘空间** | 清理备份后释放约500MB-1GB |
| **技术债务** | 降低长期维护复杂度 |

### 📋 推荐决策（基于分析结论）

| 决策项 | 推荐选项 | 理由 |
|--------|---------|------|
| 决策1：审计日志映射 | ✅ A（已确认删除） | 数据已100%标准化（4,029条），零风险 |
| 决策2：前端name字段 | ✅ A（已确认删除） | 仅1个文件需修改+5处兼容写法清理 |
| 决策3：rarity fallback | ✅ A（已确认删除） | `market_listings` 无 `offer_item_meta` 字段，fallback 无意义 |
| 决策4：empty→fallback | ✅ B（已确认分离） | 分离统计，新增 `empty_count`，便于监控异常 |
| 决策5：服务合并 | ✅ A 已确认 | 执行3项服务合并（抽奖监控、会员查询、订单查询） |
| 决策6：备份保留 | ✅ 已确认 | 删除15个旧备份，保留5个2026年备份（01-04/08/13/18/20） |
| 决策7：测试脚本 | ✅ A 已确认 | 删除9个重复脚本 + 移动6个有价值脚本到 `scripts/` |
| 决策8：根目录备份 | ✅ B 已确认 | 保留3个根目录备份文件 |
| 决策9：空表处理 | ✅ B 已确认 | 保留空表，功能待启用 |
| 决策10：products表 | ✅ B 已确认 | 保留"双空间商品"功能：臻选空间用于中高级材料/积分兑换，幸运空间用于所有用户可见商品 |
| 决策11：@deprecated方法 | ✅ A 已确认 | 迁移已完成，无调用方，删除3处约30行注释 |
| 决策12：迁移文件归档 | ✅ C 已确认 | 删除21个清理类迁移文件（172个→151个） |

---

## 十二、代码层验证结果（2026-01-21 最新）

### 12.1 @deprecated 标记分布（3处）

| 文件 | 行号 | 内容 |
|------|------|------|
| `services/UnifiedLotteryEngine/UnifiedLotteryEngine.js` | 939 | `@deprecated 2026-01-21 技术债务修复` - getDrawPricing 方法 |
| `services/UnifiedLotteryEngine/pipeline/stages/PricingStage.js` | 205 | `@deprecated 2026-01-21 技术债务修复` - 旧方法迁移注释 |
| `services/UnifiedLotteryEngine/pipeline/stages/EligibilityStage.js` | 257 | `@deprecated 此方法已移除` - _getDailyDrawsCount 方法 |

### 12.2 兼容/迁移相关代码分布（38处）

通过 `grep -r "兼容|已迁移|旧链路|旧方案"` 扫描 `services/` 目录，发现 38 处相关代码：

#### 需要清理的迁移注释（约15处）

| 文件 | 关键内容 | 建议动作 |
|------|---------|---------|
| `services/index.js:7-17` | `移除向后兼容代码`、`不再兼容 camelCase key` | 删除迁移说明注释 |
| `services/UnifiedLotteryEngine/UnifiedLotteryEngine.js:201` | `旧链路：strategies → executeLottery` | 删除旧链路说明 |
| `services/UnifiedLotteryEngine/UnifiedLotteryEngine.js:369` | `已删除字段：strategy_used` | 删除已删除字段说明 |
| `services/UnifiedLotteryEngine/UnifiedLotteryEngine.js:632` | `已删除 enabled_strategies 兼容字段` | 删除已删除字段说明 |
| `services/IdempotencyService.js:609` | `强制不兼容策略` | 可保留（是设计决策） |
| `services/IdempotencyService.js:791` | `强制不兼容，不做双算比对` | 可保留（是设计决策） |
| `services/ExchangeService.js:60` | `暴力移除旧方案` | 删除迁移说明 |
| `services/ExchangeService.js:1110,1255` | `已删除 item_name/item_description 兼容逻辑` | 删除已删除说明 |
| `services/PopupBannerService.js:604` | `已删除"完整URL兼容"逻辑` | 删除已删除说明 |

#### 需要决策的兼容代码（4处）

| 文件 | 行号 | 内容 | 对应决策 |
|------|------|------|---------|
| `services/ActivityService.js` | 95 | `name: campaign.campaign_name, // 兼容前端期望的 name 字段` | ✅ 决策2 已确认删除 |
| `services/MarketListingService.js` | 1032 | `rarity: listing.offer_item_rarity \|\| ... \|\| 'common'` | ✅ 决策3 已确认删除 |
| `services/LotteryMetricsCollector.js` | 90 | `empty: 'fallback_tier_count' // 兼容处理` | ✅ 决策4 已确认分离 |
| `services/IdempotencyService.js` | 428 | 尾斜杠兼容匹配逻辑 | 建议保留（安全处理） |

#### 合理保留的兼容代码（约19处）

| 文件 | 说明 | 保留理由 |
|------|------|---------|
| `services/DataSanitizer.js:19,26,64` | 禁止字段兼容逻辑说明 | 架构设计文档 |
| `services/ChatWebSocketService.js:11` | 向后兼容 REST API | 正常设计说明 |
| `services/ChatWebSocketService.js:174` | 删除 is_admin 兼容 | 已完成的清理说明 |
| `services/ImageService.js:8,476,480` | S3 兼容存储说明 | 技术架构文档 |
| `services/sealosStorage.js:723` | S3 兼容存储回退 | 正常容错逻辑 |
| `services/PrizePoolService.js:141,153` | 兼容字段注释 | 需确认后清理 |
| `services/ConsumptionService.js:279,375` | 架构决策说明 | 已完成的清理说明 |
| `services/UnifiedLotteryEngine/utils/PerformanceMonitor.js:462` | 兼容测试调用 | 正常公共方法 |
| `services/UnifiedLotteryEngine/pipeline/stages/SettleStage.js:169,182` | 与旧链路一致 | 可改为"保持一致" |
| `services/UnifiedLotteryEngine/pipeline/stages/PricingStage.js:140` | 兼容旧链路输出格式 | 可改为"保持一致" |

### 12.3 表结构验证（2026-01-21 实测）

```
📊 核心表结构验证:

item_instances (物品实例表):
  ✅ item_instance_id: bigint (主键)
  ✅ owner_user_id: int (所有者)
  ✅ item_type: varchar(50)
  ✅ item_template_id: bigint (关联模板)
  ✅ status: enum('available','locked','transferred','used','expired')
  ✅ meta: json
  ✅ locks: json

redemption_orders (核销订单表):
  ✅ order_id: char(36) (UUID主键)
  ✅ code_hash: varchar(64) (核销码哈希)
  ✅ item_instance_id: bigint (关联物品)
  ✅ redeemer_user_id: int
  ✅ status: enum('pending','fulfilled','cancelled','expired')
  ✅ expires_at: datetime
  ✅ fulfilled_at: datetime

account_asset_balances (账户资产余额表):
  ✅ balance_id: bigint (主键)
  ✅ account_id: bigint (关联账户)
  ✅ asset_code: varchar(50) (资产代码)
  ✅ available_amount: bigint (可用余额)
  ✅   frozen_amount: bigint (冻结余额)
  ✅ campaign_id: varchar(50) (活动维度)
  ✅ campaign_key: varchar(50) (活动键)

item_templates (物品模板表):
  ✅ item_template_id: bigint (主键)
  ✅ template_code: varchar(100)
  ✅ item_type: varchar(50)
  ✅ category_code: varchar(50)
  ✅ rarity_code: varchar(50)
  ✅ display_name: varchar(200)
  ✅ reference_price_points: decimal(10,2)
  ✅ is_tradable: tinyint(1)
  ✅ is_enabled: tinyint(1)
  ✅ meta: json

market_listings (市场挂牌表):
  ✅ offer_item_rarity: varchar(50) (快照字段)
  ❌ offer_item_meta: 字段不存在（无需 fallback 到 meta）
```

### 12.4 关键发现总结

| 发现项 | 结论 | 影响 |
|--------|------|------|
| 审计日志 target_type | 100% snake_case 格式（4,029条验证通过） | 决策1 自动变为 A（直接删除映射代码） |
| market_listings.offer_item_meta | 字段不存在 | 决策3 的 fallback 逻辑实际无意义 |
| products 表 | 52条数据，**服务层零引用**（grep验证） | 决策10 已确认保留：待开发 ProductService 和路由 |
| exchange_items 表 | 26条数据，被 ExchangeService 实际使用 | 兑换功能正常 |
| Product.js 模型 | 功能完整但从未被服务层调用 | ✅ 已确认保留：待开发 ProductService |
| 旧表删除状态 | 7个旧表全部已删除 | 无代码引用风险 |
| lottery_campaigns.draw_pricing | 字段已删除 | 定价迁移完成 |
| 迁移文件数 | 279个全部已执行 | 数据库结构稳定 |

### 12.5 核心业务数据统计（2026-01-21 验证）

| 表名 | 记录数 | 说明 |
|------|--------|------|
| `users` | 28 | 用户主表 |
| `item_instances` | 1,992 | 物品实例（背包+交易） |
| `asset_transactions` | 5,075 | 资产交易流水 |
| `admin_operation_logs` | 4,029 | 审计日志 |
| `redemption_orders` | 932 | 核销订单 |
| `lottery_management_settings` | 2,338 | 抽奖管理配置 |
| `market_listings` | 38 | 市场挂牌 |
| `exchange_items` | 26 | 兑换商品 |
| `products` | 52 | ✅ 保留（"双空间商品"功能待开发） |

---

**文档维护说明**：本文档将随重构进度更新，每完成一个Phase后在对应章节标记 ✅ 完成状态。
