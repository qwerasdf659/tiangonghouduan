# 技术债务清理 — 执行清单

> 生成日期：2026-03-02  
> 状态：**全部完成（第一批～第七批）**  
> 完成日期：2026-03-02  
> 验证方式：真实代码库文件引用检查 + 数据库 `restaurant_points_dev` 实时查询

---

## 执行总览

| 批次 | 内容 | 文件数 | 风险 | 状态 |
|------|------|--------|------|------|
| 第一批 | 零风险批量删除 | ~315 | 零 | **已完成** |
| 第二批 | 后端代码清理 | 7 | 低 | **已完成** |
| 第三批 | 数据库迁移 | 1 迁移 + 1 模型 | 低 | **已完成** |
| 第四批 | package.json 精简 | 1 | 低 | **已完成** |
| 第五批 | 空目录 + gitignore | ~5 目录 | 零 | **已完成** |
| 第六批 | docs 清理 | 1 | 零 | **已完成** |
| 第七批 | P1 合并 daily-asset-reconciliation | 3 | 中 | **已完成** |
| 第八批 | P2 迁移 squash（推迟） | 98 → 1 | 高 | **推迟至上线前** |

---

## 质量验证结果（2026-03-02）

| 检查项 | 结果 |
|--------|------|
| Health Check | 通过 — database connected, redis connected, status healthy |
| ESLint | 通过 — 0 新增错误（18 个预存 JSDoc/style 警告级错误） |
| Jest 单元/集成测试 | 37/38 通过 — 1 个预存 WebSocket 认证测试失败 |
| Jest 业务测试 | 503/548 通过 — 37 个预存失败（DECIMAL 类型比较等） |
| Redis | 连接正常 — PING/PONG |
| PM2 | 在线 — PID 稳定、内存正常 |

---

## 各批次完成详情

### 第一批：零风险批量删除（~315 个文件）— 已完成

- Step 1.1：`migrations/archive/` 整个目录已删除（189 个文件）
- Step 1.2：`scripts/temp-*.js` 临时脚本已删除（5 个文件）
- Step 1.3：一次性 fix/update 脚本已删除（13 个文件）
- Step 1.4：手动测试脚本已删除（~43 个文件）
- Step 1.5：admin 测试脚本已删除（9 个文件）
- Step 1.6：一次性 check/verify/analyze 脚本已删除（~30 个文件）
- Step 1.7：一次性数据操作脚本已删除（11 个文件）
- Step 1.8：子目录中可删除的脚本已删除（11 个文件）
- Step 1.9：无引用的工具文件已删除（`utils/model-converter.js`、`utils/TestAccountManager.js`）
- Step 1.10：杂项文件已删除（`callbacks/settings.local.json`、`data/pca-code.json`）

### 第二批：后端代码清理 — 已完成

- Step 2.1：`services/index.js` 墓碑注释已清理
- Step 2.2：`utils/QRCodeValidator.js` V1 代码已清理
- Step 2.3：`utils/CampaignCodeGenerator.js` 旧注释已清理
- Step 2.4：`utils/BusinessCacheHelper.js` 废弃方法已清理
- Step 2.5：`jobs/content-cron-jobs.js` 已重命名为 `jobs/ad-campaign-expiry-jobs.js`
- Step 2.6：`StrategyConfig.js` 已重命名为 `ComputeConfig.js`，7 个文件 10 处引用已更新
- Step 2.7：`app.js` 冗余注释已清理

### 第三批：数据库迁移 — 已完成

- Step 3.1：`item_templates` 废弃字段 `image_url`、`thumbnail_url` 已 DROP，模型已同步
- Step 3.2：4 条旧编码 CampaignCode 已迁移为新格式（迁移文件：`20260302122453-update-legacy-campaign-codes.js`）

### 第四批：package.json 精简 — 已完成

- 已删除引用已删除脚本的 npm scripts：`check:dom`、`check:api`、`check:all`、`quality:business-semantic`、`test:full`

### 第五批：空目录 + gitignore — 已完成

- `scripts/integration/` 空目录已删除
- `.gitignore` 已添加 `test-results/`

### 第六批：docs 清理 — 已完成

- `docs/管理员告警持久化-实施方案.md` 已删除（文档内标注"已实施完成"）

### 第七批：P1 合并 daily-asset-reconciliation — 已完成

- `executeBusinessRecordReconciliation()` 已迁移到 `scripts/reconcile-items.js`
- `scheduled_tasks.js` Task 15 引用已更新
- `jobs/daily-asset-reconciliation.js` 已删除

---

## 第八批（推迟）：P2 迁移 squash

| 操作 | 风险 | 建议时机 |
|------|------|---------|
| 98 个活跃迁移文件 squash 为 baseline-v4 | 高 | 上线前单独窗口执行 |

推迟原因：需冻结数据库变更、全量验证 baseline、回归测试。开发阶段风险高于收益。

---

## 经验证不执行的项目

| 项目 | 原因 |
|------|------|
| 拆分 `scheduled_tasks.js` | 实际 3,644 行、37 个模块化方法，结构合理 |
| 路由域合并 activities→lottery | 经验证无功能重叠，属于不同业务域 |
| 文件命名改 kebab-case | PascalCase 是 150+ 文件的一致约定，改动收益低 |
| scripts/ 目录重组 | Phase 1 清理后剩余结构已合理 |
| transactionHelpers vs TransactionManager 合并 | 经验证为互补关系，非重叠 |

---

## 微信小程序前端对接说明

以下变更可能影响微信小程序前端，需前端开发人员确认：

### 无影响项（前端无需修改）

| 变更 | 原因 |
|------|------|
| 后端文件删除（脚本、迁移归档） | 不影响 API 接口 |
| `services/index.js` 注释清理 | 不影响 API 行为 |
| `ComputeConfig` 重命名 | 内部实现细节，API 无变化 |
| `app.js` 注释清理 | 不影响 API 行为 |
| `package.json` 精简 | 不影响运行时 |
| `item_templates` 字段 DROP | `image_url`/`thumbnail_url` 字段全部为 NULL，前端应使用 `image_resource_id` 关联查询 |

### 需前端确认项

| 变更 | 前端检查点 |
|------|-----------|
| 4 条旧 CampaignCode 已更新为新格式 | 如果前端有硬编码引用 `BASIC_LOTTERY`、`CAMP_1769298977641_853LKG`、`CAMP_1769299553926_MRXZNB`、`CAMP_1769347406486_TUS05A`，需替换为新编码。正常情况下前端通过 API 动态获取，无需修改。 |
| `jobs/content-cron-jobs.js` → `jobs/ad-campaign-expiry-jobs.js` | 前端不直接调用 cron job，无影响。但如果有相关日志排查场景，留意文件名变更。 |

### 后端 API 无变更清单

本次技术债务清理**未修改任何 API 端点、请求参数、响应格式**，微信小程序前端可继续按现有接口规范开发。
