# 餐厅积分抽奖系统 V4.0 - 环境变量配置模板
# 🔴 重要：生产环境部署前必须修改所有 CHANGE_ME_* 占位符
# 生成时间：2025/12/30 21:31:03

# === 运行环境 ===
# 运行环境标识（development/staging/production） (必需)
NODE_ENV=development

# HTTP 服务端口 (必需)
PORT=3000

# 监听地址 (可选)
HOST=0.0.0.0

# 时区设置（固定 Asia/Shanghai） (必需)
TZ=Asia/Shanghai

# === 数据库配置 ===
# MySQL 数据库主机地址 (必需)
DB_HOST=your_database_host

# MySQL 数据库端口 (必需)
DB_PORT=3306

# 数据库名称 (必需)
DB_NAME=your_database_name

# 数据库用户名 (必需)
DB_USER=your_username

# 数据库密码 (必需)
DB_PASSWORD=CHANGE_ME_db_password_min_1_chars

# === 缓存配置 ===
# Redis 连接 URL（格式：redis://host:port 或 redis://:password@host:port/db） (必需)
REDIS_URL=redis://localhost:6379

# === JWT配置 ===
# JWT 签名密钥（至少 32 字符） (必需)
JWT_SECRET=CHANGE_ME_jwt_secret_min_32_chars

# JWT 刷新令牌密钥（至少 32 字符） (必需)
JWT_REFRESH_SECRET=CHANGE_ME_jwt_refresh_secret_min_32_chars

# JWT Token 有效期（默认 2h） (可选)
JWT_EXPIRES_IN=2h

# JWT 刷新令牌有效期（默认 7d） (可选)
JWT_REFRESH_EXPIRES_IN=7d

# === 加密配置 ===
# 数据加密密钥（必须 32 字符） (必需)
ENCRYPTION_KEY=CHANGE_ME_encryption_key_min_32_chars

# === 对象存储 ===
# Sealos 对象存储端点 (必需)
SEALOS_ENDPOINT=your_value_here

# Sealos 内网端点（仅 Sealos 部署需要） (可选)
SEALOS_INTERNAL_ENDPOINT=your_value_here

# Sealos 存储桶名称 (必需)
SEALOS_BUCKET=your_value_here

# Sealos 访问密钥 (必需)
SEALOS_ACCESS_KEY=CHANGE_ME_sealos_access_key

# Sealos 密钥 (必需)
SEALOS_SECRET_KEY=CHANGE_ME_sealos_secret_key

# Sealos 区域 (必需)
SEALOS_REGION=your_value_here

# === 微信小程序 ===
# 微信小程序 AppID (必需)
WX_APPID=wx1234567890abcdef

# 微信小程序 AppSecret (必需)
WX_SECRET=CHANGE_ME_wx_secret

# === CORS白名单 ===
# CORS 允许的域名列表（逗号分隔） (必需)
ALLOWED_ORIGINS=http://localhost:3000,https://your-domain.com

# === 日志配置 ===
# 日志级别（debug/info/warn/error） (可选)
LOG_LEVEL=debug

# === 性能监控 ===
# 是否启用连接池监控 (可选)
ENABLE_POOL_MONITORING=true

# === OCR服务 ===
# OCR 服务提供商（baidu_ocr 等） (可选)
OCR_PROVIDER=your_value_here

# OCR 服务 API Key (可选)
OCR_API_KEY=CHANGE_ME_ocr_api_key

# OCR 服务 Secret Key (可选)
OCR_SECRET_KEY=CHANGE_ME_ocr_secret_key

# === 短信服务 ===
# 短信服务提供商（aliyun_sms 等） (可选)
SMS_PROVIDER=your_value_here

# 短信服务访问密钥 (可选)
SMS_ACCESS_KEY=CHANGE_ME_sms_access_key

# 短信服务密钥 (可选)
SMS_SECRET_KEY=CHANGE_ME_sms_secret_key

# 短信签名名称 (可选)
SMS_SIGN_NAME=your_value_here

# 短信模板代码 (可选)
SMS_TEMPLATE_CODE=your_value_here

