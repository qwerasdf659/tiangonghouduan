---
alwaysApply: true
lastUpdated: 2025年10月13日 20:19:57
---

# ⏰ 日期时间处理标准（中国区域 - 存储/传输 UTC，展示北京时间）

> 时间形态定稿（B-2，2026-06-25）：**API 对外时间字段统一为 UTC ISO8601 带 `Z`**（如 `2026-06-24T19:14:09.000Z`）。
> 实现：`config/database.js` 的 `dateStrings:false` + `timezone:'+08:00'`，使 Sequelize JSON 序列化输出 UTC ISO；
> 存储/传输用 UTC（无歧义、对齐大厂主流），展示层由前端统一组件强制按 `Asia/Shanghai` 渲染北京时间。
> 相对时间（"2天前"）由前端实时计算。后端不再下发"已格式化的本地时间串/中文串/四合一对象"。

## 🔴 **基于实际时间处理问题的标准化体系**

### **实际发现的时间处理问题统计**
从11个cursor文档深度分析发现：
- **时区不一致问题**: 混用UTC和GMT+8时间，出现15+次
- **数据库时间字段混用**: `NOW()` vs `new Date()`混用，导致时区不一致8次
- **日期格式不统一**: ISO格式、本地格式、UTC字符串格式混用12+次
- **临时ID时间戳冲突**: `Date.now()`高频使用可能导致重复ID，发现20+处
- **日期范围查询复杂**: 复杂的时间计算代码，容易出错6次

### **实际发现的时区混乱模式**
```markdown
# cursor文档中发现的实际时区不一致问题：

## 问题1：导出时间 vs 应用时间不一致
导出时间：_Exported on 2025/8/13 at GMT+8 6:41:06_
应用时间：**更新时间**：2025年08月05日 23:03:15 UTC

## 问题2：混合时间格式和时区
- `new Date().toLocaleString()` (本地时间，时区不确定)
- `new Date().toISOString()` (UTC时间，需要转换)
- `NOW()` (数据库服务器时间)
- 手动格式化时间字符串
```

## 🇨🇳 **时区一致性强制规范（中国区域专用）**

### **全系统北京时间标准化**
```javascript
// 🔴 强制要求：全系统统一使用北京时间（GMT+8/Asia/Shanghai）
const BEIJING_TIME_STANDARD_SYSTEM = {
  // 系统时间配置标准
  timeConfig: {
    // 内部存储/传输：UTC（标准做法，无歧义）
    storage: 'UTC',
    // 数据库连接：会话时区 +08:00（把库内北京墙钟正确解释为北京时刻），dateStrings:false 使序列化输出 UTC ISO
    database: { timezone: '+08:00', sessionTimeZone: '+08:00', dateStrings: false },
    // 前端显示：统一组件强制按 Asia/Shanghai 展示北京时间
    display: { userTimeZone: 'Asia/Shanghai', format: 'YYYY-MM-DD HH:mm:ss' },
    /*
     * API传输：UTC ISO8601 带 Z（B-2 定稿，2026-06-25）。
     * 例：2026-06-24T19:14:09.000Z（= 北京 2026-06-25 03:14:09）。展示转北京交前端统一组件。
     */
    api: { format: 'ISO8601-UTC', timezone: 'UTC', example: '2026-06-24T19:14:09.000Z' }
  },

  // 统一时间创建函数
  createTime: {
    // 北京时间创建（系统内部使用）
    beijing: () => {
      return new Date().toLocaleString('zh-CN', { 
        timeZone: 'Asia/Shanghai',
        year: 'numeric', month: '2-digit', day: '2-digit',
        hour: '2-digit', minute: '2-digit', second: '2-digit',
        hour12: false
      });
    },
    
    // ISO格式北京时间（API传输）
    beijingISO: () => {
      const date = new Date();
      const beijingDate = new Date(date.toLocaleString('en-US', { timeZone: 'Asia/Shanghai' }));
      return beijingDate.toISOString().replace('Z', '+08:00');
    },
    
    // 数据库时间（使用北京时间）
    database: () => {
      const date = new Date();
      return new Date(date.toLocaleString('en-US', { timeZone: 'Asia/Shanghai' }));
    },
    
    // 显示时间（用户界面 - 北京时间）
    display: (dateTime) => {
      const date = typeof dateTime === 'string' ? new Date(dateTime) : dateTime;
      return date.toLocaleString('zh-CN', {
        timeZone: 'Asia/Shanghai',
        year: 'numeric', month: '2-digit', day: '2-digit',
        hour: '2-digit', minute: '2-digit', second: '2-digit',
        hour12: false
      });
    }
  },

  // 禁用的时间处理方式（基于实际问题发现）
  forbiddenPatterns: [
    'new Date().toLocaleString()', // 本地时间，时区不确定
    'toUTCString()', // RFC1123 文本格式，非 ISO8601，对外格式不统一（应用 toISOString()）
    'getTime()', // 裸时间戳，不含时区标识；如需对外用 toISOString()
    'toString()' // 含时区文本，格式不统一
  ],

  /*
   * 推荐的时间处理方式
   * B-2：API 对外时间字段统一 UTC ISO（toISOString() 产出 ...Z）；
   * 模型时间字段裸传（dateStrings:false 下 Sequelize JSON 自动输出 UTC ISO，无需手动包装）；
   * 北京时间展示与相对时间在前端统一组件完成。
   */
  recommendedPatterns: [
    'date.toISOString() // → UTC ISO8601 带 Z（API对外标准）',
    '模型时间字段裸传（不再用 formatForAPI/formatChinese 包装）',
    'BeijingTimeHelper.apiTimestamp() // 仅系统级响应/日志的北京ISO',
    '前端：new Date(zStr) 后强制按 Asia/Shanghai 展示'
  ],

  // 验证时间处理代码规范性
  validateTimeUsage: (codeContent) => {
    const violations = [];
    
    BEIJING_TIME_STANDARD_SYSTEM.forbiddenPatterns.forEach(pattern => {
      if (codeContent.includes(pattern)) {
        violations.push(`禁用模式: ${pattern}`);
      }
    });

    if (violations.length > 0) {
      console.error('🚫 时间处理代码违规:');
      violations.forEach(v => console.error(`   ❌ ${v}`));
      console.error('💡 推荐使用:');
      BEIJING_TIME_STANDARD_SYSTEM.recommendedPatterns.forEach(p => 
        console.error(`   ✅ ${p}`)
      );
      return false;
    }

    console.log('✅ 时间处理代码符合北京时间规范');
    return true;
  }
};
```

## 📅 **日期格式统一规范（北京时间）**

### **标准化日期格式定义和处理**
```javascript
// 🔴 强制要求：统一日期格式标准系统（北京时间GMT+8）
const DATE_FORMAT_STANDARD_SYSTEM = {
  // 标准格式定义
  formats: {
    // ISO格式：API对外传输统一形态（UTC，带 Z）
    iso: {
      format: 'YYYY-MM-DDTHH:mm:ss.sssZ',
      example: '2026-06-24T19:14:09.000Z',
      usage: 'API传输（对外统一）、跨端无歧义；展示转北京交前端'
    },
    // 北京本地ISO（仅 /health 等少数系统响应或内部日志可用，非业务字段对外形态）
    isoBeijing: {
      format: 'YYYY-MM-DDTHH:mm:ss.sss+08:00',
      example: '2026-06-25T03:14:09.000+08:00',
      usage: '系统级响应时间戳（ApiResponse middleware）、日志记录'
    },
    // 显示格式：用户界面（北京时间）
    display: {
      format: 'YYYY-MM-DD HH:mm:ss',
      example: '2025-10-13 20:19:57', 
      usage: '前端显示、报表生成、用户通知'
    },
    // 中文显示格式：用户友好界面
    displayCN: {
      format: 'YYYY年MM月DD日 HH:mm:ss',
      example: '2025年10月13日 20:19:57',
      usage: '用户通知、管理后台、报表标题'
    },
    // 日期格式：仅日期
    date: {
      format: 'YYYY-MM-DD',
      example: '2025-10-13',
      usage: '日期选择器、报表标题、统计分组'
    },
    // 时间格式：仅时间
    time: {
      format: 'HH:mm:ss',
      example: '20:19:57',
      usage: '时间选择器、运行时间显示'
    }
  },

  // 统一格式化工具类（北京时间）
  formatter: {
    // 转换为北京时间ISO格式（系统内部标准）
    toISO: (date) => {
      const d = typeof date === 'string' ? new Date(date) : date;
      const beijingDate = new Date(d.toLocaleString('en-US', { timeZone: 'Asia/Shanghai' }));
      return beijingDate.toISOString().replace('Z', '+08:00');
    },
    
    // 转换为显示格式（北京时间）
    toDisplay: (date) => {
      const d = typeof date === 'string' ? new Date(date) : date;
      return new Intl.DateTimeFormat('zh-CN', {
        year: 'numeric', month: '2-digit', day: '2-digit',
        hour: '2-digit', minute: '2-digit', second: '2-digit',
        timeZone: 'Asia/Shanghai', hour12: false
      }).format(d).replace(/\//g, '-');
    },
    
    // 转换为中文显示格式（北京时间）
    toDisplayCN: (date) => {
      const d = typeof date === 'string' ? new Date(date) : date;
      return d.toLocaleString('zh-CN', {
        timeZone: 'Asia/Shanghai',
        year: 'numeric', month: '2-digit', day: '2-digit',
        hour: '2-digit', minute: '2-digit', second: '2-digit',
        hour12: false
      });
    },
    
    // 仅日期格式（北京时间）
    toDate: (date) => {
      const d = typeof date === 'string' ? new Date(date) : date;
      return d.toLocaleString('zh-CN', {
        timeZone: 'Asia/Shanghai',
        year: 'numeric', month: '2-digit', day: '2-digit'
      }).replace(/\//g, '-');
    },
    
    // 仅时间格式（北京时间）
    toTime: (date) => {
      const d = typeof date === 'string' ? new Date(date) : date;
      return d.toLocaleString('zh-CN', {
        timeZone: 'Asia/Shanghai',
        hour: '2-digit', minute: '2-digit', second: '2-digit',
        hour12: false
      });
    },
    
    // 相对时间显示（如：1小时前）- 基于北京时间
    toRelative: (dateTime) => {
      const now = new Date(new Date().toLocaleString('en-US', { timeZone: 'Asia/Shanghai' }));
      const time = new Date(new Date(dateTime).toLocaleString('en-US', { timeZone: 'Asia/Shanghai' }));
      const diffMs = now - time;
      const diffMinutes = Math.floor(diffMs / 60000);
      const diffHours = Math.floor(diffMinutes / 60);
      const diffDays = Math.floor(diffHours / 24);
      
      if (diffDays > 0) return `${diffDays}天前`;
      if (diffHours > 0) return `${diffHours}小时前`;  
      if (diffMinutes > 0) return `${diffMinutes}分钟前`;
      return '刚刚';
    }
  }
};
```

## 🆔 **安全ID生成规范**

### **解决时间戳冲突的ID生成系统**
```javascript
// 🔴 解决cursor文档中发现的Date.now()冲突风险
const SAFE_ID_GENERATION_SYSTEM = {
  // 内部计数器（避免高并发冲突）
  _counter: 0,
  _sequence: 0,

  // ID生成策略库
  strategies: {
    // 高精度时间戳（包含毫秒+随机数+计数器）
    highPrecision: () => {
      const now = Date.now();
      const random = Math.random().toString(36).substr(2, 6);
      const counter = (SAFE_ID_GENERATION_SYSTEM._counter++ % 1000).toString().padStart(3, '0');
      return `${now}_${random}_${counter}`;
    },

    // UUID v4（完全随机，无时间依赖）
    uuid: () => {
      return require('uuid').v4();
    },

    // 雪花算法（分布式唯一ID）
    snowflake: (workerId = 1, datacenterId = 1) => {
      const timestamp = Date.now() - 1640995200000; // 2022-01-01 epoch
      const sequence = (SAFE_ID_GENERATION_SYSTEM._sequence++ % 4096);
      
      return ((timestamp << 22) | (datacenterId << 17) | (workerId << 12) | sequence).toString();
    }
  },

  // 业务ID生成器
  generateBusinessId: {
    // 消息ID：msg_20251003170511_a1b2c3d4
    message: () => {
      const timestamp = DATE_FORMAT_STANDARD_SYSTEM.formatter.toISO(new Date());
      const uuid = require('uuid').v4().substr(0, 8);
      return `msg_${timestamp.replace(/[-:T.]/g, '')}_${uuid}`;
    },

    // 会话ID：session_20251003_a1b2c3d4e5f6
    session: () => {
      const dateStr = DATE_FORMAT_STANDARD_SYSTEM.formatter.toDate(new Date()).replace(/-/g, '');
      const uuid = require('uuid').v4().substr(0, 12);
      return `session_${dateStr}_${uuid}`;
    },

    // 临时ID：temp_1728048311123_a1b2c3_001
    temporary: (prefix = 'temp') => {
      return `${prefix}_${SAFE_ID_GENERATION_SYSTEM.strategies.highPrecision()}`;
    },

    // 事务ID：用于数据库事务追踪
    transaction: () => {
      return `tx_${SAFE_ID_GENERATION_SYSTEM.strategies.snowflake()}`;
    }
  }
};
```

## ⏱️ **时间计算工具类标准化（北京时间）**

### **标准时间计算和查询工具**
```javascript
// 🔴 解决复杂时间计算的标准化工具类（北京时间GMT+8）
class StandardTimeCalculator {
  // 今日时间范围（北京时间基准）
  static getTodayRange() {
    const now = new Date();
    const beijingNow = new Date(now.toLocaleString('en-US', { timeZone: 'Asia/Shanghai' }));
    
    const start = new Date(beijingNow);
    start.setHours(0, 0, 0, 0);
    
    const end = new Date(beijingNow);
    end.setHours(23, 59, 59, 999);
    
    return {
      start: DATE_FORMAT_STANDARD_SYSTEM.formatter.toISO(start),
      end: DATE_FORMAT_STANDARD_SYSTEM.formatter.toISO(end),
      display: `${DATE_FORMAT_STANDARD_SYSTEM.formatter.toDate(start)} 全天`
    };
  }

  // N天前的时间（北京时间）
  static daysAgo(days) {
    const now = new Date();
    const beijingNow = new Date(now.toLocaleString('en-US', { timeZone: 'Asia/Shanghai' }));
    const past = new Date(beijingNow.getTime() - days * 24 * 60 * 60 * 1000);
    return DATE_FORMAT_STANDARD_SYSTEM.formatter.toISO(past);
  }

  // N小时前的时间（北京时间）
  static hoursAgo(hours) {
    const now = new Date();
    const beijingNow = new Date(now.toLocaleString('en-US', { timeZone: 'Asia/Shanghai' }));
    const past = new Date(beijingNow.getTime() - hours * 60 * 60 * 1000);
    return DATE_FORMAT_STANDARD_SYSTEM.formatter.toISO(past);
  }

  // 时间差计算（返回易读格式）
  static timeDifference(startTime, endTime) {
    const start = new Date(startTime);
    const end = new Date(endTime);
    const diffMs = end - start;
    
    if (diffMs < 0) return '时间顺序错误';
    
    const seconds = Math.floor(diffMs / 1000);
    const minutes = Math.floor(seconds / 60);
    const hours = Math.floor(minutes / 60);
    const days = Math.floor(hours / 24);
    
    if (days > 0) return `${days}天${hours % 24}小时${minutes % 60}分钟`;
    if (hours > 0) return `${hours}小时${minutes % 60}分钟${seconds % 60}秒`;
    if (minutes > 0) return `${minutes}分钟${seconds % 60}秒`;
    return `${seconds}秒`;
  }

  // 数据库查询时间范围生成器（北京时间）
  static createQueryRange(period) {
    const now = new Date();
    const beijingNow = new Date(now.toLocaleString('en-US', { timeZone: 'Asia/Shanghai' }));
    
    const ranges = {
      today: StandardTimeCalculator.getTodayRange(),
      yesterday: {
        start: StandardTimeCalculator.daysAgo(1),
        end: StandardTimeCalculator.daysAgo(0)
      },
      lastWeek: {
        start: StandardTimeCalculator.daysAgo(7),
        end: DATE_FORMAT_STANDARD_SYSTEM.formatter.toISO(beijingNow)
      },
      lastMonth: {
        start: StandardTimeCalculator.daysAgo(30),
        end: DATE_FORMAT_STANDARD_SYSTEM.formatter.toISO(beijingNow)
      }
    };

    return ranges[period] || ranges.today;
  }
}

// 数据库查询示例（替代复杂的时间计算）
const timeQueryExamples = {
  // 今日数据查询
  todayData: () => ({
    created_at: {
      [Op.gte]: StandardTimeCalculator.getTodayRange().start,
      [Op.lte]: StandardTimeCalculator.getTodayRange().end
    }
  }),
  
  // 最近一周数据
  recentData: (days = 7) => ({
    created_at: {
      [Op.gte]: StandardTimeCalculator.daysAgo(days)
    }
  })
};
```

## 📝 **日志时间戳标准化系统（北京时间）**

### **统一日志时间格式**
```javascript
// 🔴 统一日志时间戳格式系统（北京时间GMT+8）
class StandardLogger {
  // 格式化北京时间日志时间戳
  static formatLogTime() {
    const now = new Date();
    return now.toLocaleString('zh-CN', {
      timeZone: 'Asia/Shanghai',
      year: 'numeric', month: '2-digit', day: '2-digit',
      hour: '2-digit', minute: '2-digit', second: '2-digit',
      hour12: false
    });
  }

  static createLogEntry(level, message, data = {}) {
    const timestamp = StandardLogger.formatLogTime();
    const logEntry = {
      timestamp,
      level: level.toUpperCase(),
      message,
      ...data
    };

    // 标准化日志输出格式（北京时间）
    const formattedOutput = `[${timestamp}] ${logEntry.level}: ${message}`;
    
    if (Object.keys(data).length > 0) {
      console.log(formattedOutput, data);
    } else {
      console.log(formattedOutput);
    }

    return logEntry;
  }

  // 标准日志级别方法
  static info(message, data) { return StandardLogger.createLogEntry('info', message, data); }
  static warn(message, data) { return StandardLogger.createLogEntry('warn', message, data); }
  static error(message, data) { return StandardLogger.createLogEntry('error', message, data); }
  static debug(message, data) { return StandardLogger.createLogEntry('debug', message, data); }

  // 操作日志（用于记录重要操作）- 北京时间
  static operation(operationType, details) {
    return StandardLogger.createLogEntry('operation', `${operationType}操作`, {
      type: operationType,
      details: details,
      timestamp: StandardLogger.formatLogTime()
    });
  }
}

// 使用示例和标准（北京时间）
// ✅ 正确使用
StandardLogger.info('服务启动', { port: 3000, env: 'development' });
// 输出: [2025/10/13 20:19:57] INFO: 服务启动 { port: 3000, env: 'development' }

// ❌ 禁止使用
// console.log('📅 时间:', new Date().toISOString()); // UTC时间，不适用于中国区域
// console.log('📅 时间:', new Date().toUTCString()); // UTC时间，不适用于中国区域
```

## ⏰ **时间数据验证和错误处理（北京时间）**

### **安全时间数据处理系统**
```javascript
// 🔴 强制要求：时间数据验证和错误处理系统（北京时间）
class TimeDataValidator {
  // 时间格式验证（支持北京时间格式）
  static isValidBeijingTime(timeString) {
    try {
      const date = new Date(timeString);
      if (isNaN(date.getTime())) return false;
      
      // 验证是否包含+08:00时区信息或为有效的日期时间字符串
      return timeString.includes('+08:00') || 
             timeString.match(/^\d{4}-\d{2}-\d{2}/) || 
             !isNaN(date.getTime());
    } catch {
      return false;
    }
  }

  // 时间范围验证
  static isValidRange(startTime, endTime) {
    try {
      const start = new Date(startTime);
      const end = new Date(endTime);
      return !isNaN(start.getTime()) && !isNaN(end.getTime()) && start <= end;
    } catch {
      return false;
    }
  }

  // 安全时间解析（转换为北京时间）
  static safeParse(timeString, defaultValue = null) {
    try {
      const date = new Date(timeString);
      if (isNaN(date.getTime())) {
        console.warn(`⚠️ 无效时间字符串: ${timeString}`);
        return defaultValue;
      }
      // 返回北京时间ISO格式
      return DATE_FORMAT_STANDARD_SYSTEM.formatter.toISO(date);
    } catch (error) {
      console.error(`❌ 时间解析错误: ${error.message}`);
      return defaultValue;
    }
  }

  // 时间范围安全解析（北京时间）
  static parseTimeRange(startTime, endTime) {
    const start = TimeDataValidator.safeParse(startTime);
    const end = TimeDataValidator.safeParse(endTime);
    
    if (!start || !end) {
      throw new Error('时间范围解析失败');
    }

    if (!TimeDataValidator.isValidRange(start, end)) {
      throw new Error('无效的时间范围：开始时间必须早于结束时间');
    }

    return { start, end };
  }
}

// API参数时间验证中间件（北京时间）
function validateTimeParamsMiddleware(req, res, next) {
  const { start_time, end_time } = req.query;
  
  try {
    if (start_time && !TimeDataValidator.isValidBeijingTime(start_time)) {
      return res.status(400).json({ 
        error: 'Invalid start_time format, use ISO 8601 with Beijing time (YYYY-MM-DDTHH:mm:ss.sss+08:00) or YYYY-MM-DD HH:mm:ss',
        example: '2025-10-13T20:19:57.000+08:00 or 2025-10-13 20:19:57'
      });
    }

    if (end_time && !TimeDataValidator.isValidBeijingTime(end_time)) {
      return res.status(400).json({ 
        error: 'Invalid end_time format, use ISO 8601 with Beijing time (YYYY-MM-DDTHH:mm:ss.sss+08:00) or YYYY-MM-DD HH:mm:ss',
        example: '2025-10-13T20:19:57.000+08:00 or 2025-10-13 20:19:57'
      });
    }

    if (start_time && end_time && !TimeDataValidator.isValidRange(start_time, end_time)) {
      return res.status(400).json({ 
        error: 'Invalid time range: start_time must be before end_time' 
      });
    }

    next();
  } catch (error) {
    return res.status(400).json({ error: error.message });
  }
}
```

## 💾 **时间计算缓存优化系统**

### **频繁时间计算结果缓存**
```javascript
// 🔴 时间计算缓存系统（提升性能）
class TimeCalculationCache {
  constructor() {
    this.cache = new Map();
    this.cacheTTL = 60000; // 1分钟缓存
  }

  // 缓存时间计算结果
  getCachedCalculation(cacheKey, calculationFunction) {
    const cached = this.cache.get(cacheKey);
    const now = Date.now();
    
    // 检查缓存是否有效
    if (cached && (now - cached.timestamp) < this.cacheTTL) {
      console.log(`💾 使用缓存: ${cacheKey} (${Math.round((now-cached.timestamp)/1000)}秒前)`);
      return cached.value;
    }
    
    // 执行计算并缓存
    console.log(`⏳ 计算: ${cacheKey}`);
    const value = calculationFunction();
    
    this.cache.set(cacheKey, {
      value: value,
      timestamp: now
    });
    
    return value;
  }

  // 常用时间计算的缓存方法
  getTodayStart() {
    return this.getCachedCalculation('today_start', () => 
      StandardTimeCalculator.getTodayRange().start);
  }

  getTodayEnd() {
    return this.getCachedCalculation('today_end', () => 
      StandardTimeCalculator.getTodayRange().end);
  }

  getCurrentWeekStart() {
    return this.getCachedCalculation('week_start', () => {
      const now = new Date();
      const weekStart = new Date(now.getTime() - (now.getDay() * 24 * 60 * 60 * 1000));
      return DATE_FORMAT_STANDARD_SYSTEM.formatter.toISO(weekStart);
    });
  }

  // 清理过期缓存
  cleanup() {
    const now = Date.now();
    for (const [key, item] of this.cache.entries()) {
      if (now - item.timestamp > this.cacheTTL) {
        this.cache.delete(key);
      }
    }
  }
}

// 全局时间缓存实例
const globalTimeCache = new TimeCalculationCache();
setInterval(() => globalTimeCache.cleanup(), 60000); // 每分钟清理过期缓存
```

## 🧪 **时间处理测试验证规范（北京时间）**

### **时间处理功能单元测试标准**
```javascript
// 🔴 强制要求：时间处理功能必须有完整的单元测试（北京时间）
describe('时间处理标准化测试（北京时间）', () => {
  describe('StandardTimeCalculator', () => {
    test('getTodayRange返回今日北京时间范围', () => {
      const range = StandardTimeCalculator.getTodayRange();
      // 验证包含+08:00时区信息或为北京时间格式
      expect(range.start).toMatch(/\+08:00$|T00:00:00/);
      expect(range.end).toMatch(/\+08:00$|T23:59:59/);
      expect(range.display).toContain('全天');
    });

    test('daysAgo计算正确的过去时间（北京时间）', () => {
      const threeDaysAgo = StandardTimeCalculator.daysAgo(3);
      const now = new Date();
      const past = new Date(threeDaysAgo);
      const diffDays = Math.floor((now - past) / (24 * 60 * 60 * 1000));
      expect(diffDays).toBeGreaterThanOrEqual(2);
      expect(diffDays).toBeLessThanOrEqual(4); // 考虑时区差异
    });

    test('timeDifference格式化时间差显示', () => {
      const start = '2025-10-13T20:00:00.000+08:00';
      const end = '2025-10-13T20:05:11.000+08:00';
      const diff = StandardTimeCalculator.timeDifference(start, end);
      expect(diff).toBe('5分钟11秒');
    });
  });

  describe('TimeDataValidator', () => {
    test('验证北京时间格式正确性', () => {
      expect(TimeDataValidator.isValidBeijingTime('2025-10-13T20:19:57.000+08:00')).toBe(true);
      expect(TimeDataValidator.isValidBeijingTime('2025-10-13 20:19:57')).toBe(true);
      expect(TimeDataValidator.isValidBeijingTime('2025-10-13')).toBe(true);
      expect(TimeDataValidator.isValidBeijingTime('invalid')).toBe(false);
    });

    test('验证时间范围正确性', () => {
      const start = '2025-10-13T20:00:00.000+08:00';
      const end = '2025-10-13T20:05:11.000+08:00';
      expect(TimeDataValidator.isValidRange(start, end)).toBe(true);
      expect(TimeDataValidator.isValidRange(end, start)).toBe(false);
    });
  });

  describe('DATE_FORMAT_STANDARD_SYSTEM', () => {
    test('北京时间格式化正确', () => {
      const date = new Date('2025-10-13T20:19:57.000+08:00');
      const display = DATE_FORMAT_STANDARD_SYSTEM.formatter.toDisplay(date);
      expect(display).toMatch(/2025-10-13 \d{2}:\d{2}:\d{2}/);
    });

    test('中文显示格式正确', () => {
      const date = new Date('2025-10-13T20:19:57.000+08:00');
      const displayCN = DATE_FORMAT_STANDARD_SYSTEM.formatter.toDisplayCN(date);
      expect(displayCN).toContain('2025');
      expect(displayCN).toContain('10');
      expect(displayCN).toContain('13');
    });
  });

  describe('SAFE_ID_GENERATION_SYSTEM', () => {
    test('生成的ID应该是唯一的', () => {
      const ids = new Set();
      for (let i = 0; i < 1000; i++) {
        const id = SAFE_ID_GENERATION_SYSTEM.strategies.highPrecision();
        expect(ids.has(id)).toBe(false);
        ids.add(id);
      }
    });

    test('业务ID格式应该正确', () => {
      const messageId = SAFE_ID_GENERATION_SYSTEM.generateBusinessId.message();
      expect(messageId).toMatch(/^msg_\d{14}_[a-f0-9]{8}$/);
      
      const sessionId = SAFE_ID_GENERATION_SYSTEM.generateBusinessId.session();
      expect(sessionId).toMatch(/^session_\d{8}_[a-f0-9]{12}$/);
    });
  });
});
```

## 🔄 **时间数据迁移工具（北京时间）**

### **现有时间数据迁移和验证**
```sql
-- 时区数据迁移和验证脚本
-- 🔴 基于实际时区不一致问题的迁移方案（统一为北京时间）

-- 1. 设置数据库时区为北京时间
SET time_zone = '+08:00';
SET SESSION time_zone = 'Asia/Shanghai';

-- 2. 创建迁移脚本（确保所有时间数据为北京时间）
CREATE PROCEDURE MigrateTimeDataToBeijingTime()
BEGIN
  DECLARE done INT DEFAULT FALSE;
  DECLARE table_name VARCHAR(64);
  DECLARE time_column VARCHAR(64);
  
  -- 声明游标遍历所有时间字段
  DECLARE time_cursor CURSOR FOR 
    SELECT TABLE_NAME, COLUMN_NAME 
    FROM INFORMATION_SCHEMA.COLUMNS 
    WHERE TABLE_SCHEMA = DATABASE() 
    AND DATA_TYPE IN ('datetime', 'timestamp');
  
  DECLARE CONTINUE HANDLER FOR NOT FOUND SET done = TRUE;
  
  -- 开启事务
  START TRANSACTION;
  
  OPEN time_cursor;
  
  read_loop: LOOP
    FETCH time_cursor INTO table_name, time_column;
    IF done THEN
      LEAVE read_loop;
    END IF;
    
    -- 确保时间字段存储的是北京时间
    -- 如果数据原本是UTC，需要加8小时转换为北京时间
    SET @sql = CONCAT('UPDATE ', table_name, 
                     ' SET ', time_column, ' = DATE_ADD(', time_column, ', INTERVAL 8 HOUR)',
                     ' WHERE ', time_column, ' IS NOT NULL');
    PREPARE stmt FROM @sql;
    EXECUTE stmt;
    DEALLOCATE PREPARE stmt;
    
  END LOOP;
  
  CLOSE time_cursor;
  COMMIT;
  
  SELECT '✅ 时区数据迁移完成（北京时间）' as result;
END;

-- 3. 验证迁移结果（北京时间）
SELECT 
  'users' as table_name,
  COUNT(*) as total_records,
  MIN(created_at) as earliest_time,
  MAX(created_at) as latest_time,
  DATE_FORMAT(created_at, '%Y-%m-%d %H:%i:%s') as sample_time
FROM users
LIMIT 1;

-- 4. 验证时区设置
SELECT @@global.time_zone, @@session.time_zone;

-- 5. 确保所有新记录使用北京时间
-- 在应用启动时执行：
-- SET time_zone = '+08:00';
```

## 📋 **时间处理实施检查清单（北京时间）**

### **每次时间相关开发前**
- [ ] 确认使用**北京时间（GMT+8/Asia/Shanghai）**作为内部存储标准
- [ ] 验证时间格式使用ISO8601标准（包含+08:00时区信息）
- [ ] 检查时区处理的一致性（统一使用Asia/Shanghai）
- [ ] 准备时间数据验证机制（支持北京时间格式）

### **数据库时间字段设计时**
- [ ] 使用Sequelize的DataTypes.DATE类型
- [ ] 数据库连接配置中设置 `timezone: '+08:00'`
- [ ] 建立统一的时间查询方法（使用StandardTimeCalculator）
- [ ] 确保数据库会话时区为 `Asia/Shanghai`

### **前端时间显示时**
- [ ] 统一显示北京时间（不需要时区转换）
- [ ] 使用DATE_FORMAT_STANDARD_SYSTEM.formatter格式化函数
- [ ] 提供相对时间显示选项（如：1小时前）
- [ ] 中文界面使用中文日期格式（2025年10月13日 20:19:57）

### **API时间参数处理时**
- [ ] 强制使用ISO8601格式（包含+08:00时区）
- [ ] 添加时间参数验证中间件（validateTimeParamsMiddleware）
- [ ] 提供清晰的错误信息（包含北京时间示例）
- [ ] 记录时间相关的操作日志（使用StandardLogger）

### **配置文件检查**
- [ ] config/database.js 中设置 `timezone: '+08:00'`
- [ ] Sequelize初始化时设置 `dialectOptions: { timezone: '+08:00' }`
- [ ] 确保环境变量 TZ='Asia/Shanghai'（如有需要）

---

**核心原则**: 北京时间标准化、格式强制统一（+08:00）、时区处理一致（Asia/Shanghai）、ID生成安全、验证机制完整