# 前端运营体验问题审计报告

> **审计范围**: `/admin/src/` 全部 JS/HTML 文件  
> **审计日期**: 2026-02-06  
> **审计方法**: 代码级全量扫描 + 模式匹配验证  
> **技术栈核实**: 2026-02-07 — 已对照源码逐条验证  
> **结论**: 附录 13 项问题 **全部确认存在**（#1 已接受、#8 不修复，其余 11 项待修复）  
> **修复决策**: 2026-02-07 确认  
> **策略变更**: 项目未上线，采用「统一组件一次到位」策略 — 注册 `data-table` 为标准表格组件，26 个页面全量迁移，#9/#10/#4/#3/#12/#13 六项顺带解决

### 项目实际技术栈

| 层面 | 技术 | 版本 | 说明 |
|---|---|---|---|
| 框架 | Alpine.js | ^3.15.4 | `Alpine.data()` 注册组件 + `Alpine.store()` 状态管理 |
| 构建 | Vite | ^6.4.1 | MPA 多页应用，每个 HTML 独立入口 |
| 样式 | Tailwind CSS | ^3.4.19 | `darkMode: 'class'` 暗色模式 |
| 图表 | ECharts | ^6.0.0 | Canvas 渲染，不受 CSS 类控制 |
| 实时 | socket.io-client | ^4.8.3 | WebSocket 通知 |
| 模板 | vite-plugin-ejs | ^1.7.0 | HTML 模板注入 |
| 测试 | Playwright | ^1.58.1 | E2E 测试 |

**架构模式**: Vite MPA → 各 HTML 页面引入 `src/main.js` → 初始化 Alpine.js → 业务模块按 `src/modules/{domain}/` 组织，composable 模式拆分逻辑。通知使用 `Alpine.store('notification')` / `$toast` magic、确认使用 `confirmAndExecute` mixin + `confirm-dialog` store。

---

## 问题总览

| # | 问题 | 严重程度 | 修复决策 | 修复方案 |
|:---:|---|:---:|:---:|---|
| 1 | ~~错误提示暴露技术细节~~ | ✅ 已接受 | 不修 | 保留技术细节便于调试 |
| 2 | 数据刷新状态不透明 | 🔴 高 | **P1 修** | 有轮询的页面补 `lastUpdateTime` 展示 |
| 3 | 筛选重置覆盖不全 | 🟡 中 | **P1 修** | `filter-bar` + `data-table` 联动标准化，统一重置 |
| 4 | 分页默认值不统一 | 🟡 中 | **P1 修** | `data-table` 全局默认 20；exchange-stats 100；门店 100 保留 |
| 5 | 错误静默/反馈缺失 | 🔴 高 | **P1 修** | 扩展 `empty-state.js` 增加 error 变体 |
| 6 | 危险操作缺少二次确认 | 🟡 中 | **P2 修** | 原生 `confirm()` 全换 `confirmAndExecute` |
| 7 | 筛选条件刷新后丢失 | 🟡 中 | **P3 修** | URL params 同步，渐进式推进 |
| 8 | ~~图表环境色不跟随暗色主题~~ | ~~🟡 中~~ | **不修** | 运营白天办公用亮色，ROI 不够 |
| 9 | 已有组件未接入业务 | 🟡 中 | **P1 修** | 注册 `data-table` 为标准组件，26 个表格页全量迁移；`virtual-list` 保留给大数据列表 |
| 10 | 表格不支持列排序 | 🟡 中 | **P1 修** | `data-table` 内置 `sortable: true`，迁移后自动获得 |
| 11 | 部分页面无帮助提示 | 🟠 低 | **P3 修** | 补 `x-tooltip` / placeholder 覆盖 |
| 12 | 部分页面不支持数据导出 | 🟠 低 | **P2 修** | `data-table` 标准导出入口 `exportable: true`，联动 `export-modal.js` |
| 13 | 批量操作覆盖不全 | 🟠 低 | **P2 修** | `data-table` 内置 `selectable: true`，迁移后自动获得 |

---

## ~~1. 错误提示直接暴露技术细节~~ — ✅ 已接受

**状态**: ✅ 已接受 — 保留技术细节直接展示，便于运营协助调试和反馈问题

---

## 2. 数据刷新状态不透明 — P1 修

**严重程度**: 🔴 高 — 运营不知道当前数据是10秒前还是10分钟前的

**修复方案**: 有轮询的页面统一补 `lastUpdateTime` 展示，照 dashboard-panel 现有写法

### 轮询逻辑统计（17处 `setInterval`，跨13个文件）

| 文件 | setInterval数量 | 轮询内容 |
|---|:---:|---|
| `customer-service.js` | 2 | 会话列表(30s) + 响应统计(60s) |
| `dashboard-panel.js` | 1 | 仪表盘数据刷新 |
| `pending.js` (composable) | 1 | 待处理项更新 |
| `message-center.js` | 1 | 消息轮询 |
| `customer-service.js` (composable) | 1 | 消息内容轮询 |
| `lottery-alerts.js` | 2 | 告警列表(60s) × 2处 |
| `risk-alerts.js` | 3 | 告警列表(60s) × 2处 + 升级检查 |
| `sidebar-nav.js` | 1 | 徽章数字更新 |
| `notification-center.js` | 1 | 通知轮询 |
| `dashboard.js` | 1 | 仪表盘自动刷新 |
| `export-modal.js` | 1 | 导出进度轮询 |
| `assets-portfolio.js` | 1 | 重试定时器 |

### 「上次更新时间」展示情况

**仅 2 个页面** 在 UI 上展示了 `lastUpdateTime`：

```
dashboard-panel.html:59  → 实时监控核心业务指标 · 最后更新: <span x-text="lastUpdateTime">
pending-center.html:46   → 汇总所有待处理事项 · 最后更新: <span x-text="lastUpdateTime">
```

以下页面 **有轮询但无更新时间提示**：
- `lottery-alerts`（60秒轮询）
- `risk-alerts`（60秒轮询 × 3处）
- `customer-service`（30秒轮询）
- `message-center`（轮询）
- `notification-center`（轮询）
- `sidebar-nav`（徽章轮询）
- `dashboard`（轮询）

---

## 3. 筛选重置功能覆盖不全 — P1 修（由 #9 data-table 迁移顺带解决）

**严重程度**: 🟡 中 — 设了筛选条件后想看全量数据只能刷新整个页面

**修复方案**: `data-table` 增强后与 `filter-bar.js` 联动，统一提供「清除所有筛选」重置按钮，迁移后所有页面自动具备

### 现状

- `filter-bar.js` 组件提供了 `resetFilters` 方法
- 多个 composable 有 `resetFilters`/`clearSearch` 实现（`presets.js`、`user-hierarchy.js`、`audit-logs.js` 等）
- 但 `lottery-alerts`、`risk-alerts`、`sessions` 等页面的筛选逻辑是自行实现的，重置按钮覆盖参差不齐
- 没有发现统一的「清除所有筛选」按钮规范

---

## 4. 分页默认值不统一 — P1 修（由 #9 data-table 迁移顺带解决）

**严重程度**: 🟡 中 — 用户体验不一致，`page_size=1000` 会导致性能问题

**修复方案**: `data-table` 组件全局默认 `page_size: 20`，迁移后所有页面统一。个别覆盖：`exchange-stats` 100（统计聚合页一次看全貌）、门店列表 100 保留

### 各 page_size 值分布

| page_size | 使用数量 | 典型文件 |
|:---:|:---:|---|
| **20** | ~28处 | `user-management`、`sessions`、`lottery-alerts`、`risk-alerts`、`message-center`、`system-settings`、`staff`、`finance-management`、`orphan-frozen`、`pending`、`user-hierarchy`、`system-advance` 等 |
| **10** | ~6处 | `presets.js`、`batch-operations.js`、`user-drawer.js`（4个分页）、`drill-down.js`、`dashboard-panel.js`（实时告警） |
| **1000** | **2处** | `exchange-stats.js:57` 和 `exchange-stats.js:119` |
| **100** | 2处 | `stores.js:58`（门店列表）、`debt-management.js:63`（活动列表） |
| **50** | 1处 | `campaign-budget.js:66` |

#### 高风险项

```javascript
// exchange-stats.js:57 — 数据量增长后会突然变卡
params: { page: 1, page_size: 1000 }

// exchange-stats.js:119 — 同上
params: { page: 1, page_size: 1000 }
```

---

## 5. 错误静默/反馈缺失 — P1 修

**严重程度**: 🔴 高 — 加载失败时运营看到空白，不知道原因

**修复方案**: 扩展 `empty-state.js` 组件增加 error 变体（含重试按钮），各页面 API 失败后展示错误状态替代空白

### 问题证据

HTML 层面搜索 `x-if="resolveError"`，发现 **5处**（分布在4个页面，全部为表单 user_id 解析校验，非加载错误状态）：

```
user-management.html:1230     → <template x-if="resolveError">
user-hierarchy.html:238       → <template x-if="resolveError">
lottery-management.html:2884  → <template x-if="resolveError">
lottery-management.html:3868  → <template x-if="resolveError && quotaForm.rule_type === 'user'">
finance-management.html:995   → <template x-if="resolveError">
```

虽然有 `empty-state.js` 组件（15处相关代码），但该组件用于「数据为空」的提示，**没有「加载失败」的统一错误状态展示机制**。

多数页面在 API 失败后仅通过 toast 提示，内容区域保持空白或旧数据——如果 toast 自动消失后，运营完全无法知道发生了什么。

---

## 6. 危险操作缺少二次确认 — P2 修

**严重程度**: 🟡 中 — 实现方式不统一，部分操作缺少确认

**修复方案**: 原生 `confirm()` 全部替换为 `confirmAndExecute` mixin

### 现有确认机制

1. **`confirmAndExecute`**（推荐方式）：在 ~23 个文件中使用，如 `users.js` 的用户状态切换：
   ```javascript
   // users.js:331-344 — 正确实现
   await this.confirmAndExecute(
     `确定要${statusText}用户「${user.nickname || user.user_id}」吗？`,
     async () => { ... },
     { successMessage: `用户已${statusText}` }
   )
   ```

2. **原生 `confirm()`**（不推荐）：部分文件仍在使用：
   ```javascript
   // adjustment.js:517 — 使用原生 confirm
   if (!confirm(`确定要审批通过调账记录 ${record.adjustment_id} 吗？`)) return
   ```

3. **`confirm-dialog.js` store**：已定义（9处引用），但仅在 `alpine/init.js` 中集成

### 未覆盖的危险操作

需逐页排查以下场景是否均有确认：
- 资产调账审批（`adjustment.js` — 用原生 confirm）
- 批量操作（`batch-operations.js`、`pending.js`）
- 数据清理（`orphan-frozen.js`）
- 配置修改（`config-tools.js`、`system-settings.js`）

---

## 7. 筛选条件刷新后丢失 — P1 修（由 #9 data-table 迁移顺带解决）

**严重程度**: 🟡 中 — 不保存到 URL，刷新回初始状态

**修复方案**: `data-table` 增强内置 URL params 同步（筛选/分页/排序状态自动写入 URL），MPA 天然适合。迁移后所有页面自动支持刷新恢复 + URL 分享

### 现状分析

- `page-state.js` 已实现基于 `sessionStorage` 的状态保存/恢复机制（含 TTL 1小时过期策略）
- **但没有任何页面将筛选条件同步到 URL 参数**
- 全局搜索 `filter.*url`/`syncFiltersToURL`/`persistFilter` 等模式，仅找到 2 处不相关的结果

### 影响

- 刷新页面后依赖 `sessionStorage` 恢复（不可靠 + 有 1 小时过期）
- 无法通过 URL 分享当前筛选状态给同事
- 浏览器前进/后退无法恢复筛选状态

---

## ~~8. 图表环境色不跟随暗色主题~~ — 不修复

**决策**: 不修复 — 运营白天办公统一使用亮色模式，38 处 ECharts 环境色改动量大，ROI 不够。问题记录保留供未来参考。

<details>
<summary>原始问题记录（折叠）</summary>

### 硬编码颜色值分布（JS 文件中 `#hex` / `rgba` 出现次数）

| 文件 | 硬编码颜色数 | 是否含环境色问题 |
|---|:---:|:---:|
| `dashboard-panel.js` | 26 | ✅ |
| `analytics.js` | 16 | ✅ |
| `dashboard.js` | 16 | ✅ |
| `mini-chart.js` | 15 | ✅ |
| `statistics.js` | 14 | ✅ |
| `audit-logs.js` | 12 | ✅ |
| `lottery-alerts.js` | 12 | 部分 |
| `metrics.js` | 10 | 部分 |
| `user-management.js` | 9 | 部分 |
| `report.js` | 8 | 主要为导出色 |
| `appearance-settings.js` | 26 | 主题配置本身 |
| `其他文件` | 若干 | 多为语义固定色 |

需改的 ~38 处为图表环境色（轴线/背景/文字/tooltip），如 `backgroundColor: 'rgba(255,255,255,0.95)'` 在暗色下不可见。

</details>

---

## 9. 已有组件能力未接入业务 — P1 修（核心改造）

**严重程度**: 🟡 中 — 项目未上线，统一组件是降低长期维护成本的最佳窗口

**修复方案（策略变更）**: 项目未上线，不删代码而是**全量启用**。增强 `data-table.js` 为标准表格组件，在 `index.js` 中注册，26 个表格页面全量迁移为声明式 `columns + dataSource` 模式。`virtual-list.js` 注册保留给大数据量场景（如 exchange-stats）。

### 三个已定义但无业务页面使用的组件

| 组件 | 定义文件 | 注册状态 | 业务页面引用 | 处置 |
|---|---|:---:|:---:|:---:|
| `keyboard-shortcuts` | `alpine/components/keyboard-shortcuts.js` (581行) | ✅ store 已注册 | 仅 `workspace.html` 壳页面 | 保留现状 |
| `data-table` | `alpine/components/data-table.js` (225行) | ❌ 未注册 | **0** | **注册 + 增强 + 26 页迁移** |
| `virtual-list` | `alpine/components/virtual-list.js` (890行) | ❌ 未导入 | **0** | **注册，大数据列表页接入** |

### data-table 增强计划（源码核实后）

逐行读取现有源码后的精确评估：

#### ✅ 可直接复用（已实现，代码可用）

| 能力 | 源文件 | 现有代码行数 | 状态 |
|---|---|:---:|---|
| **前端排序** | `data-table.js:64-84` | 20行 | `sort(column)` 方法完整，含升序/降序切换、嵌套字段支持 |
| **行选择** | `data-table.js:94-129` | 35行 | `toggleSelectAll`/`toggleSelect`/`isSelected`/`getSelectedRows` 完整 |
| **单元格渲染** | `data-table.js:132-175` | 43行 | 支持 code/badge/status/date/datetime/boolean/currency/number/image/link 10种类型 |
| **空状态/错误状态** | `empty-state.js:27-47` | — | 已有 `error`/`network-error`/`search-empty`/`filter-empty` 预设，含操作按钮回调 |
| **筛选栏** | `filter-bar.js:23-121` | 98行 | 完整的 `search()`/`reset()`/`getValues()` + 高级筛选展开/收起 |
| **分页** | `pagination.js:24-346` | 322行 | 完整的分页状态 + 页码计算 + 预加载下一页 + `buildPaginationParams()` |
| **表格选择 mixin** | `table-selection.js:22-162` | 140行 | `toggleSelectAll`/`isPartialSelected`/`afterBatchOperation` 完整 |
| **异步数据加载** | `async-data.js:75+` | — | `createDataLoaderConfig()` 已提供标准化 `loadListData` 模式 |
| **页面状态持久化** | `page-state.js:46+` | — | sessionStorage + TTL + 缓存失效策略已完整 |
| **导出弹窗** | `export-modal.js` | 400行 | 支持 Excel/CSV/PDF、当前页/筛选/全量、字段选择、脱敏 |
| **虚拟滚动** | `virtual-list.js` | 890行 | `virtualList` + `virtualTable` 完整实现，支持动态行高 |

#### 🔧 需扩展（已有基础，需增强对接）

| 能力 | 现有基础 | 需要做的 | 关联问题 |
|---|---|---|:---:|
| **data-table 分页** | `data-table.js` 无分页；`paginationMixin` 独立存在 | data-table 内部 mixin `paginationMixin`，统一 `page_size: 20` | #4 |
| **data-table 选择对接批量操作** | `data-table.js` 有选择；`batch-operations.js` 独立 | 选择事件 `$dispatch` 对接 batch-operations | #13 |
| **data-table 联动 filter-bar** | 两个组件各自独立 | data-table 监听 filter-bar 的 `@search`/`@reset` 事件自动刷新 | #3 |
| **data-table 联动 export-modal** | export-modal 已有 `Alpine.store('exportModal').open()` | data-table 加 `exportable` 配置，工具栏加导出按钮调用 store | #12 |
| **data-table 联动 empty-state** | empty-state 已有 error 预设 | data-table 在 `loading=false && data.length===0` 或 `error` 时渲染 empty-state | #5 |
| **后端排序** | data-table 目前仅前端排序（内存 sort） | 增加 `serverSort: true` 模式，排序参数传入 API 请求 | #10 |

#### 🆕 需新建

| 能力 | 说明 | 关联问题 |
|---|---|:---:|
| **URL 状态同步** | 筛选/分页/排序参数写入 `URLSearchParams`，初始化时从 URL 恢复 | #7 |
| **data-table HTML 模板** | EJS partial：标准表格骨架 `<table>` + 分页 + 工具栏（现有页面都是裸写 HTML） | #9 迁移 |
| **`lastUpdateTime` mixin** | 轮询数据后自动记录时间，模板统一展示 | #2 |

### 现有页面模式分析

当前 26 个表格页的统一模式：

```
x-data="xxxPage()" x-init="init()"
  └── 页面函数 mixin 组合：asyncDataMixin() + paginationMixin() + tableSelectionMixin()
  └── HTML 裸写 <table>，手动 x-for 渲染行
  └── 手动写分页按钮 HTML
  └── 手动写空状态 HTML
```

迁移目标：

```
x-data="xxxPage()" x-init="init()"
  └── 页面函数只关心 columns 定义 + dataSource 函数 + actions
  └── <div x-data="dataTable({ columns, dataSource, sortable, selectable, exportable })">
  └── 分页/空状态/错误状态/工具栏 由 data-table 组件统一渲染
```

### 迁移范围（26 个表格页）

每个页面迁移后只需关心：
1. `columns` — 列定义（字段名、标题、宽度、是否可排序、渲染类型）
2. `dataSource` — 数据加载函数（返回 `{ items, total }`，复用现有 `createDataLoaderConfig`）
3. `actions` — 行操作按钮
4. 可选开关：`selectable`/`exportable`/`sortable`/`serverSort`

---

## 10. 表格不支持列排序 — P1 修（由 #9 data-table 迁移顺带解决）

**严重程度**: 🟡 中 — 只有约4个页面支持排序

**修复方案**: `data-table` 组件已内置排序能力（`sortable: true`），26 页迁移后自动获得列排序功能，无需额外开发。

### 迁移前后对比

| | 迁移前 | 迁移后 |
|---|---|---|
| 有排序的页面 | ~4 个（sessions/exchange-items/asset-management/dict） | **全部 26 个** |
| 排序实现方式 | 各页面自行实现 `sortField`/`sortDirection` | `data-table` 统一内置，columns 配置 `sortable: true` |
| 新增排序的成本 | 改 JS + 改 HTML | 列配置加一个字段 |

---

## 11. 部分页面无帮助提示 — P3 修

**严重程度**: 🟠 低 — 无 tooltip/placeholder 引导

**修复方案**: 利用已有 `x-tooltip` 指令，补筛选条件/操作按钮/数据指标的 hover 说明和 placeholder 文字

`dashboard-panel.js` 中 10 处 tooltip 相关代码全部是 ECharts 图表数据提示，不是 UI 操作引导。

大部分页面的筛选条件、操作按钮、数据指标缺乏辅助说明（如 placeholder 提示文字、hover tooltip 解释含义等），运营新人上手成本高。

---

## 12. 部分页面不支持数据导出 — P2 修（由 #9 data-table 迁移顺带解决）

**严重程度**: 🟠 低 — 运营需手动复制数据

**修复方案**: `data-table` 增强新增 `exportable: true` 配置项，联动已有 `export-modal.js` 框架。迁移后页面加一行配置即可获得导出能力

### 已有导出能力的页面

`export-modal.js`（24处相关代码）已提供统一导出框架，以下页面有导出功能：
- `statistics.js`（16处）
- `analytics.js`（24处）
- `audit-logs.js`（3处）
- `user-management.js`（7处）
- `user-hierarchy.js`（25处）
- `assets-portfolio.js`（6处）
- `report.js` / `daily-report.js`

### 缺少导出功能的页面

- `store-management`
- `content-management`
- `trade-management`
- `sessions`
- `dict-management`
- `config-tools`

---

## 13. 批量操作覆盖不全 — P2 修（由 #9 data-table 迁移顺带解决）

**严重程度**: 🟠 低 — 需要逐条操作

**修复方案**: `data-table` 已内置 `selectable: true` 选择能力，与 `batch-operations.js` 对接。迁移后页面加一行配置即可获得批量选择 + 操作能力

### 已有批量操作能力的页面

- `batch-operations.js`（81处相关代码，专门的批量操作 composable）
- `table-selection.js` mixin（8处相关代码）
- 已接入页面：`lottery-alerts`、`risk-alerts`、`sessions`、`pending-center`、`orphan-frozen`、`consumption`

### 缺少批量操作的页面

- `analytics`
- `dict-management`
- `content-management`
- `store-management`
- `customer-service`

---

## 修复执行计划

> **核心策略**: 项目未上线，一次性投入降低长期维护成本。以 `data-table` 统一组件为主线，一次迁移顺带解决 6 项问题。

### 第一阶段：data-table 组件增强（基础设施）

增强现有 `data-table.js`（225行），补齐缺失能力后在 `index.js` 注册：

| 增强项 | 关联问题 | 说明 |
|---|:---:|---|
| 全局默认 `page_size: 20` | #4 | 组件级统一，个别页面配置覆盖 |
| 列排序 `sortable` | #10 | 已有，确认 API 兼容 |
| 行选择 `selectable` | #13 | 已有，对接 `batch-operations.js` |
| 筛选联动 + 重置 | #3 | 新增，与 `filter-bar.js` 联动 |
| 导出入口 `exportable` | #12 | 新增，联动 `export-modal.js` |
| URL 状态同步 | #7 | 新增，筛选/分页/排序写入 URL params |
| 错误状态展示 | #5 | 新增，联动 `empty-state.js` error 变体 |

同步注册 `virtual-list.js`，供大数据量页面使用。

### 第二阶段：26 个表格页全量迁移（核心工作量）

将各页面裸 HTML `<table>` 迁移为 `x-data="dataTable({ columns, dataSource, ... })"` 声明式配置。迁移后每个页面自动获得：排序 / 分页统一 / 选择 / 导出 / 筛选重置 / URL 同步 / 错误状态。

### 第三阶段：独立问题修复

| # | 问题 | 具体动作 | 与 data-table 无关 |
|:---:|---|---|:---:|
| 5 | 加载失败无错误状态 | 扩展 `empty-state.js` 增加 error 变体（含重试按钮） | ✅ |
| 2 | 轮询无更新时间提示 | 有 `setInterval` 但无 `lastUpdateTime` 的 7 个页面补展示 | ✅ |
| 6 | 原生 confirm 未统一 | 逐文件替换 `confirm()` → `confirmAndExecute`（~18 文件） | ✅ |
| 11 | 缺帮助提示 | 补 `x-tooltip` / placeholder 覆盖 | ✅ |

### 不修复

| # | 问题 | 理由 |
|:---:|---|---|
| 1 | 错误提示暴露技术细节 | 已接受，保留便于调试 |
| 8 | 图表暗色模式 | 运营白天亮色办公，38处改动 ROI 不够 |

### 收益总结

| 指标 | 补丁策略（之前） | 统一组件策略（现在） |
|---|---|---|
| 一次性投入 | 低（逐个打补丁） | **高（增强组件 + 26 页迁移）** |
| 长期维护成本 | 高（每页自管表格逻辑） | **低（只维护一个组件）** |
| 新页面开发成本 | 复制粘贴 HTML + 重写逻辑 | **声明式配置，~10 行** |
| 一次投入解决问题数 | 逐项解决 11 项 | **一次迁移解决 6 项**（#3/#4/#7/#10/#12/#13），其余 5 项独立修 |
| 技术债务 | 保留（26 页各自为政） | **清零（统一组件标准）** |

---

## 附录A：技术栈核实结论（2026-02-07）

逐条对照 `admin/src/` 源码验证，报告中 **所有问题描述与实际技术框架一致**，无虚报。

| # | 核实结论 | 说明 |
|:---:|:---:|---|
| 2 | ✅ 准确 | `setInterval` 实际 17 处/13 文件（原报 15 处，已修正）；`lastUpdateTime` 仅 2 个 HTML 页面展示 |
| 3 | ✅ 准确 | `filter-bar.js` 组件存在 `resetFilters` 方法；业务页面各自实现，覆盖参差不齐 |
| 4 | ✅ 准确 | `page_size: 1000` 在 `exchange-stats.js:57,119` 实锤 |
| 5 | ✅ 准确 | `resolveError` 实际5处/4个HTML（原报2处，已修正），均为表单校验非加载错误；无统一加载失败状态组件 |
| 6 | ✅ 准确 | `confirmAndExecute` mixin ~23 文件使用；原生 `confirm()` ~18 文件仍在用；`confirm-dialog.js` store 已定义含7处引用 |
| 7 | ✅ 准确 | `page-state.js` 基于 sessionStorage 含 TTL 策略；无筛选→URL 同步机制 |
| 8 | ✅ 准确 | ECharts 在 Canvas 渲染不受 Tailwind `dark:` 类控制；`dashboard-panel.js` 单文件 26 处硬编码色值 |
| 9 | 🔧 已修正 | `data-table.js` / `virtual-list.js` 不仅未使用且未在 `index.js` 注册（死代码）；`keyboard-shortcuts` store 已注册，`workspace.html` 有引用但无业务页面调用 |
| 10 | ✅ 准确 | 排序仅 `sessions`/`exchange-items`/`dashboard-panel` 等 ~4 页面；`data-table.js` 组件有排序能力但未注册 |
| 11–13 | ✅ 准确 | `export-modal.js` 导出框架存在；`batch-operations.js` + `table-selection.js` mixin 存在；缺失页面列表与代码一致 |

---

## 附录B：搜索方法论

本报告中所有数据均通过以下方式获取：

| 验证项 | 搜索模式 | 搜索范围 |
|---|---|---|
| 错误暴露 | `showError(error.message`、`notification.*error.message` | `src/**/*.js` |
| 轮询逻辑 | `setInterval` | `src/**/*.js` |
| 更新时间提示 | `lastUpdateTime`、`最后更新` | `*.html` + `src/**/*.js` |
| 分页值 | `page_size` | `src/**/*.js` |
| 二次确认 | `confirmAndExecute`、`confirm(` | `src/**/*.js` |
| URL状态同步 | `syncFiltersToURL`、`pushState`、`replaceState` | `src/**/*.js` |
| 图表硬编码色 | `#[0-9a-fA-F]{6}`、`rgba(` | `src/**/*.js` |
| 组件使用 | `dataTable(`、`virtualList(`、`$store.shortcuts` | `*.html` + `src/**/*.js` |
| 排序 | `sortable`、`sort_by`、`orderBy` | `src/**/*.js` |
| 导出 | `export.*csv`、`导出`、`exportData` | `src/**/*.js` |
| 批量操作 | `batch`、`批量`、`selectAll`、`selectedItems` | `src/**/*.js` |

