console.log('开始检查抽奖配置...'); const { LotterySetting } = require('./models'); (async () => { try { const settings = await LotterySetting.findAll({ where: { status: 'active' }, order: [['angle', 'ASC']], attributes: ['prize_id', 'prize_name', 'prize_type', 'prize_value', 'angle', 'probability'] }); console.log('抽奖配置数据:', JSON.stringify(settings, null, 2)); process.exit(0); } catch (error) { console.error('查询失败:', error); process.exit(1); } })();
